vocabulary  V {
    type RuleNr isa int
    type Penalty isa int
    type Object
    type Time

    type Fork isa Object 
    type Knife isa Object
    type Spoon isa Object
    type SmallKnife isa Object
    
    type Plate isa Object
    type Bowl isa Object
    type PastaPlate isa Object
    type SoupPlate isa Object
    type Saucer isa Object

    type WaterGlass isa Object 
    type WineGlass isa Object
    type TeaCup isa Object

    type Breakfast isa Time
    type Lunch isa Time
    type Dinner isa Time

    type PossCo isa int

    fork(Fork)
    knife(Knife)
    spoon(Spoon)
    smallKnife(SmallKnife)

    plate(Plate)
    bowl(Bowl)
    pastaPlate(PastaPlate)
    soupPlate(SoupPlate)
    saucer(Saucer)

    waterGlass(WaterGlass)
    wineGlass(WineGlass)
    teaCup(TeaCup)

    food_container(Object)
    drink_container(Object)
    food_utensil(Object)

    above_of(Object, Object)
    left_of(Object, Object)
    on(Object, Object)

    position(PossCo,PossCo,PossCo,Object)
    time(Time)

    there_is_a_fork
    there_is_a_knife
    there_is_a_spoon
    there_is_a_smallKnife
    there_is_a_plate
    there_is_a_bowl
    there_is_a_pastaPlate
    there_is_a_soupPlate
    there_is_a_saucer
    there_is_a_waterGlass
    there_is_a_wineGlass
    there_is_a_teaCup
    
    breakfastTime(Breakfast)
    lunchTime(Lunch)
    dinnerTime(Dinner)

    breakfast
    lunch
    dinner
    
    penalty(RuleNr, Penalty)
}

term penaltyTerm:V{
    sum{X, Y : penalty(X, Y) : Y}
}

theory T : V {
// Necessary background rules
!A, B: above_of(A, B) <=>  ? X, Y1, Y2, Z1, Z2 :(position(X, Y1, Z1, A) & position(X, Y2, Z2, B) & Y1 < Y2).
!A, B: left_of(A, B) <=> ? X1, X2, Y, Z1, Z2 : (position(X1, Y, Z1, A) & position(X2, Y, Z2, B) & X1 + 1 = X2).
!A, B: on(A, B) <=> ? X, Y, Z1, Z2 : (position(X, Y, Z1, A) & position(X, Y, Z2, B) & Z1 < Z2).

(there_is_a_fork) <=> ?X, Y, Z, Object : position(X, Y, Z, Object) & fork(Object).
(there_is_a_knife) <=> ?X, Y, Z, Object : position(X, Y, Z, Object) & knife(Object).
(there_is_a_spoon) <=> ?X, Y, Z, Object : position(X, Y, Z, Object) & spoon(Object).
(there_is_a_smallKnife) <=> ?X, Y, Z, Object : position(X, Y, Z, Object) & smallKnife(Object).
(there_is_a_plate) <=> ?X, Y, Z, Object : position(X, Y, Z, Object) & plate(Object).
(there_is_a_bowl) <=> ?X, Y, Z, Object : position(X, Y, Z, Object) & bowl(Object).
(there_is_a_pastaPlate) <=> ?X, Y, Z, Object : position(X, Y, Z, Object) & pastaPlate(Object).
(there_is_a_soupPlate) <=> ?X, Y, Z, Object : position(X, Y, Z, Object) & soupPlate(Object).
(there_is_a_saucer) <=> ?X, Y, Z, Object : position(X, Y, Z, Object) & saucer(Object).
(there_is_a_waterGlass) <=> ?X, Y, Z, Object : position(X, Y, Z, Object) & waterGlass(Object).
(there_is_a_wineGlass) <=> ?X, Y, Z, Object : position(X, Y, Z, Object) & wineGlass(Object).
(there_is_a_teaCup) <=> ?X, Y, Z, Object : position(X, Y, Z, Object) & teaCup(Object).

breakfast <=> ?T : time(T) & breakfastTime(T).
lunch <=> ?T : time(T) & lunchTime(T).
dinner <=> ?T : time(T) & dinnerTime(T).


// Rules that always have to satisfy

// Maximum set of rules that have to satisfy with penalty
{
penalty(1, 1) <- ~( !X : ( left_of(X,X) ) => ( false)).
penalty(2, 1) <- ~( !X : ( above_of(X,X) ) => ( false)).
penalty(3, 1) <- ~( !X : ( on(X,X) ) => ( false)).
penalty(4, 1) <- ~( !X : ( fork(X) & knife(X) ) => ( false)).
penalty(5, 1) <- ~( !X : ( fork(X) & spoon(X) ) => ( false)).
penalty(6, 1) <- ~( !X : ( fork(X) & smallKnife(X) ) => ( false)).
penalty(7, 1) <- ~( !X : ( fork(X) & plate(X) ) => ( false)).
penalty(8, 1) <- ~( !X : ( fork(X) & bowl(X) ) => ( false)).
penalty(9, 1) <- ~( !X : ( fork(X) & saucer(X) ) => ( false)).
penalty(10, 1) <- ~( !X : ( fork(X) & pastaPlate(X) ) => ( false)).
penalty(11, 1) <- ~( !X : ( fork(X) & soupPlate(X) ) => ( false)).
penalty(12, 1) <- ~( !X : ( fork(X) & waterGlass(X) ) => ( false)).
penalty(13, 1) <- ~( !X : ( fork(X) & wineGlass(X) ) => ( false)).
penalty(14, 1) <- ~( !X : ( fork(X) & teaCup(X) ) => ( false)).
penalty(15, 1) <- ~( !X : ( fork(X) ) => ( there_is_a_fork)).
penalty(16, 1) <- ~( !X : ( fork(X) & left_of(X,X) ) => ( false)).
penalty(17, 1) <- ~( !X, Y : ( fork(X) & on(X,Y) ) => ( false)).
penalty(18, 1) <- ~( !X : ( fork(X) & food_container(X) ) => ( false)).
penalty(19, 1) <- ~( !X : ( fork(X) & drink_container(X) ) => ( false)).
penalty(20, 1) <- ~( !X : ( fork(X) ) => ( food_utensil(X))).
penalty(21, 1) <- ~( !X : ( knife(X) & spoon(X) ) => ( false)).
penalty(22, 1) <- ~( !X : ( knife(X) & smallKnife(X) ) => ( false)).
penalty(23, 1) <- ~( !X : ( knife(X) & plate(X) ) => ( false)).
penalty(24, 1) <- ~( !X : ( knife(X) & bowl(X) ) => ( false)).
penalty(25, 1) <- ~( !X : ( knife(X) & saucer(X) ) => ( false)).
penalty(26, 1) <- ~( !X : ( knife(X) & pastaPlate(X) ) => ( false)).
penalty(27, 1) <- ~( !X : ( knife(X) & soupPlate(X) ) => ( false)).
penalty(28, 1) <- ~( !X : ( knife(X) & waterGlass(X) ) => ( false)).
penalty(29, 1) <- ~( !X : ( knife(X) & wineGlass(X) ) => ( false)).
penalty(30, 1) <- ~( !X : ( knife(X) & teaCup(X) ) => ( false)).
penalty(31, 1) <- ~( !X : ( knife(X) ) => ( there_is_a_knife)).
penalty(32, 1) <- ~( !X : ( knife(X) ) => ( there_is_a_plate)).
penalty(33, 1) <- ~( !X : ( knife(X) & there_is_a_pastaPlate ) => ( false)).
penalty(34, 1) <- ~( !X : ( knife(X) & left_of(X,X) ) => ( false)).
penalty(35, 1) <- ~( !X, Y : ( knife(X) & on(X,Y) ) => ( false)).
penalty(36, 1) <- ~( !X : ( knife(X) & food_container(X) ) => ( false)).
penalty(37, 1) <- ~( !X : ( knife(X) & drink_container(X) ) => ( false)).
penalty(38, 1) <- ~( !X : ( knife(X) & dinner ) => ( false)).
penalty(39, 1) <- ~( !X : ( knife(X) ) => ( food_utensil(X))).
penalty(40, 1) <- ~( !X : ( spoon(X) & smallKnife(X) ) => ( false)).
penalty(41, 1) <- ~( !X : ( spoon(X) & plate(X) ) => ( false)).
penalty(42, 1) <- ~( !X : ( spoon(X) & bowl(X) ) => ( false)).
penalty(43, 1) <- ~( !X : ( spoon(X) & saucer(X) ) => ( false)).
penalty(44, 1) <- ~( !X : ( spoon(X) & pastaPlate(X) ) => ( false)).
penalty(45, 1) <- ~( !X : ( spoon(X) & soupPlate(X) ) => ( false)).
penalty(46, 1) <- ~( !X : ( spoon(X) & waterGlass(X) ) => ( false)).
penalty(47, 1) <- ~( !X : ( spoon(X) & wineGlass(X) ) => ( false)).
penalty(48, 1) <- ~( !X : ( spoon(X) & teaCup(X) ) => ( false)).
penalty(49, 1) <- ~( !X : ( spoon(X) ) => ( there_is_a_spoon)).
penalty(50, 1) <- ~( !X : ( spoon(X) & left_of(X,X) ) => ( false)).
penalty(51, 1) <- ~( !X, Y : ( spoon(X) & on(X,Y) ) => ( false)).
penalty(52, 1) <- ~( !X : ( spoon(X) & food_container(X) ) => ( false)).
penalty(53, 1) <- ~( !X : ( spoon(X) & drink_container(X) ) => ( false)).
penalty(54, 1) <- ~( !X : ( spoon(X) ) => ( food_utensil(X))).
penalty(55, 1) <- ~( !X : ( smallKnife(X) & plate(X) ) => ( false)).
penalty(56, 1) <- ~( !X : ( smallKnife(X) & bowl(X) ) => ( false)).
penalty(57, 1) <- ~( !X : ( smallKnife(X) & saucer(X) ) => ( false)).
penalty(58, 1) <- ~( !X : ( smallKnife(X) & pastaPlate(X) ) => ( false)).
penalty(59, 1) <- ~( !X : ( smallKnife(X) & soupPlate(X) ) => ( false)).
penalty(60, 1) <- ~( !X : ( smallKnife(X) & waterGlass(X) ) => ( false)).
penalty(61, 1) <- ~( !X : ( smallKnife(X) & wineGlass(X) ) => ( false)).
penalty(62, 1) <- ~( !X : ( smallKnife(X) & teaCup(X) ) => ( false)).
penalty(63, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_knife)).
penalty(64, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_smallKnife)).
penalty(65, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_plate)).
penalty(66, 1) <- ~( !X : ( smallKnife(X) & there_is_a_pastaPlate ) => ( false)).
penalty(67, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_saucer)).
penalty(68, 1) <- ~( !X : ( smallKnife(X) & left_of(X,X) ) => ( false)).
penalty(69, 1) <- ~( !X, Y : ( smallKnife(X) & left_of(X,Y) ) => ( false)).
penalty(70, 1) <- ~( !X, Y : ( smallKnife(X) & above_of(X,Y) ) => ( false)).
penalty(71, 1) <- ~( !X, Y : ( smallKnife(X) & on(X,Y) ) => ( false)).
penalty(72, 1) <- ~( !X : ( smallKnife(X) & food_container(X) ) => ( false)).
penalty(73, 1) <- ~( !X : ( smallKnife(X) & drink_container(X) ) => ( false)).
penalty(74, 1) <- ~( !X : ( smallKnife(X) & dinner ) => ( false)).
penalty(75, 1) <- ~( !X : ( smallKnife(X) ) => ( food_utensil(X))).
penalty(76, 1) <- ~( !X : ( plate(X) & bowl(X) ) => ( false)).
penalty(77, 1) <- ~( !X : ( plate(X) & saucer(X) ) => ( false)).
penalty(78, 1) <- ~( !X : ( plate(X) & pastaPlate(X) ) => ( false)).
penalty(79, 1) <- ~( !X : ( plate(X) & soupPlate(X) ) => ( false)).
penalty(80, 1) <- ~( !X : ( plate(X) & waterGlass(X) ) => ( false)).
penalty(81, 1) <- ~( !X : ( plate(X) & wineGlass(X) ) => ( false)).
penalty(82, 1) <- ~( !X : ( plate(X) & teaCup(X) ) => ( false)).
penalty(83, 1) <- ~( !X : ( plate(X) ) => ( there_is_a_knife)).
penalty(84, 1) <- ~( !X : ( plate(X) ) => ( there_is_a_plate)).
penalty(85, 1) <- ~( !X : ( plate(X) & there_is_a_pastaPlate ) => ( false)).
penalty(86, 1) <- ~( !X : ( plate(X) & left_of(X,X) ) => ( false)).
penalty(87, 1) <- ~( !X, Y : ( plate(X) & above_of(X,Y) ) => ( false)).
penalty(88, 1) <- ~( !X : ( plate(X) & food_utensil(X) ) => ( false)).
penalty(89, 1) <- ~( !X : ( plate(X) & drink_container(X) ) => ( false)).
penalty(90, 1) <- ~( !X : ( plate(X) & dinner ) => ( false)).
penalty(91, 1) <- ~( !X : ( plate(X) ) => ( food_container(X))).
penalty(92, 1) <- ~( !X : ( bowl(X) & saucer(X) ) => ( false)).
penalty(93, 1) <- ~( !X : ( bowl(X) & pastaPlate(X) ) => ( false)).
penalty(94, 1) <- ~( !X : ( bowl(X) & soupPlate(X) ) => ( false)).
penalty(95, 1) <- ~( !X : ( bowl(X) & waterGlass(X) ) => ( false)).
penalty(96, 1) <- ~( !X : ( bowl(X) & wineGlass(X) ) => ( false)).
penalty(97, 1) <- ~( !X : ( bowl(X) & teaCup(X) ) => ( false)).
penalty(98, 1) <- ~( !X : ( bowl(X) ) => ( there_is_a_spoon)).
penalty(99, 1) <- ~( !X : ( bowl(X) ) => ( there_is_a_bowl)).
penalty(100, 1) <- ~( !X : ( bowl(X) & there_is_a_pastaPlate ) => ( false)).
penalty(101, 1) <- ~( !X : ( bowl(X) & there_is_a_soupPlate ) => ( false)).
penalty(102, 1) <- ~( !X : ( bowl(X) & left_of(X,X) ) => ( false)).
penalty(103, 1) <- ~( !X, Y : ( bowl(X) & above_of(X,Y) ) => ( false)).
penalty(104, 1) <- ~( !X, Y : ( bowl(X) & on(X,Y) ) => ( false)).
penalty(105, 1) <- ~( !X : ( bowl(X) & food_utensil(X) ) => ( false)).
penalty(106, 1) <- ~( !X : ( bowl(X) & drink_container(X) ) => ( false)).
penalty(107, 1) <- ~( !X : ( bowl(X) & dinner ) => ( false)).
penalty(108, 1) <- ~( !X : ( bowl(X) ) => ( food_container(X))).
penalty(109, 1) <- ~( !X : ( saucer(X) & pastaPlate(X) ) => ( false)).
penalty(110, 1) <- ~( !X : ( saucer(X) & soupPlate(X) ) => ( false)).
penalty(111, 1) <- ~( !X : ( saucer(X) & waterGlass(X) ) => ( false)).
penalty(112, 1) <- ~( !X : ( saucer(X) & wineGlass(X) ) => ( false)).
penalty(113, 1) <- ~( !X : ( saucer(X) & teaCup(X) ) => ( false)).
penalty(114, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_knife)).
penalty(115, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_smallKnife)).
penalty(116, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_plate)).
penalty(117, 1) <- ~( !X : ( saucer(X) & there_is_a_pastaPlate ) => ( false)).
penalty(118, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_saucer)).
penalty(119, 1) <- ~( !X : ( saucer(X) & left_of(X,X) ) => ( false)).
penalty(120, 1) <- ~( !X, Y : ( saucer(X) & left_of(X,Y) ) => ( false)).
penalty(121, 1) <- ~( !X, Y : ( saucer(X) & above_of(X,Y) ) => ( false)).
penalty(122, 1) <- ~( !X : ( saucer(X) & food_utensil(X) ) => ( false)).
penalty(123, 1) <- ~( !X : ( saucer(X) & drink_container(X) ) => ( false)).
penalty(124, 1) <- ~( !X : ( saucer(X) & dinner ) => ( false)).
penalty(125, 1) <- ~( !X : ( saucer(X) ) => ( food_container(X))).
penalty(126, 1) <- ~( !X : ( pastaPlate(X) & soupPlate(X) ) => ( false)).
penalty(127, 1) <- ~( !X : ( pastaPlate(X) & waterGlass(X) ) => ( false)).
penalty(128, 1) <- ~( !X : ( pastaPlate(X) & wineGlass(X) ) => ( false)).
penalty(129, 1) <- ~( !X : ( pastaPlate(X) & teaCup(X) ) => ( false)).
penalty(130, 1) <- ~( !X : ( pastaPlate(X) ) => ( there_is_a_fork)).
penalty(131, 1) <- ~( !X : ( pastaPlate(X) & there_is_a_knife ) => ( false)).
penalty(132, 1) <- ~( !X : ( pastaPlate(X) ) => ( there_is_a_spoon)).
penalty(133, 1) <- ~( !X : ( pastaPlate(X) & there_is_a_smallKnife ) => ( false)).
penalty(134, 1) <- ~( !X : ( pastaPlate(X) & there_is_a_plate ) => ( false)).
penalty(135, 1) <- ~( !X : ( pastaPlate(X) & there_is_a_bowl ) => ( false)).
penalty(136, 1) <- ~( !X : ( pastaPlate(X) ) => ( there_is_a_pastaPlate)).
penalty(137, 1) <- ~( !X : ( pastaPlate(X) & there_is_a_soupPlate ) => ( false)).
penalty(138, 1) <- ~( !X : ( pastaPlate(X) & there_is_a_saucer ) => ( false)).
penalty(139, 1) <- ~( !X : ( pastaPlate(X) & there_is_a_teaCup ) => ( false)).
penalty(140, 1) <- ~( !X : ( pastaPlate(X) & left_of(X,X) ) => ( false)).
penalty(141, 1) <- ~( !X, Y : ( pastaPlate(X) & above_of(X,Y) ) => ( false)).
penalty(142, 1) <- ~( !X, Y : ( pastaPlate(X) & on(X,Y) ) => ( false)).
penalty(143, 1) <- ~( !X : ( pastaPlate(X) & food_utensil(X) ) => ( false)).
penalty(144, 1) <- ~( !X : ( pastaPlate(X) & drink_container(X) ) => ( false)).
penalty(145, 1) <- ~( !X : ( pastaPlate(X) & breakfast ) => ( false)).
penalty(146, 1) <- ~( !X : ( pastaPlate(X) ) => ( food_container(X))).
penalty(147, 1) <- ~( !X : ( soupPlate(X) & waterGlass(X) ) => ( false)).
penalty(148, 1) <- ~( !X : ( soupPlate(X) & wineGlass(X) ) => ( false)).
penalty(149, 1) <- ~( !X : ( soupPlate(X) & teaCup(X) ) => ( false)).
penalty(150, 1) <- ~( !X : ( soupPlate(X) ) => ( there_is_a_spoon)).
penalty(151, 1) <- ~( !X : ( soupPlate(X) & there_is_a_bowl ) => ( false)).
penalty(152, 1) <- ~( !X : ( soupPlate(X) & there_is_a_pastaPlate ) => ( false)).
penalty(153, 1) <- ~( !X : ( soupPlate(X) ) => ( there_is_a_soupPlate)).
penalty(154, 1) <- ~( !X : ( soupPlate(X) ) => ( there_is_a_wineGlass)).
penalty(155, 1) <- ~( !X : ( soupPlate(X) & there_is_a_teaCup ) => ( false)).
penalty(156, 1) <- ~( !X : ( soupPlate(X) & left_of(X,X) ) => ( false)).
penalty(157, 1) <- ~( !X, Y : ( soupPlate(X) & above_of(X,Y) ) => ( false)).
penalty(158, 1) <- ~( !X, Y : ( soupPlate(X) & on(X,Y) ) => ( false)).
penalty(159, 1) <- ~( !X : ( soupPlate(X) & food_utensil(X) ) => ( false)).
penalty(160, 1) <- ~( !X : ( soupPlate(X) & drink_container(X) ) => ( false)).
penalty(161, 1) <- ~( !X : ( soupPlate(X) & breakfast ) => ( false)).
penalty(162, 1) <- ~( !X : ( soupPlate(X) ) => ( food_container(X))).
penalty(163, 1) <- ~( !X : ( waterGlass(X) & wineGlass(X) ) => ( false)).
penalty(164, 1) <- ~( !X : ( waterGlass(X) & teaCup(X) ) => ( false)).
penalty(165, 1) <- ~( !X : ( waterGlass(X) & left_of(X,X) ) => ( false)).
penalty(166, 1) <- ~( !X, Y : ( waterGlass(X) & above_of(X,Y) ) => ( false)).
penalty(167, 1) <- ~( !X, Y : ( waterGlass(X) & on(X,Y) ) => ( false)).
penalty(168, 1) <- ~( !X : ( waterGlass(X) & food_utensil(X) ) => ( false)).
penalty(169, 1) <- ~( !X : ( waterGlass(X) & food_container(X) ) => ( false)).
penalty(170, 1) <- ~( !X : ( waterGlass(X) & dinner ) => ( false)).
penalty(171, 1) <- ~( !X : ( waterGlass(X) ) => ( drink_container(X))).
penalty(172, 1) <- ~( !X : ( wineGlass(X) & teaCup(X) ) => ( false)).
penalty(173, 1) <- ~( !X : ( wineGlass(X) ) => ( there_is_a_wineGlass)).
penalty(174, 1) <- ~( !X : ( wineGlass(X) & there_is_a_teaCup ) => ( false)).
penalty(175, 1) <- ~( !X : ( wineGlass(X) & left_of(X,X) ) => ( false)).
penalty(176, 1) <- ~( !X, Y : ( wineGlass(X) & left_of(X,Y) ) => ( false)).
penalty(177, 1) <- ~( !X, Y : ( wineGlass(X) & above_of(X,Y) ) => ( false)).
penalty(178, 1) <- ~( !X, Y : ( wineGlass(X) & on(X,Y) ) => ( false)).
penalty(179, 1) <- ~( !X : ( wineGlass(X) & food_utensil(X) ) => ( false)).
penalty(180, 1) <- ~( !X : ( wineGlass(X) & food_container(X) ) => ( false)).
penalty(181, 1) <- ~( !X : ( wineGlass(X) & breakfast ) => ( false)).
penalty(182, 1) <- ~( !X : ( wineGlass(X) ) => ( drink_container(X))).
penalty(183, 1) <- ~( !X : ( teaCup(X) & there_is_a_pastaPlate ) => ( false)).
penalty(184, 1) <- ~( !X : ( teaCup(X) & there_is_a_soupPlate ) => ( false)).
penalty(185, 1) <- ~( !X : ( teaCup(X) & there_is_a_wineGlass ) => ( false)).
penalty(186, 1) <- ~( !X : ( teaCup(X) ) => ( there_is_a_teaCup)).
penalty(187, 1) <- ~( !X, Y : ( teaCup(X) & above_of(X,Y) ) => ( false)).
penalty(188, 1) <- ~( !X, Y : ( teaCup(X) & on(X,Y) ) => ( false)).
penalty(189, 1) <- ~( !X : ( teaCup(X) & food_utensil(X) ) => ( false)).
penalty(190, 1) <- ~( !X : ( teaCup(X) & food_container(X) ) => ( false)).
penalty(191, 1) <- ~( !X : ( teaCup(X) ) => ( breakfast)).
penalty(192, 1) <- ~( !X : ( teaCup(X) & lunch ) => ( false)).
penalty(193, 1) <- ~( !X : ( teaCup(X) & dinner ) => ( false)).
penalty(194, 1) <- ~( !X : ( teaCup(X) ) => ( drink_container(X))).
penalty(195, 1) <- ~(( there_is_a_pastaPlate ) => ( there_is_a_fork)).
penalty(196, 1) <- ~( !X : ( left_of(X,X) ) => ( there_is_a_fork)).
penalty(197, 1) <- ~(( there_is_a_knife ) => ( there_is_a_plate)).
penalty(198, 1) <- ~(( there_is_a_knife & there_is_a_pastaPlate ) => ( false)).
penalty(199, 1) <- ~(( there_is_a_knife & dinner ) => ( false)).
penalty(200, 1) <- ~(( there_is_a_knife | there_is_a_spoon)).
penalty(201, 1) <- ~(( there_is_a_smallKnife ) => ( there_is_a_knife)).
penalty(202, 1) <- ~(( there_is_a_plate ) => ( there_is_a_knife)).
penalty(203, 1) <- ~(( there_is_a_saucer ) => ( there_is_a_knife)).
penalty(204, 1) <- ~( !X : ( left_of(X,X) ) => ( there_is_a_knife)).
penalty(205, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_knife)).
penalty(206, 1) <- ~( !X : ( there_is_a_spoon & left_of(X,X) ) => ( false)).
penalty(207, 1) <- ~(( there_is_a_spoon | there_is_a_plate)).
penalty(208, 1) <- ~(( there_is_a_bowl ) => ( there_is_a_spoon)).
penalty(209, 1) <- ~(( there_is_a_pastaPlate ) => ( there_is_a_spoon)).
penalty(210, 1) <- ~(( there_is_a_soupPlate ) => ( there_is_a_spoon)).
penalty(211, 1) <- ~(( dinner ) => ( there_is_a_spoon)).
penalty(212, 1) <- ~(( there_is_a_smallKnife ) => ( there_is_a_plate)).
penalty(213, 1) <- ~(( there_is_a_smallKnife & there_is_a_pastaPlate ) => ( false)).
penalty(214, 1) <- ~(( there_is_a_smallKnife ) => ( there_is_a_saucer)).
penalty(215, 1) <- ~( !X : ( there_is_a_smallKnife & left_of(X,X) ) => ( false)).
penalty(216, 1) <- ~(( there_is_a_smallKnife & dinner ) => ( false)).
penalty(217, 1) <- ~(( there_is_a_saucer ) => ( there_is_a_smallKnife)).
penalty(218, 1) <- ~(( there_is_a_plate & there_is_a_pastaPlate ) => ( false)).
penalty(219, 1) <- ~(( there_is_a_plate & dinner ) => ( false)).
penalty(220, 1) <- ~(( there_is_a_saucer ) => ( there_is_a_plate)).
penalty(221, 1) <- ~( !X : ( left_of(X,X) ) => ( there_is_a_plate)).
penalty(222, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_plate)).
penalty(223, 1) <- ~(( there_is_a_bowl & there_is_a_pastaPlate ) => ( false)).
penalty(224, 1) <- ~(( there_is_a_bowl & there_is_a_soupPlate ) => ( false)).
penalty(225, 1) <- ~( !X : ( there_is_a_bowl & left_of(X,X) ) => ( false)).
penalty(226, 1) <- ~(( there_is_a_bowl & dinner ) => ( false)).
penalty(227, 1) <- ~(( there_is_a_pastaPlate & there_is_a_soupPlate ) => ( false)).
penalty(228, 1) <- ~(( there_is_a_pastaPlate & there_is_a_saucer ) => ( false)).
penalty(229, 1) <- ~(( there_is_a_pastaPlate & there_is_a_teaCup ) => ( false)).
penalty(230, 1) <- ~( !X : ( there_is_a_pastaPlate & left_of(X,X) ) => ( false)).
penalty(231, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & on(X,Y) ) => ( false)).
penalty(232, 1) <- ~(( there_is_a_pastaPlate & breakfast ) => ( false)).
penalty(233, 1) <- ~(( there_is_a_soupPlate ) => ( there_is_a_wineGlass)).
penalty(234, 1) <- ~(( there_is_a_soupPlate & there_is_a_teaCup ) => ( false)).
penalty(235, 1) <- ~( !X : ( there_is_a_soupPlate & left_of(X,X) ) => ( false)).
penalty(236, 1) <- ~(( there_is_a_soupPlate & breakfast ) => ( false)).
penalty(237, 1) <- ~( !X : ( there_is_a_saucer & left_of(X,X) ) => ( false)).
penalty(238, 1) <- ~(( there_is_a_saucer & dinner ) => ( false)).
penalty(239, 1) <- ~( !X : ( there_is_a_waterGlass & left_of(X,X) ) => ( false)).
penalty(240, 1) <- ~(( there_is_a_waterGlass & dinner ) => ( false)).
penalty(241, 1) <- ~(( there_is_a_wineGlass & there_is_a_teaCup ) => ( false)).
penalty(242, 1) <- ~( !X : ( there_is_a_wineGlass & left_of(X,X) ) => ( false)).
penalty(243, 1) <- ~(( there_is_a_wineGlass & breakfast ) => ( false)).
penalty(244, 1) <- ~(( dinner ) => ( there_is_a_wineGlass)).
penalty(245, 1) <- ~(( there_is_a_teaCup ) => ( breakfast)).
penalty(246, 1) <- ~(( there_is_a_teaCup & lunch ) => ( false)).
penalty(247, 1) <- ~(( there_is_a_teaCup & dinner ) => ( false)).
penalty(248, 1) <- ~( !X : ( left_of(X,X) ) => ( there_is_a_teaCup)).
penalty(249, 1) <- ~( !X, Y : ( left_of(X,X) & left_of(X,Y) ) => ( false)).
penalty(250, 1) <- ~( !X, Y : ( left_of(X,X) & above_of(X,Y) ) => ( false)).
penalty(251, 1) <- ~( !X, Y : ( left_of(X,X) & on(X,Y) ) => ( false)).
penalty(252, 1) <- ~( !X : ( left_of(X,X) & food_utensil(X) ) => ( false)).
penalty(253, 1) <- ~( !X : ( left_of(X,X) & food_container(X) ) => ( false)).
penalty(254, 1) <- ~( !X : ( left_of(X,X) ) => ( breakfast)).
penalty(255, 1) <- ~( !X : ( left_of(X,X) & lunch ) => ( false)).
penalty(256, 1) <- ~( !X : ( left_of(X,X) & dinner ) => ( false)).
penalty(257, 1) <- ~( !X : ( left_of(X,X) ) => ( teaCup(X))).
penalty(258, 1) <- ~( !X : ( left_of(X,X) ) => ( drink_container(X))).
penalty(259, 1) <- ~( !X, Y : ( left_of(X,Y) & above_of(X,Y) ) => ( false)).
penalty(260, 1) <- ~( !X, Y : ( left_of(X,Y) & on(X,Y) ) => ( false)).
penalty(261, 1) <- ~( !X, Y : ( left_of(X,Y) & smallKnife(Y) ) => ( false)).
penalty(262, 1) <- ~( !X, Y : ( left_of(X,Y) & saucer(Y) ) => ( false)).
penalty(263, 1) <- ~( !X, Y : ( left_of(X,Y) & teaCup(Y) ) => ( false)).
penalty(264, 1) <- ~( !X, Y : ( left_of(X,Y) & left_of(Y,X) ) => ( false)).
penalty(265, 1) <- ~( !X, Y : ( left_of(X,Y) & left_of(Y,Y) ) => ( false)).
penalty(266, 1) <- ~( !X, Y : ( left_of(X,Y) & above_of(Y,X) ) => ( false)).
penalty(267, 1) <- ~( !X, Y : ( left_of(X,Y) & on(Y,X) ) => ( false)).
penalty(268, 1) <- ~( !X, Y : ( above_of(X,Y) & on(X,Y) ) => ( false)).
penalty(269, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(X) ) => ( false)).
penalty(270, 1) <- ~( !X, Y : ( above_of(X,Y) & drink_container(X) ) => ( false)).
penalty(271, 1) <- ~( !X, Y : ( above_of(X,Y) & fork(Y) ) => ( false)).
penalty(272, 1) <- ~( !X, Y : ( above_of(X,Y) & knife(Y) ) => ( false)).
penalty(273, 1) <- ~( !X, Y : ( above_of(X,Y) & spoon(Y) ) => ( false)).
penalty(274, 1) <- ~( !X, Y : ( above_of(X,Y) & plate(Y) ) => ( false)).
penalty(275, 1) <- ~( !X, Y : ( above_of(X,Y) & bowl(Y) ) => ( false)).
penalty(276, 1) <- ~( !X, Y : ( above_of(X,Y) & pastaPlate(Y) ) => ( false)).
penalty(277, 1) <- ~( !X, Y : ( above_of(X,Y) & soupPlate(Y) ) => ( false)).
penalty(278, 1) <- ~( !X, Y : ( above_of(X,Y) & above_of(Y,X) ) => ( false)).
penalty(279, 1) <- ~( !X, Y : ( above_of(X,Y) & on(Y,X) ) => ( false)).
penalty(280, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( food_utensil(X))).
penalty(281, 1) <- ~( !X, Y : ( on(X,Y) & food_utensil(X) ) => ( false)).
penalty(282, 1) <- ~( !X, Y : ( on(X,Y) & drink_container(X) ) => ( false)).
penalty(283, 1) <- ~( !X, Y : ( on(X,Y) & dinner ) => ( false)).
penalty(284, 1) <- ~( !X, Y : ( on(X,Y) & fork(Y) ) => ( false)).
penalty(285, 1) <- ~( !X, Y : ( on(X,Y) & knife(Y) ) => ( false)).
penalty(286, 1) <- ~( !X, Y : ( on(X,Y) & spoon(Y) ) => ( false)).
penalty(287, 1) <- ~( !X, Y : ( on(X,Y) & plate(Y) ) => ( false)).
penalty(288, 1) <- ~( !X, Y : ( on(X,Y) & saucer(Y) ) => ( false)).
penalty(289, 1) <- ~( !X, Y : ( on(X,Y) & pastaPlate(Y) ) => ( false)).
penalty(290, 1) <- ~( !X, Y : ( on(X,Y) & waterGlass(Y) ) => ( false)).
penalty(291, 1) <- ~( !X, Y : ( on(X,Y) & wineGlass(Y) ) => ( false)).
penalty(292, 1) <- ~( !X, Y : ( on(X,Y) & teaCup(Y) ) => ( false)).
penalty(293, 1) <- ~( !X, Y : ( on(X,Y) & left_of(Y,Y) ) => ( false)).
penalty(294, 1) <- ~( !X, Y : ( on(X,Y) & on(Y,X) ) => ( false)).
penalty(295, 1) <- ~( !X, Y : ( on(X,Y) ) => ( food_container(X))).
penalty(296, 1) <- ~( !X, Y : ( on(X,Y) & drink_container(Y) ) => ( false)).
penalty(297, 1) <- ~( !X : ( food_utensil(X) & food_container(X) ) => ( false)).
penalty(298, 1) <- ~( !X : ( food_utensil(X) & drink_container(X) ) => ( false)).
penalty(299, 1) <- ~( !X : ( food_container(X) & drink_container(X) ) => ( false)).
penalty(300, 1) <- ~(( breakfast & lunch ) => ( false)).
penalty(301, 1) <- ~(( breakfast & dinner ) => ( false)).
penalty(302, 1) <- ~(( lunch & dinner ) => ( false)).
penalty(303, 1) <- ~( !X : ( fork(X) & there_is_a_bowl ) => ( there_is_a_knife)).
penalty(304, 1) <- ~( !X : ( fork(X) ) => ( there_is_a_knife | there_is_a_pastaPlate)).
penalty(305, 1) <- ~( !X : ( fork(X) & there_is_a_soupPlate ) => ( there_is_a_knife)).
penalty(306, 1) <- ~( !X : ( fork(X) & there_is_a_teaCup ) => ( there_is_a_knife)).
penalty(307, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) ) => ( there_is_a_knife)).
penalty(308, 1) <- ~( !X : ( fork(X) & breakfast ) => ( there_is_a_knife)).
penalty(309, 1) <- ~( !X : ( fork(X) & there_is_a_bowl ) => ( there_is_a_plate)).
penalty(310, 1) <- ~( !X : ( fork(X) ) => ( there_is_a_plate | there_is_a_pastaPlate)).
penalty(311, 1) <- ~( !X : ( fork(X) & there_is_a_soupPlate ) => ( there_is_a_plate)).
penalty(312, 1) <- ~( !X : ( fork(X) & there_is_a_teaCup ) => ( there_is_a_plate)).
penalty(313, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) ) => ( there_is_a_plate)).
penalty(314, 1) <- ~( !X : ( fork(X) & breakfast ) => ( there_is_a_plate)).
penalty(315, 1) <- ~( !X, Y : ( fork(X) & there_is_a_pastaPlate & left_of(X,Y) ) => ( false)).
penalty(316, 1) <- ~( !X : ( fork(X) & dinner ) => ( there_is_a_pastaPlate)).
penalty(317, 1) <- ~( !X : ( fork(X) & there_is_a_soupPlate ) => ( there_is_a_waterGlass)).
penalty(318, 1) <- ~( !X : ( fork(X) & there_is_a_soupPlate ) => ( lunch)).
penalty(319, 1) <- ~( !X : ( fork(X) & there_is_a_soupPlate & dinner ) => ( false)).
penalty(320, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & dinner ) => ( false)).
penalty(321, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(322, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(323, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & pastaPlate(Y) ) => ( false)).
penalty(324, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(325, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(326, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(327, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & teaCup(Y) ) => ( false)).
penalty(328, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & left_of(Y,Y) ) => ( false)).
penalty(329, 1) <- ~( !X : ( knife(X) & there_is_a_soupPlate ) => ( there_is_a_fork)).
penalty(330, 1) <- ~( !X : ( knife(X) & there_is_a_wineGlass ) => ( there_is_a_fork)).
penalty(331, 1) <- ~( !X : ( knife(X) ) => ( there_is_a_fork | breakfast)).
penalty(332, 1) <- ~( !X : ( knife(X) & lunch ) => ( there_is_a_fork)).
penalty(333, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) ) => ( there_is_a_spoon)).
penalty(334, 1) <- ~( !X : ( knife(X) & there_is_a_soupPlate ) => ( there_is_a_waterGlass)).
penalty(335, 1) <- ~( !X : ( knife(X) & there_is_a_soupPlate ) => ( lunch)).
penalty(336, 1) <- ~( !X : ( knife(X) & there_is_a_wineGlass ) => ( lunch)).
penalty(337, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(338, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) & knife(Y) ) => ( false)).
penalty(339, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) ) => ( spoon(Y))).
penalty(340, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) & plate(Y) ) => ( false)).
penalty(341, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) & bowl(Y) ) => ( false)).
penalty(342, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) & pastaPlate(Y) ) => ( false)).
penalty(343, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) & soupPlate(Y) ) => ( false)).
penalty(344, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(345, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(346, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) ) => ( food_utensil(Y))).
penalty(347, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) & food_container(Y) ) => ( false)).
penalty(348, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(349, 1) <- ~( !X, Y : ( knife(X) & above_of(X,Y) & smallKnife(Y) ) => ( false)).
penalty(350, 1) <- ~( !X, Y : ( knife(X) & above_of(X,Y) & saucer(Y) ) => ( false)).
penalty(351, 1) <- ~( !X, Y : ( knife(X) & above_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(352, 1) <- ~( !X, Y : ( knife(X) & above_of(X,Y) & food_container(Y) ) => ( false)).
penalty(353, 1) <- ~( !X, Y : ( knife(X) & above_of(X,Y) ) => ( drink_container(Y))).
penalty(354, 1) <- ~( !X : ( knife(X) ) => ( breakfast | lunch)).
penalty(355, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) ) => ( there_is_a_fork)).
penalty(356, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_knife & left_of(X,Y) ) => ( false)).
penalty(357, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_smallKnife & left_of(X,Y) ) => ( false)).
penalty(358, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_plate & left_of(X,Y) ) => ( false)).
penalty(359, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_bowl & left_of(X,Y) ) => ( false)).
penalty(360, 1) <- ~( !X : ( spoon(X) & there_is_a_teaCup ) => ( there_is_a_bowl)).
penalty(361, 1) <- ~( !X : ( spoon(X) & breakfast ) => ( there_is_a_bowl)).
penalty(362, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_pastaPlate & above_of(X,Y) ) => ( false)).
penalty(363, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) ) => ( there_is_a_pastaPlate)).
penalty(364, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_soupPlate & left_of(X,Y) ) => ( false)).
penalty(365, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_saucer & left_of(X,Y) ) => ( false)).
penalty(366, 1) <- ~( !X : ( spoon(X) & lunch ) => ( there_is_a_waterGlass)).
penalty(367, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_teaCup & left_of(X,Y) ) => ( false)).
penalty(368, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) & breakfast ) => ( false)).
penalty(369, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(370, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) & knife(Y) ) => ( false)).
penalty(371, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(372, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) & plate(Y) ) => ( false)).
penalty(373, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) & bowl(Y) ) => ( false)).
penalty(374, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) ) => ( pastaPlate(Y))).
penalty(375, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) & soupPlate(Y) ) => ( false)).
penalty(376, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(377, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(378, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(379, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) ) => ( food_container(Y))).
penalty(380, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(381, 1) <- ~( !X, Y : ( spoon(X) & above_of(X,Y) & smallKnife(Y) ) => ( false)).
penalty(382, 1) <- ~( !X, Y : ( spoon(X) & above_of(X,Y) & saucer(Y) ) => ( false)).
penalty(383, 1) <- ~( !X, Y : ( spoon(X) & above_of(X,Y) & left_of(Y,Y) ) => ( false)).
penalty(384, 1) <- ~( !X, Y : ( spoon(X) & above_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(385, 1) <- ~( !X, Y : ( spoon(X) & above_of(X,Y) & food_container(Y) ) => ( false)).
penalty(386, 1) <- ~( !X, Y : ( spoon(X) & above_of(X,Y) ) => ( drink_container(Y))).
penalty(387, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(388, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(389, 1) <- ~( !X : ( smallKnife(X) & there_is_a_soupPlate ) => ( there_is_a_fork)).
penalty(390, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_fork | there_is_a_waterGlass)).
penalty(391, 1) <- ~( !X : ( smallKnife(X) & there_is_a_wineGlass ) => ( there_is_a_fork)).
penalty(392, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_fork | there_is_a_teaCup)).
penalty(393, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_fork | breakfast)).
penalty(394, 1) <- ~( !X : ( smallKnife(X) & lunch ) => ( there_is_a_fork)).
penalty(395, 1) <- ~( !X : ( smallKnife(X) & there_is_a_wineGlass ) => ( there_is_a_spoon)).
penalty(396, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_spoon | breakfast)).
penalty(397, 1) <- ~( !X : ( smallKnife(X) & lunch ) => ( there_is_a_spoon)).
penalty(398, 1) <- ~( !X : ( smallKnife(X) & there_is_a_bowl & there_is_a_wineGlass ) => ( false)).
penalty(399, 1) <- ~( !X : ( smallKnife(X) & there_is_a_bowl ) => ( breakfast)).
penalty(400, 1) <- ~( !X : ( smallKnife(X) & there_is_a_bowl & lunch ) => ( false)).
penalty(401, 1) <- ~( !X : ( smallKnife(X) & there_is_a_soupPlate ) => ( there_is_a_waterGlass)).
penalty(402, 1) <- ~( !X : ( smallKnife(X) & there_is_a_soupPlate ) => ( lunch)).
penalty(403, 1) <- ~( !X : ( smallKnife(X) & there_is_a_wineGlass ) => ( there_is_a_soupPlate)).
penalty(404, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_soupPlate | breakfast)).
penalty(405, 1) <- ~( !X : ( smallKnife(X) & lunch ) => ( there_is_a_soupPlate)).
penalty(406, 1) <- ~( !X : ( smallKnife(X) & there_is_a_wineGlass ) => ( there_is_a_waterGlass)).
penalty(407, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(408, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_waterGlass | breakfast)).
penalty(409, 1) <- ~( !X : ( smallKnife(X) & lunch ) => ( there_is_a_waterGlass)).
penalty(410, 1) <- ~( !X : ( smallKnife(X) & there_is_a_wineGlass ) => ( lunch)).
penalty(411, 1) <- ~( !X : ( smallKnife(X) ) => ( there_is_a_wineGlass | breakfast)).
penalty(412, 1) <- ~( !X : ( smallKnife(X) & lunch ) => ( there_is_a_wineGlass)).
penalty(413, 1) <- ~( !X : ( smallKnife(X) ) => ( breakfast | lunch)).
penalty(414, 1) <- ~( !X : ( plate(X) & there_is_a_soupPlate ) => ( there_is_a_fork)).
penalty(415, 1) <- ~( !X : ( plate(X) & there_is_a_wineGlass ) => ( there_is_a_fork)).
penalty(416, 1) <- ~( !X : ( plate(X) ) => ( there_is_a_fork | breakfast)).
penalty(417, 1) <- ~( !X : ( plate(X) & lunch ) => ( there_is_a_fork)).
penalty(418, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) ) => ( there_is_a_spoon)).
penalty(419, 1) <- ~( !X : ( plate(X) & there_is_a_soupPlate ) => ( there_is_a_waterGlass)).
penalty(420, 1) <- ~( !X : ( plate(X) & there_is_a_soupPlate ) => ( lunch)).
penalty(421, 1) <- ~( !X : ( plate(X) & there_is_a_wineGlass ) => ( lunch)).
penalty(422, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(423, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) & plate(Y) ) => ( false)).
penalty(424, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) & bowl(Y) ) => ( false)).
penalty(425, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) & pastaPlate(Y) ) => ( false)).
penalty(426, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) & soupPlate(Y) ) => ( false)).
penalty(427, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(428, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(429, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) ) => ( food_utensil(Y))).
penalty(430, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) & food_container(Y) ) => ( false)).
penalty(431, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(432, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) & smallKnife(Y) ) => ( false)).
penalty(433, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) & food_utensil(Y) ) => ( false)).
penalty(434, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) ) => ( food_container(Y))).
penalty(435, 1) <- ~( !X : ( plate(X) ) => ( breakfast | lunch)).
penalty(436, 1) <- ~( !X : ( bowl(X) & there_is_a_fork ) => ( there_is_a_knife)).
penalty(437, 1) <- ~( !X : ( bowl(X) & there_is_a_fork ) => ( there_is_a_plate)).
penalty(438, 1) <- ~( !X : ( bowl(X) & there_is_a_wineGlass ) => ( there_is_a_fork)).
penalty(439, 1) <- ~( !X : ( bowl(X) & there_is_a_wineGlass ) => ( there_is_a_knife)).
penalty(440, 1) <- ~( !X : ( bowl(X) & there_is_a_smallKnife & there_is_a_wineGlass ) => ( false)).
penalty(441, 1) <- ~( !X : ( bowl(X) & there_is_a_smallKnife ) => ( breakfast)).
penalty(442, 1) <- ~( !X : ( bowl(X) & there_is_a_smallKnife & lunch ) => ( false)).
penalty(443, 1) <- ~( !X : ( bowl(X) & there_is_a_wineGlass ) => ( there_is_a_plate)).
penalty(444, 1) <- ~( !X : ( bowl(X) & there_is_a_saucer & there_is_a_wineGlass ) => ( false)).
penalty(445, 1) <- ~( !X : ( bowl(X) & there_is_a_saucer ) => ( breakfast)).
penalty(446, 1) <- ~( !X : ( bowl(X) & there_is_a_saucer & lunch ) => ( false)).
penalty(447, 1) <- ~( !X : ( bowl(X) & there_is_a_wineGlass ) => ( there_is_a_waterGlass)).
penalty(448, 1) <- ~( !X : ( bowl(X) ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(449, 1) <- ~( !X : ( bowl(X) ) => ( there_is_a_waterGlass | breakfast)).
penalty(450, 1) <- ~( !X : ( bowl(X) & lunch ) => ( there_is_a_waterGlass)).
penalty(451, 1) <- ~( !X : ( bowl(X) & there_is_a_wineGlass ) => ( lunch)).
penalty(452, 1) <- ~( !X, Y : ( bowl(X) & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(453, 1) <- ~( !X, Y : ( bowl(X) & left_of(X,Y) & plate(Y) ) => ( false)).
penalty(454, 1) <- ~( !X, Y : ( bowl(X) & left_of(X,Y) & bowl(Y) ) => ( false)).
penalty(455, 1) <- ~( !X, Y : ( bowl(X) & left_of(X,Y) & pastaPlate(Y) ) => ( false)).
penalty(456, 1) <- ~( !X, Y : ( bowl(X) & left_of(X,Y) & soupPlate(Y) ) => ( false)).
penalty(457, 1) <- ~( !X, Y : ( bowl(X) & left_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(458, 1) <- ~( !X, Y : ( bowl(X) & left_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(459, 1) <- ~( !X, Y : ( bowl(X) & left_of(X,Y) ) => ( food_utensil(Y))).
penalty(460, 1) <- ~( !X, Y : ( bowl(X) & left_of(X,Y) & food_container(Y) ) => ( false)).
penalty(461, 1) <- ~( !X, Y : ( bowl(X) & left_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(462, 1) <- ~( !X : ( bowl(X) ) => ( breakfast | lunch)).
penalty(463, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(464, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(465, 1) <- ~( !X : ( saucer(X) & there_is_a_soupPlate ) => ( there_is_a_fork)).
penalty(466, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_fork | there_is_a_waterGlass)).
penalty(467, 1) <- ~( !X : ( saucer(X) & there_is_a_wineGlass ) => ( there_is_a_fork)).
penalty(468, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_fork | there_is_a_teaCup)).
penalty(469, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_fork | breakfast)).
penalty(470, 1) <- ~( !X : ( saucer(X) & lunch ) => ( there_is_a_fork)).
penalty(471, 1) <- ~( !X : ( saucer(X) & there_is_a_wineGlass ) => ( there_is_a_spoon)).
penalty(472, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_spoon | breakfast)).
penalty(473, 1) <- ~( !X : ( saucer(X) & lunch ) => ( there_is_a_spoon)).
penalty(474, 1) <- ~( !X : ( saucer(X) & there_is_a_bowl & there_is_a_wineGlass ) => ( false)).
penalty(475, 1) <- ~( !X : ( saucer(X) & there_is_a_bowl ) => ( breakfast)).
penalty(476, 1) <- ~( !X : ( saucer(X) & there_is_a_bowl & lunch ) => ( false)).
penalty(477, 1) <- ~( !X : ( saucer(X) & there_is_a_soupPlate ) => ( there_is_a_waterGlass)).
penalty(478, 1) <- ~( !X : ( saucer(X) & there_is_a_soupPlate ) => ( lunch)).
penalty(479, 1) <- ~( !X : ( saucer(X) & there_is_a_wineGlass ) => ( there_is_a_soupPlate)).
penalty(480, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_soupPlate | breakfast)).
penalty(481, 1) <- ~( !X : ( saucer(X) & lunch ) => ( there_is_a_soupPlate)).
penalty(482, 1) <- ~( !X : ( saucer(X) & there_is_a_wineGlass ) => ( there_is_a_waterGlass)).
penalty(483, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(484, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_waterGlass | breakfast)).
penalty(485, 1) <- ~( !X : ( saucer(X) & lunch ) => ( there_is_a_waterGlass)).
penalty(486, 1) <- ~( !X : ( saucer(X) & there_is_a_wineGlass ) => ( lunch)).
penalty(487, 1) <- ~( !X : ( saucer(X) ) => ( there_is_a_wineGlass | breakfast)).
penalty(488, 1) <- ~( !X : ( saucer(X) & lunch ) => ( there_is_a_wineGlass)).
penalty(489, 1) <- ~( !X, Y : ( saucer(X) & on(X,Y) ) => ( smallKnife(Y))).
penalty(490, 1) <- ~( !X, Y : ( saucer(X) & on(X,Y) & bowl(Y) ) => ( false)).
penalty(491, 1) <- ~( !X, Y : ( saucer(X) & on(X,Y) & soupPlate(Y) ) => ( false)).
penalty(492, 1) <- ~( !X, Y : ( saucer(X) & on(X,Y) ) => ( food_utensil(Y))).
penalty(493, 1) <- ~( !X, Y : ( saucer(X) & on(X,Y) & food_container(Y) ) => ( false)).
penalty(494, 1) <- ~( !X : ( saucer(X) ) => ( breakfast | lunch)).
penalty(495, 1) <- ~( !X : ( pastaPlate(X) & there_is_a_waterGlass & there_is_a_wineGlass ) => ( false)).
penalty(496, 1) <- ~( !X : ( pastaPlate(X) & there_is_a_waterGlass ) => ( lunch)).
penalty(497, 1) <- ~( !X : ( pastaPlate(X) ) => ( there_is_a_waterGlass | there_is_a_wineGlass)).
penalty(498, 1) <- ~( !X : ( pastaPlate(X) & lunch ) => ( there_is_a_waterGlass)).
penalty(499, 1) <- ~( !X : ( pastaPlate(X) ) => ( there_is_a_waterGlass | dinner)).
penalty(500, 1) <- ~( !X : ( pastaPlate(X) & there_is_a_wineGlass & lunch ) => ( false)).
penalty(501, 1) <- ~( !X : ( pastaPlate(X) & there_is_a_wineGlass ) => ( dinner)).
penalty(502, 1) <- ~( !X : ( pastaPlate(X) ) => ( there_is_a_wineGlass | lunch)).
penalty(503, 1) <- ~( !X, Y : ( pastaPlate(X) & left_of(X,Y) ) => ( fork(Y))).
penalty(504, 1) <- ~( !X, Y : ( pastaPlate(X) & left_of(X,Y) & knife(Y) ) => ( false)).
penalty(505, 1) <- ~( !X, Y : ( pastaPlate(X) & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(506, 1) <- ~( !X, Y : ( pastaPlate(X) & left_of(X,Y) & plate(Y) ) => ( false)).
penalty(507, 1) <- ~( !X, Y : ( pastaPlate(X) & left_of(X,Y) & bowl(Y) ) => ( false)).
penalty(508, 1) <- ~( !X, Y : ( pastaPlate(X) & left_of(X,Y) & pastaPlate(Y) ) => ( false)).
penalty(509, 1) <- ~( !X, Y : ( pastaPlate(X) & left_of(X,Y) & soupPlate(Y) ) => ( false)).
penalty(510, 1) <- ~( !X, Y : ( pastaPlate(X) & left_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(511, 1) <- ~( !X, Y : ( pastaPlate(X) & left_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(512, 1) <- ~( !X, Y : ( pastaPlate(X) & left_of(X,Y) ) => ( food_utensil(Y))).
penalty(513, 1) <- ~( !X, Y : ( pastaPlate(X) & left_of(X,Y) & food_container(Y) ) => ( false)).
penalty(514, 1) <- ~( !X, Y : ( pastaPlate(X) & left_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(515, 1) <- ~( !X : ( pastaPlate(X) ) => ( lunch | dinner)).
penalty(516, 1) <- ~( !X : ( soupPlate(X) & there_is_a_fork ) => ( there_is_a_knife)).
penalty(517, 1) <- ~( !X : ( soupPlate(X) & there_is_a_fork ) => ( there_is_a_plate)).
penalty(518, 1) <- ~( !X : ( soupPlate(X) & there_is_a_fork ) => ( there_is_a_waterGlass)).
penalty(519, 1) <- ~( !X : ( soupPlate(X) & there_is_a_fork ) => ( lunch)).
penalty(520, 1) <- ~( !X : ( soupPlate(X) & there_is_a_fork & dinner ) => ( false)).
penalty(521, 1) <- ~( !X : ( soupPlate(X) & there_is_a_knife ) => ( there_is_a_fork)).
penalty(522, 1) <- ~( !X : ( soupPlate(X) & there_is_a_smallKnife ) => ( there_is_a_fork)).
penalty(523, 1) <- ~( !X : ( soupPlate(X) & there_is_a_plate ) => ( there_is_a_fork)).
penalty(524, 1) <- ~( !X : ( soupPlate(X) & there_is_a_saucer ) => ( there_is_a_fork)).
penalty(525, 1) <- ~( !X : ( soupPlate(X) & there_is_a_waterGlass ) => ( there_is_a_fork)).
penalty(526, 1) <- ~( !X : ( soupPlate(X) & lunch ) => ( there_is_a_fork)).
penalty(527, 1) <- ~( !X : ( soupPlate(X) ) => ( there_is_a_fork | dinner)).
penalty(528, 1) <- ~( !X : ( soupPlate(X) & there_is_a_knife ) => ( there_is_a_waterGlass)).
penalty(529, 1) <- ~( !X : ( soupPlate(X) & there_is_a_knife ) => ( lunch)).
penalty(530, 1) <- ~( !X : ( soupPlate(X) & there_is_a_waterGlass ) => ( there_is_a_knife)).
penalty(531, 1) <- ~( !X : ( soupPlate(X) & lunch ) => ( there_is_a_knife)).
penalty(532, 1) <- ~( !X : ( soupPlate(X) ) => ( there_is_a_knife | dinner)).
penalty(533, 1) <- ~( !X : ( soupPlate(X) & there_is_a_smallKnife ) => ( there_is_a_waterGlass)).
penalty(534, 1) <- ~( !X : ( soupPlate(X) & there_is_a_smallKnife ) => ( lunch)).
penalty(535, 1) <- ~( !X : ( soupPlate(X) & there_is_a_plate ) => ( there_is_a_waterGlass)).
penalty(536, 1) <- ~( !X : ( soupPlate(X) & there_is_a_plate ) => ( lunch)).
penalty(537, 1) <- ~( !X : ( soupPlate(X) & there_is_a_waterGlass ) => ( there_is_a_plate)).
penalty(538, 1) <- ~( !X : ( soupPlate(X) & lunch ) => ( there_is_a_plate)).
penalty(539, 1) <- ~( !X : ( soupPlate(X) ) => ( there_is_a_plate | dinner)).
penalty(540, 1) <- ~( !X : ( soupPlate(X) & there_is_a_saucer ) => ( there_is_a_waterGlass)).
penalty(541, 1) <- ~( !X : ( soupPlate(X) & there_is_a_saucer ) => ( lunch)).
penalty(542, 1) <- ~( !X : ( soupPlate(X) & there_is_a_waterGlass ) => ( lunch)).
penalty(543, 1) <- ~( !X : ( soupPlate(X) & lunch ) => ( there_is_a_waterGlass)).
penalty(544, 1) <- ~( !X : ( soupPlate(X) ) => ( there_is_a_waterGlass | dinner)).
penalty(545, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(546, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & plate(Y) ) => ( false)).
penalty(547, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & bowl(Y) ) => ( false)).
penalty(548, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & pastaPlate(Y) ) => ( false)).
penalty(549, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & soupPlate(Y) ) => ( false)).
penalty(550, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(551, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(552, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) ) => ( food_utensil(Y))).
penalty(553, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & food_container(Y) ) => ( false)).
penalty(554, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(555, 1) <- ~( !X : ( soupPlate(X) ) => ( lunch | dinner)).
penalty(556, 1) <- ~( !X : ( waterGlass(X) & there_is_a_soupPlate ) => ( there_is_a_fork)).
penalty(557, 1) <- ~( !X : ( waterGlass(X) ) => ( there_is_a_fork | there_is_a_waterGlass)).
penalty(558, 1) <- ~( !X : ( waterGlass(X) & there_is_a_wineGlass ) => ( there_is_a_fork)).
penalty(559, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) ) => ( there_is_a_fork)).
penalty(560, 1) <- ~( !X : ( waterGlass(X) & there_is_a_soupPlate ) => ( there_is_a_knife)).
penalty(561, 1) <- ~( !X : ( waterGlass(X) ) => ( there_is_a_knife | there_is_a_waterGlass)).
penalty(562, 1) <- ~( !X : ( waterGlass(X) & there_is_a_wineGlass ) => ( there_is_a_knife)).
penalty(563, 1) <- ~( !X : ( waterGlass(X) & there_is_a_teaCup ) => ( there_is_a_knife)).
penalty(564, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) ) => ( there_is_a_knife)).
penalty(565, 1) <- ~( !X : ( waterGlass(X) & there_is_a_spoon ) => ( there_is_a_waterGlass)).
penalty(566, 1) <- ~( !X : ( waterGlass(X) & there_is_a_wineGlass ) => ( there_is_a_spoon)).
penalty(567, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) ) => ( there_is_a_spoon)).
penalty(568, 1) <- ~( !X : ( waterGlass(X) & there_is_a_smallKnife ) => ( there_is_a_waterGlass)).
penalty(569, 1) <- ~( !X : ( waterGlass(X) & there_is_a_soupPlate ) => ( there_is_a_plate)).
penalty(570, 1) <- ~( !X : ( waterGlass(X) ) => ( there_is_a_plate | there_is_a_waterGlass)).
penalty(571, 1) <- ~( !X : ( waterGlass(X) & there_is_a_wineGlass ) => ( there_is_a_plate)).
penalty(572, 1) <- ~( !X : ( waterGlass(X) & there_is_a_teaCup ) => ( there_is_a_plate)).
penalty(573, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) ) => ( there_is_a_plate)).
penalty(574, 1) <- ~( !X : ( waterGlass(X) & there_is_a_bowl ) => ( there_is_a_waterGlass)).
penalty(575, 1) <- ~( !X : ( waterGlass(X) & there_is_a_pastaPlate ) => ( there_is_a_waterGlass)).
penalty(576, 1) <- ~( !X : ( waterGlass(X) & there_is_a_pastaPlate & there_is_a_wineGlass ) => ( false)).
penalty(577, 1) <- ~( !X, Y : ( waterGlass(X) & there_is_a_pastaPlate & left_of(X,Y) ) => ( false)).
penalty(578, 1) <- ~( !X : ( waterGlass(X) & there_is_a_pastaPlate ) => ( lunch)).
penalty(579, 1) <- ~( !X : ( waterGlass(X) & there_is_a_soupPlate ) => ( there_is_a_waterGlass)).
penalty(580, 1) <- ~( !X : ( waterGlass(X) & there_is_a_soupPlate ) => ( lunch)).
penalty(581, 1) <- ~( !X : ( waterGlass(X) & there_is_a_saucer ) => ( there_is_a_waterGlass)).
penalty(582, 1) <- ~( !X : ( waterGlass(X) & there_is_a_wineGlass ) => ( there_is_a_waterGlass)).
penalty(583, 1) <- ~( !X : ( waterGlass(X) ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(584, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) ) => ( there_is_a_waterGlass)).
penalty(585, 1) <- ~( !X : ( waterGlass(X) ) => ( there_is_a_waterGlass | breakfast)).
penalty(586, 1) <- ~( !X : ( waterGlass(X) & lunch ) => ( there_is_a_waterGlass)).
penalty(587, 1) <- ~( !X : ( waterGlass(X) & there_is_a_wineGlass ) => ( lunch)).
penalty(588, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) ) => ( there_is_a_wineGlass)).
penalty(589, 1) <- ~( !X, Y : ( waterGlass(X) & there_is_a_teaCup & left_of(X,Y) ) => ( false)).
penalty(590, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) & breakfast ) => ( false)).
penalty(591, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) ) => ( lunch)).
penalty(592, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(593, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) & knife(Y) ) => ( false)).
penalty(594, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(595, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) & plate(Y) ) => ( false)).
penalty(596, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) & bowl(Y) ) => ( false)).
penalty(597, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) & pastaPlate(Y) ) => ( false)).
penalty(598, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) & soupPlate(Y) ) => ( false)).
penalty(599, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(600, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) ) => ( wineGlass(Y))).
penalty(601, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(602, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) & food_container(Y) ) => ( false)).
penalty(603, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) ) => ( drink_container(Y))).
penalty(604, 1) <- ~( !X : ( waterGlass(X) ) => ( breakfast | lunch)).
penalty(605, 1) <- ~( !X : ( wineGlass(X) & there_is_a_knife ) => ( there_is_a_fork)).
penalty(606, 1) <- ~( !X : ( wineGlass(X) ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(607, 1) <- ~( !X : ( wineGlass(X) & there_is_a_smallKnife ) => ( there_is_a_fork)).
penalty(608, 1) <- ~( !X : ( wineGlass(X) & there_is_a_plate ) => ( there_is_a_fork)).
penalty(609, 1) <- ~( !X : ( wineGlass(X) & there_is_a_bowl ) => ( there_is_a_fork)).
penalty(610, 1) <- ~( !X : ( wineGlass(X) ) => ( there_is_a_fork | there_is_a_soupPlate)).
penalty(611, 1) <- ~( !X : ( wineGlass(X) & there_is_a_saucer ) => ( there_is_a_fork)).
penalty(612, 1) <- ~( !X : ( wineGlass(X) & there_is_a_waterGlass ) => ( there_is_a_fork)).
penalty(613, 1) <- ~( !X : ( wineGlass(X) & lunch ) => ( there_is_a_fork)).
penalty(614, 1) <- ~( !X : ( wineGlass(X) ) => ( there_is_a_fork | dinner)).
penalty(615, 1) <- ~( !X : ( wineGlass(X) & there_is_a_knife ) => ( lunch)).
penalty(616, 1) <- ~( !X : ( wineGlass(X) & there_is_a_bowl ) => ( there_is_a_knife)).
penalty(617, 1) <- ~( !X : ( wineGlass(X) & there_is_a_waterGlass ) => ( there_is_a_knife)).
penalty(618, 1) <- ~( !X : ( wineGlass(X) & lunch ) => ( there_is_a_knife)).
penalty(619, 1) <- ~( !X : ( wineGlass(X) ) => ( there_is_a_knife | dinner)).
penalty(620, 1) <- ~( !X : ( wineGlass(X) & there_is_a_smallKnife ) => ( there_is_a_spoon)).
penalty(621, 1) <- ~( !X : ( wineGlass(X) & there_is_a_saucer ) => ( there_is_a_spoon)).
penalty(622, 1) <- ~( !X : ( wineGlass(X) & there_is_a_waterGlass ) => ( there_is_a_spoon)).
penalty(623, 1) <- ~( !X : ( wineGlass(X) ) => ( there_is_a_spoon | lunch)).
penalty(624, 1) <- ~( !X : ( wineGlass(X) & there_is_a_smallKnife & there_is_a_bowl ) => ( false)).
penalty(625, 1) <- ~( !X : ( wineGlass(X) & there_is_a_smallKnife ) => ( there_is_a_soupPlate)).
penalty(626, 1) <- ~( !X : ( wineGlass(X) & there_is_a_smallKnife ) => ( there_is_a_waterGlass)).
penalty(627, 1) <- ~( !X : ( wineGlass(X) & there_is_a_smallKnife ) => ( lunch)).
penalty(628, 1) <- ~( !X : ( wineGlass(X) & there_is_a_plate ) => ( lunch)).
penalty(629, 1) <- ~( !X : ( wineGlass(X) & there_is_a_bowl ) => ( there_is_a_plate)).
penalty(630, 1) <- ~( !X : ( wineGlass(X) & there_is_a_waterGlass ) => ( there_is_a_plate)).
penalty(631, 1) <- ~( !X : ( wineGlass(X) & lunch ) => ( there_is_a_plate)).
penalty(632, 1) <- ~( !X : ( wineGlass(X) ) => ( there_is_a_plate | dinner)).
penalty(633, 1) <- ~( !X : ( wineGlass(X) & there_is_a_bowl & there_is_a_saucer ) => ( false)).
penalty(634, 1) <- ~( !X : ( wineGlass(X) & there_is_a_bowl ) => ( there_is_a_waterGlass)).
penalty(635, 1) <- ~( !X : ( wineGlass(X) & there_is_a_bowl ) => ( lunch)).
penalty(636, 1) <- ~( !X : ( wineGlass(X) & there_is_a_pastaPlate & there_is_a_waterGlass ) => ( false)).
penalty(637, 1) <- ~( !X : ( wineGlass(X) & there_is_a_pastaPlate & lunch ) => ( false)).
penalty(638, 1) <- ~( !X : ( wineGlass(X) & there_is_a_pastaPlate ) => ( dinner)).
penalty(639, 1) <- ~( !X : ( wineGlass(X) & there_is_a_saucer ) => ( there_is_a_soupPlate)).
penalty(640, 1) <- ~( !X : ( wineGlass(X) & there_is_a_saucer ) => ( there_is_a_waterGlass)).
penalty(641, 1) <- ~( !X : ( wineGlass(X) & there_is_a_saucer ) => ( lunch)).
penalty(642, 1) <- ~( !X : ( wineGlass(X) & there_is_a_waterGlass ) => ( lunch)).
penalty(643, 1) <- ~( !X : ( wineGlass(X) ) => ( lunch | dinner)).
penalty(644, 1) <- ~( !X : ( teaCup(X) & there_is_a_fork ) => ( there_is_a_knife)).
penalty(645, 1) <- ~( !X : ( teaCup(X) & there_is_a_fork ) => ( there_is_a_plate)).
penalty(646, 1) <- ~( !X : ( teaCup(X) ) => ( there_is_a_knife | there_is_a_bowl)).
penalty(647, 1) <- ~( !X : ( teaCup(X) & there_is_a_waterGlass ) => ( there_is_a_knife)).
penalty(648, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) ) => ( there_is_a_knife)).
penalty(649, 1) <- ~( !X : ( teaCup(X) & there_is_a_spoon ) => ( there_is_a_bowl)).
penalty(650, 1) <- ~( !X : ( teaCup(X) ) => ( there_is_a_plate | there_is_a_bowl)).
penalty(651, 1) <- ~( !X : ( teaCup(X) & there_is_a_waterGlass ) => ( there_is_a_plate)).
penalty(652, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) ) => ( there_is_a_plate)).
penalty(653, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) ) => ( there_is_a_waterGlass)).
penalty(654, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(655, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) & knife(Y) ) => ( false)).
penalty(656, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(657, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) & plate(Y) ) => ( false)).
penalty(658, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) & bowl(Y) ) => ( false)).
penalty(659, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) & pastaPlate(Y) ) => ( false)).
penalty(660, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) & soupPlate(Y) ) => ( false)).
penalty(661, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) ) => ( waterGlass(Y))).
penalty(662, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(663, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(664, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) & food_container(Y) ) => ( false)).
penalty(665, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) ) => ( drink_container(Y))).
penalty(666, 1) <- ~(( there_is_a_fork & there_is_a_bowl ) => ( there_is_a_knife)).
penalty(667, 1) <- ~(( there_is_a_fork ) => ( there_is_a_knife | there_is_a_pastaPlate)).
penalty(668, 1) <- ~(( there_is_a_fork & there_is_a_soupPlate ) => ( there_is_a_knife)).
penalty(669, 1) <- ~(( there_is_a_fork & there_is_a_teaCup ) => ( there_is_a_knife)).
penalty(670, 1) <- ~(( there_is_a_fork & breakfast ) => ( there_is_a_knife)).
penalty(671, 1) <- ~(( there_is_a_fork & there_is_a_bowl ) => ( there_is_a_plate)).
penalty(672, 1) <- ~(( there_is_a_fork ) => ( there_is_a_plate | there_is_a_pastaPlate)).
penalty(673, 1) <- ~(( there_is_a_fork & there_is_a_soupPlate ) => ( there_is_a_plate)).
penalty(674, 1) <- ~(( there_is_a_fork & there_is_a_teaCup ) => ( there_is_a_plate)).
penalty(675, 1) <- ~(( there_is_a_fork & breakfast ) => ( there_is_a_plate)).
penalty(676, 1) <- ~(( there_is_a_fork & dinner ) => ( there_is_a_pastaPlate)).
penalty(677, 1) <- ~(( there_is_a_fork & there_is_a_soupPlate ) => ( there_is_a_waterGlass)).
penalty(678, 1) <- ~(( there_is_a_fork & there_is_a_soupPlate ) => ( lunch)).
penalty(679, 1) <- ~(( there_is_a_fork & there_is_a_soupPlate & dinner ) => ( false)).
penalty(680, 1) <- ~(( there_is_a_knife & there_is_a_soupPlate ) => ( there_is_a_fork)).
penalty(681, 1) <- ~(( there_is_a_knife & there_is_a_wineGlass ) => ( there_is_a_fork)).
penalty(682, 1) <- ~(( there_is_a_knife ) => ( there_is_a_fork | breakfast)).
penalty(683, 1) <- ~(( there_is_a_knife & lunch ) => ( there_is_a_fork)).
penalty(684, 1) <- ~(( there_is_a_smallKnife ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(685, 1) <- ~(( there_is_a_saucer ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(686, 1) <- ~(( there_is_a_wineGlass ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(687, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(688, 1) <- ~(( there_is_a_fork | there_is_a_spoon | breakfast)).
penalty(689, 1) <- ~(( lunch ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(690, 1) <- ~(( there_is_a_smallKnife ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(691, 1) <- ~(( there_is_a_smallKnife & there_is_a_soupPlate ) => ( there_is_a_fork)).
penalty(692, 1) <- ~(( there_is_a_smallKnife ) => ( there_is_a_fork | there_is_a_waterGlass)).
penalty(693, 1) <- ~(( there_is_a_smallKnife & there_is_a_wineGlass ) => ( there_is_a_fork)).
penalty(694, 1) <- ~(( there_is_a_smallKnife ) => ( there_is_a_fork | there_is_a_teaCup)).
penalty(695, 1) <- ~(( there_is_a_smallKnife ) => ( there_is_a_fork | breakfast)).
penalty(696, 1) <- ~(( there_is_a_smallKnife & lunch ) => ( there_is_a_fork)).
penalty(697, 1) <- ~(( there_is_a_plate & there_is_a_soupPlate ) => ( there_is_a_fork)).
penalty(698, 1) <- ~(( there_is_a_plate & there_is_a_wineGlass ) => ( there_is_a_fork)).
penalty(699, 1) <- ~(( there_is_a_plate ) => ( there_is_a_fork | breakfast)).
penalty(700, 1) <- ~(( there_is_a_plate & lunch ) => ( there_is_a_fork)).
penalty(701, 1) <- ~(( there_is_a_bowl & there_is_a_wineGlass ) => ( there_is_a_fork)).
penalty(702, 1) <- ~(( there_is_a_saucer ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(703, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(704, 1) <- ~(( lunch ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(705, 1) <- ~(( there_is_a_soupPlate & there_is_a_saucer ) => ( there_is_a_fork)).
penalty(706, 1) <- ~(( there_is_a_soupPlate & there_is_a_waterGlass ) => ( there_is_a_fork)).
penalty(707, 1) <- ~( !X, Y : ( there_is_a_soupPlate & on(X,Y) ) => ( there_is_a_fork)).
penalty(708, 1) <- ~(( there_is_a_soupPlate & lunch ) => ( there_is_a_fork)).
penalty(709, 1) <- ~(( there_is_a_soupPlate ) => ( there_is_a_fork | dinner)).
penalty(710, 1) <- ~(( there_is_a_wineGlass ) => ( there_is_a_fork | there_is_a_soupPlate)).
penalty(711, 1) <- ~(( dinner ) => ( there_is_a_fork | there_is_a_soupPlate)).
penalty(712, 1) <- ~(( there_is_a_saucer ) => ( there_is_a_fork | there_is_a_waterGlass)).
penalty(713, 1) <- ~(( there_is_a_saucer & there_is_a_wineGlass ) => ( there_is_a_fork)).
penalty(714, 1) <- ~(( there_is_a_saucer ) => ( there_is_a_fork | there_is_a_teaCup)).
penalty(715, 1) <- ~(( there_is_a_saucer ) => ( there_is_a_fork | breakfast)).
penalty(716, 1) <- ~(( there_is_a_saucer & lunch ) => ( there_is_a_fork)).
penalty(717, 1) <- ~(( there_is_a_waterGlass & there_is_a_wineGlass ) => ( there_is_a_fork)).
penalty(718, 1) <- ~(( lunch ) => ( there_is_a_fork | there_is_a_waterGlass)).
penalty(719, 1) <- ~( !X, Y : ( there_is_a_wineGlass & on(X,Y) ) => ( there_is_a_fork)).
penalty(720, 1) <- ~(( there_is_a_wineGlass & lunch ) => ( there_is_a_fork)).
penalty(721, 1) <- ~(( there_is_a_wineGlass ) => ( there_is_a_fork | dinner)).
penalty(722, 1) <- ~( !X, Y : ( left_of(X,Y) & plate(Y) ) => ( there_is_a_fork)).
penalty(723, 1) <- ~( !X, Y : ( left_of(X,Y) & bowl(Y) ) => ( there_is_a_fork)).
penalty(724, 1) <- ~( !X, Y : ( left_of(X,Y) & soupPlate(Y) ) => ( there_is_a_fork)).
penalty(725, 1) <- ~( !X, Y : ( left_of(X,Y) & wineGlass(Y) ) => ( there_is_a_fork)).
penalty(726, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(Y) ) => ( there_is_a_fork)).
penalty(727, 1) <- ~( !X, Y : ( above_of(X,Y) & smallKnife(Y) ) => ( there_is_a_fork)).
penalty(728, 1) <- ~( !X, Y : ( above_of(X,Y) & saucer(Y) ) => ( there_is_a_fork)).
penalty(729, 1) <- ~( !X, Y : ( above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_fork)).
penalty(730, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(Y) ) => ( there_is_a_fork)).
penalty(731, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_fork | drink_container(Y))).
penalty(732, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | breakfast)).
penalty(733, 1) <- ~( !X, Y : ( on(X,Y) & lunch ) => ( there_is_a_fork)).
penalty(734, 1) <- ~( !X, Y : ( on(X,Y) & soupPlate(Y) ) => ( there_is_a_fork)).
penalty(735, 1) <- ~(( there_is_a_knife & there_is_a_soupPlate ) => ( there_is_a_waterGlass)).
penalty(736, 1) <- ~(( there_is_a_knife & there_is_a_soupPlate ) => ( lunch)).
penalty(737, 1) <- ~(( there_is_a_knife & there_is_a_wineGlass ) => ( lunch)).
penalty(738, 1) <- ~(( there_is_a_knife ) => ( breakfast | lunch)).
penalty(739, 1) <- ~(( there_is_a_bowl & there_is_a_wineGlass ) => ( there_is_a_knife)).
penalty(740, 1) <- ~(( there_is_a_teaCup ) => ( there_is_a_knife | there_is_a_bowl)).
penalty(741, 1) <- ~(( breakfast ) => ( there_is_a_knife | there_is_a_bowl)).
penalty(742, 1) <- ~(( there_is_a_soupPlate & there_is_a_waterGlass ) => ( there_is_a_knife)).
penalty(743, 1) <- ~(( there_is_a_soupPlate & lunch ) => ( there_is_a_knife)).
penalty(744, 1) <- ~(( there_is_a_soupPlate ) => ( there_is_a_knife | dinner)).
penalty(745, 1) <- ~(( there_is_a_waterGlass & there_is_a_wineGlass ) => ( there_is_a_knife)).
penalty(746, 1) <- ~(( there_is_a_waterGlass & there_is_a_teaCup ) => ( there_is_a_knife)).
penalty(747, 1) <- ~(( lunch ) => ( there_is_a_knife | there_is_a_waterGlass)).
penalty(748, 1) <- ~(( there_is_a_wineGlass & lunch ) => ( there_is_a_knife)).
penalty(749, 1) <- ~(( there_is_a_wineGlass ) => ( there_is_a_knife | dinner)).
penalty(750, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_knife)).
penalty(751, 1) <- ~( !X, Y : ( left_of(X,Y) & bowl(Y) ) => ( there_is_a_knife)).
penalty(752, 1) <- ~( !X, Y : ( left_of(X,Y) & soupPlate(Y) ) => ( there_is_a_knife)).
penalty(753, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( there_is_a_knife)).
penalty(754, 1) <- ~( !X, Y : ( left_of(X,Y) & wineGlass(Y) ) => ( there_is_a_knife)).
penalty(755, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_knife)).
penalty(756, 1) <- ~( !X, Y : ( above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_knife)).
penalty(757, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(Y) ) => ( there_is_a_knife)).
penalty(758, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_knife | drink_container(Y))).
penalty(759, 1) <- ~(( there_is_a_spoon & there_is_a_teaCup ) => ( there_is_a_bowl)).
penalty(760, 1) <- ~(( there_is_a_spoon & breakfast ) => ( there_is_a_bowl)).
penalty(761, 1) <- ~(( there_is_a_spoon & lunch ) => ( there_is_a_waterGlass)).
penalty(762, 1) <- ~(( there_is_a_smallKnife & there_is_a_wineGlass ) => ( there_is_a_spoon)).
penalty(763, 1) <- ~(( there_is_a_smallKnife ) => ( there_is_a_spoon | breakfast)).
penalty(764, 1) <- ~(( there_is_a_smallKnife & lunch ) => ( there_is_a_spoon)).
penalty(765, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_spoon | there_is_a_smallKnife)).
penalty(766, 1) <- ~(( there_is_a_saucer & there_is_a_wineGlass ) => ( there_is_a_spoon)).
penalty(767, 1) <- ~(( there_is_a_saucer ) => ( there_is_a_spoon | breakfast)).
penalty(768, 1) <- ~(( there_is_a_saucer & lunch ) => ( there_is_a_spoon)).
penalty(769, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_spoon | there_is_a_saucer)).
penalty(770, 1) <- ~(( there_is_a_waterGlass & there_is_a_wineGlass ) => ( there_is_a_spoon)).
penalty(771, 1) <- ~( !X, Y : ( there_is_a_wineGlass & on(X,Y) ) => ( there_is_a_spoon)).
penalty(772, 1) <- ~(( there_is_a_wineGlass ) => ( there_is_a_spoon | lunch)).
penalty(773, 1) <- ~( !X, Y : ( left_of(X,Y) & wineGlass(Y) ) => ( there_is_a_spoon)).
penalty(774, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_spoon | breakfast)).
penalty(775, 1) <- ~( !X, Y : ( on(X,Y) & lunch ) => ( there_is_a_spoon)).
penalty(776, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_spoon | smallKnife(Y))).
penalty(777, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_spoon | saucer(X))).
penalty(778, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_spoon | food_utensil(Y))).
penalty(779, 1) <- ~( !X, Y : ( on(X,Y) & food_container(Y) ) => ( there_is_a_spoon)).
penalty(780, 1) <- ~(( there_is_a_spoon | breakfast | lunch)).
penalty(781, 1) <- ~(( there_is_a_smallKnife & there_is_a_bowl & there_is_a_wineGlass ) => ( false)).
penalty(782, 1) <- ~(( there_is_a_smallKnife & there_is_a_bowl ) => ( breakfast)).
penalty(783, 1) <- ~(( there_is_a_smallKnife & there_is_a_bowl & lunch ) => ( false)).
penalty(784, 1) <- ~(( there_is_a_smallKnife & there_is_a_soupPlate ) => ( there_is_a_waterGlass)).
penalty(785, 1) <- ~(( there_is_a_smallKnife & there_is_a_soupPlate ) => ( lunch)).
penalty(786, 1) <- ~(( there_is_a_smallKnife & there_is_a_wineGlass ) => ( there_is_a_soupPlate)).
penalty(787, 1) <- ~(( there_is_a_smallKnife ) => ( there_is_a_soupPlate | breakfast)).
penalty(788, 1) <- ~(( there_is_a_smallKnife & lunch ) => ( there_is_a_soupPlate)).
penalty(789, 1) <- ~(( there_is_a_smallKnife & there_is_a_wineGlass ) => ( there_is_a_waterGlass)).
penalty(790, 1) <- ~(( there_is_a_smallKnife ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(791, 1) <- ~(( there_is_a_smallKnife ) => ( there_is_a_waterGlass | breakfast)).
penalty(792, 1) <- ~(( there_is_a_smallKnife & lunch ) => ( there_is_a_waterGlass)).
penalty(793, 1) <- ~(( there_is_a_smallKnife & there_is_a_wineGlass ) => ( lunch)).
penalty(794, 1) <- ~(( there_is_a_smallKnife ) => ( there_is_a_wineGlass | breakfast)).
penalty(795, 1) <- ~(( there_is_a_smallKnife & lunch ) => ( there_is_a_wineGlass)).
penalty(796, 1) <- ~( !X, Y : ( there_is_a_smallKnife & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(797, 1) <- ~(( there_is_a_smallKnife ) => ( breakfast | lunch)).
penalty(798, 1) <- ~( !X, Y : ( above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_smallKnife)).
penalty(799, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(Y) ) => ( there_is_a_smallKnife)).
penalty(800, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_smallKnife | drink_container(Y))).
penalty(801, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_smallKnife | plate(X))).
penalty(802, 1) <- ~( !X, Y : ( on(X,Y) & food_utensil(Y) ) => ( there_is_a_smallKnife)).
penalty(803, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_smallKnife | food_container(Y))).
penalty(804, 1) <- ~(( there_is_a_plate & there_is_a_soupPlate ) => ( there_is_a_waterGlass)).
penalty(805, 1) <- ~(( there_is_a_plate & there_is_a_soupPlate ) => ( lunch)).
penalty(806, 1) <- ~(( there_is_a_plate & there_is_a_wineGlass ) => ( lunch)).
penalty(807, 1) <- ~(( there_is_a_plate ) => ( breakfast | lunch)).
penalty(808, 1) <- ~(( there_is_a_bowl & there_is_a_wineGlass ) => ( there_is_a_plate)).
penalty(809, 1) <- ~(( there_is_a_teaCup ) => ( there_is_a_plate | there_is_a_bowl)).
penalty(810, 1) <- ~(( breakfast ) => ( there_is_a_plate | there_is_a_bowl)).
penalty(811, 1) <- ~(( there_is_a_soupPlate & there_is_a_waterGlass ) => ( there_is_a_plate)).
penalty(812, 1) <- ~(( there_is_a_soupPlate & lunch ) => ( there_is_a_plate)).
penalty(813, 1) <- ~(( there_is_a_soupPlate ) => ( there_is_a_plate | dinner)).
penalty(814, 1) <- ~(( there_is_a_waterGlass & there_is_a_wineGlass ) => ( there_is_a_plate)).
penalty(815, 1) <- ~(( there_is_a_waterGlass & there_is_a_teaCup ) => ( there_is_a_plate)).
penalty(816, 1) <- ~(( lunch ) => ( there_is_a_plate | there_is_a_waterGlass)).
penalty(817, 1) <- ~(( there_is_a_wineGlass & lunch ) => ( there_is_a_plate)).
penalty(818, 1) <- ~(( there_is_a_wineGlass ) => ( there_is_a_plate | dinner)).
penalty(819, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_plate)).
penalty(820, 1) <- ~( !X, Y : ( left_of(X,Y) & bowl(Y) ) => ( there_is_a_plate)).
penalty(821, 1) <- ~( !X, Y : ( left_of(X,Y) & soupPlate(Y) ) => ( there_is_a_plate)).
penalty(822, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( there_is_a_plate)).
penalty(823, 1) <- ~( !X, Y : ( left_of(X,Y) & wineGlass(Y) ) => ( there_is_a_plate)).
penalty(824, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_plate)).
penalty(825, 1) <- ~( !X, Y : ( above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_plate)).
penalty(826, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(Y) ) => ( there_is_a_plate)).
penalty(827, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_plate | drink_container(Y))).
penalty(828, 1) <- ~(( there_is_a_bowl & there_is_a_saucer & there_is_a_wineGlass ) => ( false)).
penalty(829, 1) <- ~(( there_is_a_bowl & there_is_a_saucer ) => ( breakfast)).
penalty(830, 1) <- ~(( there_is_a_bowl & there_is_a_saucer & lunch ) => ( false)).
penalty(831, 1) <- ~(( there_is_a_bowl & there_is_a_wineGlass ) => ( there_is_a_waterGlass)).
penalty(832, 1) <- ~(( there_is_a_bowl ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(833, 1) <- ~(( there_is_a_bowl ) => ( there_is_a_waterGlass | breakfast)).
penalty(834, 1) <- ~(( there_is_a_bowl & lunch ) => ( there_is_a_waterGlass)).
penalty(835, 1) <- ~(( there_is_a_bowl & there_is_a_wineGlass ) => ( lunch)).
penalty(836, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(837, 1) <- ~(( there_is_a_bowl ) => ( breakfast | lunch)).
penalty(838, 1) <- ~(( there_is_a_pastaPlate & there_is_a_waterGlass & there_is_a_wineGlass ) => ( false)).
penalty(839, 1) <- ~(( there_is_a_pastaPlate & there_is_a_waterGlass ) => ( lunch)).
penalty(840, 1) <- ~(( there_is_a_pastaPlate ) => ( there_is_a_waterGlass | there_is_a_wineGlass)).
penalty(841, 1) <- ~(( there_is_a_pastaPlate & lunch ) => ( there_is_a_waterGlass)).
penalty(842, 1) <- ~(( there_is_a_pastaPlate ) => ( there_is_a_waterGlass | dinner)).
penalty(843, 1) <- ~(( there_is_a_pastaPlate & there_is_a_wineGlass & lunch ) => ( false)).
penalty(844, 1) <- ~(( there_is_a_pastaPlate & there_is_a_wineGlass ) => ( dinner)).
penalty(845, 1) <- ~(( there_is_a_pastaPlate ) => ( there_is_a_wineGlass | lunch)).
penalty(846, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & drink_container(X) ) => ( false)).
penalty(847, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(848, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(849, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(850, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(851, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & above_of(X,Y) ) => ( fork(X))).
penalty(852, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & above_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(853, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & above_of(X,Y) & food_container(Y) ) => ( false)).
penalty(854, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & above_of(X,Y) ) => ( drink_container(Y))).
penalty(855, 1) <- ~( !X : ( there_is_a_pastaPlate & food_container(X) ) => ( pastaPlate(X))).
penalty(856, 1) <- ~(( there_is_a_pastaPlate ) => ( lunch | dinner)).
penalty(857, 1) <- ~(( dinner ) => ( there_is_a_pastaPlate | there_is_a_soupPlate)).
penalty(858, 1) <- ~(( there_is_a_soupPlate & there_is_a_saucer ) => ( there_is_a_waterGlass)).
penalty(859, 1) <- ~(( there_is_a_soupPlate & there_is_a_saucer ) => ( lunch)).
penalty(860, 1) <- ~(( there_is_a_soupPlate & there_is_a_waterGlass ) => ( lunch)).
penalty(861, 1) <- ~( !X, Y : ( there_is_a_soupPlate & on(X,Y) ) => ( there_is_a_waterGlass)).
penalty(862, 1) <- ~(( there_is_a_soupPlate & lunch ) => ( there_is_a_waterGlass)).
penalty(863, 1) <- ~(( there_is_a_soupPlate ) => ( there_is_a_waterGlass | dinner)).
penalty(864, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(865, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(866, 1) <- ~( !X, Y : ( there_is_a_soupPlate & on(X,Y) ) => ( lunch)).
penalty(867, 1) <- ~(( there_is_a_soupPlate ) => ( lunch | dinner)).
penalty(868, 1) <- ~(( there_is_a_saucer & there_is_a_wineGlass ) => ( there_is_a_soupPlate)).
penalty(869, 1) <- ~(( there_is_a_saucer ) => ( there_is_a_soupPlate | breakfast)).
penalty(870, 1) <- ~(( there_is_a_saucer & lunch ) => ( there_is_a_soupPlate)).
penalty(871, 1) <- ~(( there_is_a_saucer & there_is_a_wineGlass ) => ( there_is_a_waterGlass)).
penalty(872, 1) <- ~(( there_is_a_saucer ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(873, 1) <- ~(( there_is_a_saucer ) => ( there_is_a_waterGlass | breakfast)).
penalty(874, 1) <- ~(( there_is_a_saucer & lunch ) => ( there_is_a_waterGlass)).
penalty(875, 1) <- ~(( there_is_a_saucer & there_is_a_wineGlass ) => ( lunch)).
penalty(876, 1) <- ~(( there_is_a_saucer ) => ( there_is_a_wineGlass | breakfast)).
penalty(877, 1) <- ~(( there_is_a_saucer & lunch ) => ( there_is_a_wineGlass)).
penalty(878, 1) <- ~( !X, Y : ( there_is_a_saucer & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(879, 1) <- ~(( there_is_a_saucer ) => ( breakfast | lunch)).
penalty(880, 1) <- ~( !X, Y : ( above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_saucer)).
penalty(881, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(Y) ) => ( there_is_a_saucer)).
penalty(882, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_saucer | drink_container(Y))).
penalty(883, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_saucer | plate(X))).
penalty(884, 1) <- ~( !X, Y : ( on(X,Y) & food_utensil(Y) ) => ( there_is_a_saucer)).
penalty(885, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_saucer | food_container(Y))).
penalty(886, 1) <- ~(( there_is_a_waterGlass & there_is_a_wineGlass ) => ( lunch)).
penalty(887, 1) <- ~(( there_is_a_waterGlass ) => ( breakfast | lunch)).
penalty(888, 1) <- ~( !X, Y : ( there_is_a_wineGlass & on(X,Y) ) => ( there_is_a_waterGlass)).
penalty(889, 1) <- ~(( there_is_a_waterGlass | there_is_a_wineGlass | there_is_a_teaCup)).
penalty(890, 1) <- ~(( there_is_a_waterGlass | there_is_a_wineGlass | breakfast)).
penalty(891, 1) <- ~(( lunch ) => ( there_is_a_waterGlass | there_is_a_wineGlass)).
penalty(892, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(893, 1) <- ~(( breakfast ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(894, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_waterGlass)).
penalty(895, 1) <- ~( !X, Y : ( left_of(X,Y) & soupPlate(Y) ) => ( there_is_a_waterGlass)).
penalty(896, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( there_is_a_waterGlass)).
penalty(897, 1) <- ~( !X, Y : ( left_of(X,Y) & wineGlass(Y) ) => ( there_is_a_waterGlass)).
penalty(898, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_waterGlass)).
penalty(899, 1) <- ~( !X, Y : ( above_of(X,Y) & waterGlass(Y) ) => ( there_is_a_waterGlass)).
penalty(900, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_waterGlass | breakfast)).
penalty(901, 1) <- ~( !X, Y : ( on(X,Y) & lunch ) => ( there_is_a_waterGlass)).
penalty(902, 1) <- ~( !X, Y : ( on(X,Y) & soupPlate(Y) ) => ( there_is_a_waterGlass)).
penalty(903, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(904, 1) <- ~( !X, Y : ( there_is_a_wineGlass & on(X,Y) ) => ( lunch)).
penalty(905, 1) <- ~(( there_is_a_wineGlass ) => ( lunch | dinner)).
penalty(906, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_wineGlass | breakfast)).
penalty(907, 1) <- ~( !X, Y : ( on(X,Y) & lunch ) => ( there_is_a_wineGlass)).
penalty(908, 1) <- ~(( there_is_a_wineGlass | breakfast | lunch)).
penalty(909, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(910, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( there_is_a_teaCup)).
penalty(911, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & fork(Y) ) => ( false)).
penalty(912, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & waterGlass(Y) ) => ( false)).
penalty(913, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & wineGlass(Y) ) => ( false)).
penalty(914, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & drink_container(Y) ) => ( false)).
penalty(915, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(X) & plate(Y) ) => ( false)).
penalty(916, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(X) & bowl(Y) ) => ( false)).
penalty(917, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(X) & pastaPlate(Y) ) => ( false)).
penalty(918, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(X) & soupPlate(Y) ) => ( false)).
penalty(919, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(X) & waterGlass(Y) ) => ( false)).
penalty(920, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(X) & wineGlass(Y) ) => ( false)).
penalty(921, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(X) ) => ( food_utensil(Y))).
penalty(922, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(X) & food_container(Y) ) => ( false)).
penalty(923, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(X) & drink_container(Y) ) => ( false)).
penalty(924, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & dinner ) => ( false)).
penalty(925, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & fork(Y) ) => ( false)).
penalty(926, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & knife(Y) ) => ( false)).
penalty(927, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & spoon(Y) ) => ( false)).
penalty(928, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & plate(Y) ) => ( false)).
penalty(929, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & bowl(Y) ) => ( false)).
penalty(930, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & pastaPlate(Y) ) => ( false)).
penalty(931, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & soupPlate(Y) ) => ( false)).
penalty(932, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & food_utensil(Y) ) => ( false)).
penalty(933, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & food_container(Y) ) => ( false)).
penalty(934, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( drink_container(Y))).
penalty(935, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast & fork(Y) ) => ( false)).
penalty(936, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( breakfast)).
penalty(937, 1) <- ~( !X, Y : ( left_of(X,Y) & lunch & waterGlass(Y) ) => ( false)).
penalty(938, 1) <- ~( !X, Y : ( left_of(X,Y) & soupPlate(Y) ) => ( lunch)).
penalty(939, 1) <- ~( !X, Y : ( left_of(X,Y) & wineGlass(Y) ) => ( lunch)).
penalty(940, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner & soupPlate(Y) ) => ( false)).
penalty(941, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner & wineGlass(Y) ) => ( false)).
penalty(942, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner & drink_container(Y) ) => ( false)).
penalty(943, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( food_container(X))).
penalty(944, 1) <- ~( !X, Y : ( left_of(X,Y) & plate(Y) ) => ( fork(X))).
penalty(945, 1) <- ~( !X, Y : ( left_of(X,Y) & bowl(Y) ) => ( fork(X))).
penalty(946, 1) <- ~( !X, Y : ( left_of(X,Y) & soupPlate(Y) ) => ( fork(X))).
penalty(947, 1) <- ~( !X, Y : ( left_of(X,Y) & pastaPlate(Y) ) => ( spoon(X))).
penalty(948, 1) <- ~( !X, Y : ( left_of(X,Y) & plate(Y) ) => ( food_utensil(X))).
penalty(949, 1) <- ~( !X, Y : ( left_of(X,Y) & bowl(Y) ) => ( food_utensil(X))).
penalty(950, 1) <- ~( !X, Y : ( left_of(X,Y) & pastaPlate(Y) ) => ( food_utensil(X))).
penalty(951, 1) <- ~( !X, Y : ( left_of(X,Y) & soupPlate(Y) ) => ( food_utensil(X))).
penalty(952, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( teaCup(X))).
penalty(953, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( drink_container(X))).
penalty(954, 1) <- ~( !X, Y : ( left_of(X,Y) & wineGlass(Y) ) => ( waterGlass(X))).
penalty(955, 1) <- ~( !X, Y : ( left_of(X,Y) & wineGlass(Y) ) => ( drink_container(X))).
penalty(956, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(Y) ) => ( food_utensil(X))).
penalty(957, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( drink_container(X))).
penalty(958, 1) <- ~( !X, Y : ( above_of(X,Y) & dinner ) => ( wineGlass(Y))).
penalty(959, 1) <- ~( !X, Y : ( above_of(X,Y) & dinner & food_utensil(Y) ) => ( false)).
penalty(960, 1) <- ~( !X, Y : ( above_of(X,Y) & dinner & food_container(Y) ) => ( false)).
penalty(961, 1) <- ~( !X, Y : ( above_of(X,Y) & dinner ) => ( drink_container(Y))).
penalty(962, 1) <- ~( !X, Y : ( above_of(X,Y) & smallKnife(Y) ) => ( fork(X))).
penalty(963, 1) <- ~( !X, Y : ( above_of(X,Y) & saucer(Y) ) => ( fork(X))).
penalty(964, 1) <- ~( !X, Y : ( above_of(X,Y) & food_utensil(Y) ) => ( fork(X))).
penalty(965, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(Y) ) => ( fork(X))).
penalty(966, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( fork(X) | drink_container(Y))).
penalty(967, 1) <- ~( !X, Y : ( above_of(X,Y) & left_of(Y,Y) ) => ( knife(X))).
penalty(968, 1) <- ~( !X, Y : ( above_of(X,Y) & food_utensil(Y) ) => ( smallKnife(Y))).
penalty(969, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(Y) ) => ( saucer(Y))).
penalty(970, 1) <- ~( !X, Y : ( on(X,Y) ) => ( breakfast | lunch)).
penalty(971, 1) <- ~( !X, Y : ( on(X,Y) & soupPlate(Y) ) => ( lunch)).
penalty(972, 1) <- ~( !X, Y : ( on(X,Y) & smallKnife(Y) ) => ( saucer(X))).
penalty(973, 1) <- ~( !X, Y : ( on(X,Y) ) => ( smallKnife(Y) | plate(X))).
penalty(974, 1) <- ~( !X, Y : ( on(X,Y) & food_utensil(Y) ) => ( smallKnife(Y))).
penalty(975, 1) <- ~( !X, Y : ( on(X,Y) ) => ( smallKnife(Y) | food_container(Y))).
penalty(976, 1) <- ~( !X, Y : ( on(X,Y) & bowl(Y) ) => ( plate(X))).
penalty(977, 1) <- ~( !X, Y : ( on(X,Y) ) => ( plate(X) | saucer(X))).
penalty(978, 1) <- ~( !X, Y : ( on(X,Y) & soupPlate(Y) ) => ( plate(X))).
penalty(979, 1) <- ~( !X, Y : ( on(X,Y) ) => ( plate(X) | food_utensil(Y))).
penalty(980, 1) <- ~( !X, Y : ( on(X,Y) & food_container(Y) ) => ( plate(X))).
penalty(981, 1) <- ~( !X, Y : ( on(X,Y) & food_utensil(Y) ) => ( saucer(X))).
penalty(982, 1) <- ~( !X, Y : ( on(X,Y) ) => ( saucer(X) | food_container(Y))).
penalty(983, 1) <- ~( !X, Y : ( on(X,Y) ) => ( food_utensil(Y) | food_container(Y))).
penalty(984, 1) <- ~( !X : ( drink_container(X) & dinner ) => ( wineGlass(X))).
penalty(985, 1) <- ~(( breakfast | lunch | dinner)).
penalty(986, 1) <- ~( !X, Y : ( fork(X) & there_is_a_knife & above_of(X,Y) ) => ( there_is_a_smallKnife)).
penalty(987, 1) <- ~( !X, Y : ( fork(X) & there_is_a_knife & above_of(X,Y) ) => ( there_is_a_saucer)).
penalty(988, 1) <- ~( !X, Y : ( fork(X) & there_is_a_knife & above_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(989, 1) <- ~( !X, Y : ( fork(X) & there_is_a_knife & above_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(990, 1) <- ~( !X, Y : ( fork(X) & there_is_a_knife & above_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(991, 1) <- ~( !X : ( fork(X) & there_is_a_waterGlass ) => ( there_is_a_knife | lunch)).
penalty(992, 1) <- ~( !X : ( fork(X) ) => ( there_is_a_knife | there_is_a_waterGlass | there_is_a_wineGlass)).
penalty(993, 1) <- ~( !X : ( fork(X) ) => ( there_is_a_knife | there_is_a_waterGlass | dinner)).
penalty(994, 1) <- ~( !X : ( fork(X) ) => ( there_is_a_knife | there_is_a_wineGlass | lunch)).
penalty(995, 1) <- ~( !X : ( fork(X) ) => ( there_is_a_knife | lunch | dinner)).
penalty(996, 1) <- ~( !X, Y : ( fork(X) & there_is_a_spoon & left_of(X,Y) & knife(Y) ) => ( false)).
penalty(997, 1) <- ~( !X, Y : ( fork(X) & there_is_a_spoon & left_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(998, 1) <- ~( !X, Y : ( fork(X) & there_is_a_spoon & left_of(X,Y) ) => ( food_container(Y))).
penalty(999, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) ) => ( there_is_a_spoon | there_is_a_smallKnife)).
penalty(1000, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) ) => ( there_is_a_spoon | there_is_a_saucer)).
penalty(1001, 1) <- ~( !X, Y : ( fork(X) & there_is_a_wineGlass & above_of(X,Y) ) => ( there_is_a_spoon)).
penalty(1002, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) ) => ( there_is_a_spoon | breakfast)).
penalty(1003, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & lunch ) => ( there_is_a_spoon)).
penalty(1004, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & waterGlass(Y) ) => ( there_is_a_spoon)).
penalty(1005, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & wineGlass(Y) ) => ( there_is_a_spoon)).
penalty(1006, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & drink_container(Y) ) => ( there_is_a_spoon)).
penalty(1007, 1) <- ~( !X, Y : ( fork(X) & there_is_a_smallKnife & left_of(X,Y) & knife(Y) ) => ( false)).
penalty(1008, 1) <- ~( !X, Y : ( fork(X) & there_is_a_smallKnife & left_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(1009, 1) <- ~( !X, Y : ( fork(X) & there_is_a_smallKnife & left_of(X,Y) ) => ( food_container(Y))).
penalty(1010, 1) <- ~( !X, Y : ( fork(X) & there_is_a_smallKnife & above_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(1011, 1) <- ~( !X, Y : ( fork(X) & there_is_a_smallKnife & above_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(1012, 1) <- ~( !X, Y : ( fork(X) & there_is_a_smallKnife & above_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(1013, 1) <- ~( !X, Y : ( fork(X) & there_is_a_plate & above_of(X,Y) ) => ( there_is_a_smallKnife)).
penalty(1014, 1) <- ~( !X, Y : ( fork(X) & there_is_a_bowl & above_of(X,Y) ) => ( there_is_a_smallKnife)).
penalty(1015, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) ) => ( there_is_a_smallKnife | there_is_a_pastaPlate)).
penalty(1016, 1) <- ~( !X, Y : ( fork(X) & there_is_a_soupPlate & above_of(X,Y) ) => ( there_is_a_smallKnife)).
penalty(1017, 1) <- ~( !X, Y : ( fork(X) & there_is_a_teaCup & above_of(X,Y) ) => ( there_is_a_smallKnife)).
penalty(1018, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & breakfast ) => ( there_is_a_smallKnife)).
penalty(1019, 1) <- ~( !X, Y : ( fork(X) & there_is_a_plate & above_of(X,Y) ) => ( there_is_a_saucer)).
penalty(1020, 1) <- ~( !X, Y : ( fork(X) & there_is_a_plate & above_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(1021, 1) <- ~( !X, Y : ( fork(X) & there_is_a_plate & above_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(1022, 1) <- ~( !X, Y : ( fork(X) & there_is_a_plate & above_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(1023, 1) <- ~( !X : ( fork(X) & there_is_a_waterGlass ) => ( there_is_a_plate | lunch)).
penalty(1024, 1) <- ~( !X : ( fork(X) ) => ( there_is_a_plate | there_is_a_waterGlass | there_is_a_wineGlass)).
penalty(1025, 1) <- ~( !X : ( fork(X) ) => ( there_is_a_plate | there_is_a_waterGlass | dinner)).
penalty(1026, 1) <- ~( !X : ( fork(X) ) => ( there_is_a_plate | there_is_a_wineGlass | lunch)).
penalty(1027, 1) <- ~( !X : ( fork(X) ) => ( there_is_a_plate | lunch | dinner)).
penalty(1028, 1) <- ~( !X, Y : ( fork(X) & there_is_a_bowl & above_of(X,Y) ) => ( there_is_a_saucer)).
penalty(1029, 1) <- ~( !X, Y : ( fork(X) & there_is_a_bowl & there_is_a_wineGlass & above_of(X,Y) ) => ( false)).
penalty(1030, 1) <- ~( !X : ( fork(X) & there_is_a_bowl ) => ( there_is_a_wineGlass | breakfast)).
penalty(1031, 1) <- ~( !X : ( fork(X) & there_is_a_bowl & lunch ) => ( there_is_a_wineGlass)).
penalty(1032, 1) <- ~( !X, Y : ( fork(X) & there_is_a_bowl & left_of(X,Y) & knife(Y) ) => ( false)).
penalty(1033, 1) <- ~( !X, Y : ( fork(X) & there_is_a_bowl & left_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(1034, 1) <- ~( !X, Y : ( fork(X) & there_is_a_bowl & left_of(X,Y) ) => ( food_container(Y))).
penalty(1035, 1) <- ~( !X, Y : ( fork(X) & there_is_a_bowl & above_of(X,Y) ) => ( breakfast)).
penalty(1036, 1) <- ~( !X, Y : ( fork(X) & there_is_a_bowl & above_of(X,Y) & lunch ) => ( false)).
penalty(1037, 1) <- ~( !X, Y : ( fork(X) & there_is_a_bowl & above_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(1038, 1) <- ~( !X, Y : ( fork(X) & there_is_a_bowl & above_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(1039, 1) <- ~( !X, Y : ( fork(X) & there_is_a_bowl & above_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(1040, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) ) => ( there_is_a_pastaPlate | there_is_a_saucer)).
penalty(1041, 1) <- ~( !X : ( fork(X) & there_is_a_wineGlass ) => ( there_is_a_pastaPlate | lunch)).
penalty(1042, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & waterGlass(Y) ) => ( there_is_a_pastaPlate)).
penalty(1043, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & wineGlass(Y) ) => ( there_is_a_pastaPlate)).
penalty(1044, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & drink_container(Y) ) => ( there_is_a_pastaPlate)).
penalty(1045, 1) <- ~( !X : ( fork(X) ) => ( there_is_a_pastaPlate | breakfast | lunch)).
penalty(1046, 1) <- ~( !X, Y : ( fork(X) & there_is_a_soupPlate & above_of(X,Y) ) => ( there_is_a_saucer)).
penalty(1047, 1) <- ~( !X, Y : ( fork(X) & there_is_a_soupPlate & left_of(X,Y) & knife(Y) ) => ( false)).
penalty(1048, 1) <- ~( !X, Y : ( fork(X) & there_is_a_soupPlate & left_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(1049, 1) <- ~( !X, Y : ( fork(X) & there_is_a_soupPlate & left_of(X,Y) ) => ( food_container(Y))).
penalty(1050, 1) <- ~( !X, Y : ( fork(X) & there_is_a_soupPlate & above_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(1051, 1) <- ~( !X, Y : ( fork(X) & there_is_a_soupPlate & above_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(1052, 1) <- ~( !X, Y : ( fork(X) & there_is_a_soupPlate & above_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(1053, 1) <- ~( !X, Y : ( fork(X) & there_is_a_saucer & left_of(X,Y) & knife(Y) ) => ( false)).
penalty(1054, 1) <- ~( !X, Y : ( fork(X) & there_is_a_saucer & left_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(1055, 1) <- ~( !X, Y : ( fork(X) & there_is_a_saucer & left_of(X,Y) ) => ( food_container(Y))).
penalty(1056, 1) <- ~( !X, Y : ( fork(X) & there_is_a_saucer & above_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(1057, 1) <- ~( !X, Y : ( fork(X) & there_is_a_saucer & above_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(1058, 1) <- ~( !X, Y : ( fork(X) & there_is_a_saucer & above_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(1059, 1) <- ~( !X, Y : ( fork(X) & there_is_a_teaCup & above_of(X,Y) ) => ( there_is_a_saucer)).
penalty(1060, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & breakfast ) => ( there_is_a_saucer)).
penalty(1061, 1) <- ~( !X, Y : ( fork(X) & there_is_a_waterGlass & left_of(X,Y) & knife(Y) ) => ( false)).
penalty(1062, 1) <- ~( !X, Y : ( fork(X) & there_is_a_waterGlass & left_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(1063, 1) <- ~( !X, Y : ( fork(X) & there_is_a_waterGlass & left_of(X,Y) ) => ( food_container(Y))).
penalty(1064, 1) <- ~( !X, Y : ( fork(X) & there_is_a_waterGlass & above_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(1065, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & lunch ) => ( there_is_a_waterGlass)).
penalty(1066, 1) <- ~( !X, Y : ( fork(X) & there_is_a_wineGlass & left_of(X,Y) ) => ( lunch)).
penalty(1067, 1) <- ~( !X, Y : ( fork(X) & there_is_a_wineGlass & above_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(1068, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & knife(Y) ) => ( there_is_a_wineGlass)).
penalty(1069, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & food_utensil(Y) ) => ( there_is_a_wineGlass)).
penalty(1070, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) ) => ( there_is_a_wineGlass | food_container(Y))).
penalty(1071, 1) <- ~( !X, Y : ( fork(X) & there_is_a_teaCup & left_of(X,Y) & knife(Y) ) => ( false)).
penalty(1072, 1) <- ~( !X, Y : ( fork(X) & there_is_a_teaCup & left_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(1073, 1) <- ~( !X, Y : ( fork(X) & there_is_a_teaCup & left_of(X,Y) ) => ( food_container(Y))).
penalty(1074, 1) <- ~( !X, Y : ( fork(X) & there_is_a_teaCup & above_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(1075, 1) <- ~( !X, Y : ( fork(X) & there_is_a_teaCup & above_of(X,Y) & drink_container(Y) ) => ( false)).
penalty(1076, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & breakfast & knife(Y) ) => ( false)).
penalty(1077, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & breakfast & food_utensil(Y) ) => ( false)).
penalty(1078, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & breakfast ) => ( food_container(Y))).
penalty(1079, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) ) => ( breakfast | lunch)).
penalty(1080, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & knife(Y) ) => ( lunch)).
penalty(1081, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & food_utensil(Y) ) => ( lunch)).
penalty(1082, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) ) => ( lunch | food_container(Y))).
penalty(1083, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) & food_utensil(Y) ) => ( knife(Y))).
penalty(1084, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) ) => ( knife(Y) | food_container(Y))).
penalty(1085, 1) <- ~( !X, Y : ( fork(X) & left_of(X,Y) ) => ( food_utensil(Y) | food_container(Y))).
penalty(1086, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & breakfast & waterGlass(Y) ) => ( false)).
penalty(1087, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & breakfast & drink_container(Y) ) => ( false)).
penalty(1088, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & lunch & wineGlass(Y) ) => ( false)).
penalty(1089, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & waterGlass(Y) ) => ( lunch)).
penalty(1090, 1) <- ~( !X, Y : ( fork(X) & above_of(X,Y) & wineGlass(Y) ) => ( dinner)).
penalty(1091, 1) <- ~( !X : ( knife(X) & there_is_a_spoon ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1092, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1093, 1) <- ~( !X : ( knife(X) ) => ( there_is_a_fork | there_is_a_waterGlass | there_is_a_teaCup)).
penalty(1094, 1) <- ~( !X, Y : ( knife(X) & above_of(X,Y) & wineGlass(Y) ) => ( there_is_a_fork)).
penalty(1095, 1) <- ~( !X : ( knife(X) & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1096, 1) <- ~( !X : ( knife(X) & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1097, 1) <- ~( !X : ( knife(X) & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1098, 1) <- ~( !X : ( knife(X) & there_is_a_spoon ) => ( there_is_a_bowl | lunch)).
penalty(1099, 1) <- ~( !X : ( knife(X) & there_is_a_spoon & there_is_a_wineGlass ) => ( there_is_a_waterGlass)).
penalty(1100, 1) <- ~( !X : ( knife(X) & there_is_a_spoon ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(1101, 1) <- ~( !X : ( knife(X) & there_is_a_spoon ) => ( there_is_a_waterGlass | breakfast)).
penalty(1102, 1) <- ~( !X : ( knife(X) & there_is_a_spoon ) => ( there_is_a_wineGlass | breakfast)).
penalty(1103, 1) <- ~( !X : ( knife(X) & there_is_a_spoon & lunch ) => ( there_is_a_wineGlass)).
penalty(1104, 1) <- ~( !X, Y : ( knife(X) & there_is_a_spoon & above_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(1105, 1) <- ~( !X, Y : ( knife(X) & there_is_a_smallKnife & above_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(1106, 1) <- ~( !X : ( knife(X) & there_is_a_bowl ) => ( there_is_a_wineGlass | breakfast)).
penalty(1107, 1) <- ~( !X : ( knife(X) & there_is_a_bowl & lunch ) => ( there_is_a_wineGlass)).
penalty(1108, 1) <- ~( !X, Y : ( knife(X) & there_is_a_bowl & above_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(1109, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1110, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1111, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1112, 1) <- ~( !X, Y : ( knife(X) & there_is_a_teaCup & left_of(X,Y) ) => ( there_is_a_bowl)).
penalty(1113, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) & breakfast ) => ( there_is_a_bowl)).
penalty(1114, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) ) => ( there_is_a_bowl | lunch)).
penalty(1115, 1) <- ~( !X, Y : ( knife(X) & there_is_a_soupPlate & above_of(X,Y) ) => ( waterGlass(Y))).
penalty(1116, 1) <- ~( !X, Y : ( knife(X) & there_is_a_soupPlate & above_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(1117, 1) <- ~( !X, Y : ( knife(X) & there_is_a_saucer & above_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(1118, 1) <- ~( !X, Y : ( knife(X) & there_is_a_waterGlass & above_of(X,Y) & wineGlass(Y) ) => ( false)).
penalty(1119, 1) <- ~( !X, Y : ( knife(X) & there_is_a_wineGlass & left_of(X,Y) ) => ( there_is_a_waterGlass)).
penalty(1120, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(1121, 1) <- ~( !X : ( knife(X) ) => ( there_is_a_waterGlass | there_is_a_teaCup | lunch)).
penalty(1122, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) ) => ( there_is_a_waterGlass | breakfast)).
penalty(1123, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) & lunch ) => ( there_is_a_waterGlass)).
penalty(1124, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) ) => ( there_is_a_wineGlass | breakfast)).
penalty(1125, 1) <- ~( !X, Y : ( knife(X) & left_of(X,Y) & lunch ) => ( there_is_a_wineGlass)).
penalty(1126, 1) <- ~( !X, Y : ( knife(X) & there_is_a_teaCup & above_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(1127, 1) <- ~( !X, Y : ( knife(X) & there_is_a_teaCup & above_of(X,Y) ) => ( teaCup(Y))).
penalty(1128, 1) <- ~( !X, Y : ( knife(X) & above_of(X,Y) & wineGlass(Y) ) => ( lunch)).
penalty(1129, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_fork & above_of(X,Y) ) => ( there_is_a_knife)).
penalty(1130, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_fork & above_of(X,Y) ) => ( there_is_a_plate)).
penalty(1131, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_fork & above_of(X,Y) ) => ( there_is_a_waterGlass)).
penalty(1132, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_fork & above_of(X,Y) & dinner ) => ( false)).
penalty(1133, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_fork & above_of(X,Y) & teaCup(Y) ) => ( false)).
penalty(1134, 1) <- ~( !X : ( spoon(X) & there_is_a_knife ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1135, 1) <- ~( !X : ( spoon(X) & there_is_a_plate ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1136, 1) <- ~( !X : ( spoon(X) ) => ( there_is_a_fork | there_is_a_bowl | there_is_a_soupPlate)).
penalty(1137, 1) <- ~( !X : ( spoon(X) & there_is_a_waterGlass ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1138, 1) <- ~( !X : ( spoon(X) ) => ( there_is_a_fork | there_is_a_bowl | there_is_a_wineGlass)).
penalty(1139, 1) <- ~( !X : ( spoon(X) ) => ( there_is_a_fork | there_is_a_bowl | dinner)).
penalty(1140, 1) <- ~( !X : ( spoon(X) & there_is_a_knife ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1141, 1) <- ~( !X : ( spoon(X) & there_is_a_knife ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1142, 1) <- ~( !X : ( spoon(X) & there_is_a_knife ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1143, 1) <- ~( !X : ( spoon(X) & there_is_a_knife ) => ( there_is_a_bowl | lunch)).
penalty(1144, 1) <- ~( !X : ( spoon(X) & there_is_a_knife & there_is_a_wineGlass ) => ( there_is_a_waterGlass)).
penalty(1145, 1) <- ~( !X : ( spoon(X) & there_is_a_knife ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(1146, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_knife & above_of(X,Y) ) => ( there_is_a_waterGlass)).
penalty(1147, 1) <- ~( !X : ( spoon(X) & there_is_a_knife ) => ( there_is_a_waterGlass | breakfast)).
penalty(1148, 1) <- ~( !X : ( spoon(X) & there_is_a_knife ) => ( there_is_a_wineGlass | breakfast)).
penalty(1149, 1) <- ~( !X : ( spoon(X) & there_is_a_knife & lunch ) => ( there_is_a_wineGlass)).
penalty(1150, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_knife & above_of(X,Y) & teaCup(Y) ) => ( false)).
penalty(1151, 1) <- ~( !X : ( spoon(X) & there_is_a_smallKnife ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1152, 1) <- ~( !X : ( spoon(X) & there_is_a_smallKnife ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1153, 1) <- ~( !X : ( spoon(X) & there_is_a_smallKnife ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1154, 1) <- ~( !X : ( spoon(X) & there_is_a_smallKnife ) => ( there_is_a_bowl | lunch)).
penalty(1155, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_smallKnife & above_of(X,Y) ) => ( there_is_a_waterGlass)).
penalty(1156, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_smallKnife & above_of(X,Y) & teaCup(Y) ) => ( false)).
penalty(1157, 1) <- ~( !X : ( spoon(X) & there_is_a_plate ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1158, 1) <- ~( !X : ( spoon(X) & there_is_a_plate ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1159, 1) <- ~( !X : ( spoon(X) & there_is_a_plate ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1160, 1) <- ~( !X : ( spoon(X) & there_is_a_plate ) => ( there_is_a_bowl | lunch)).
penalty(1161, 1) <- ~( !X : ( spoon(X) & there_is_a_plate & there_is_a_wineGlass ) => ( there_is_a_waterGlass)).
penalty(1162, 1) <- ~( !X : ( spoon(X) & there_is_a_plate ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(1163, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_plate & above_of(X,Y) ) => ( there_is_a_waterGlass)).
penalty(1164, 1) <- ~( !X : ( spoon(X) & there_is_a_plate ) => ( there_is_a_waterGlass | breakfast)).
penalty(1165, 1) <- ~( !X : ( spoon(X) & there_is_a_plate ) => ( there_is_a_wineGlass | breakfast)).
penalty(1166, 1) <- ~( !X : ( spoon(X) & there_is_a_plate & lunch ) => ( there_is_a_wineGlass)).
penalty(1167, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_plate & above_of(X,Y) & teaCup(Y) ) => ( false)).
penalty(1168, 1) <- ~( !X : ( spoon(X) ) => ( there_is_a_bowl | there_is_a_pastaPlate | there_is_a_soupPlate)).
penalty(1169, 1) <- ~( !X : ( spoon(X) ) => ( there_is_a_bowl | there_is_a_pastaPlate | there_is_a_wineGlass)).
penalty(1170, 1) <- ~( !X : ( spoon(X) & there_is_a_saucer ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1171, 1) <- ~( !X, Y : ( spoon(X) & above_of(X,Y) ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1172, 1) <- ~( !X : ( spoon(X) & there_is_a_saucer ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1173, 1) <- ~( !X : ( spoon(X) & there_is_a_saucer ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1174, 1) <- ~( !X : ( spoon(X) & there_is_a_saucer ) => ( there_is_a_bowl | lunch)).
penalty(1175, 1) <- ~( !X : ( spoon(X) & there_is_a_waterGlass ) => ( there_is_a_bowl | lunch)).
penalty(1176, 1) <- ~( !X : ( spoon(X) ) => ( there_is_a_bowl | there_is_a_waterGlass | there_is_a_wineGlass)).
penalty(1177, 1) <- ~( !X : ( spoon(X) ) => ( there_is_a_bowl | there_is_a_waterGlass | dinner)).
penalty(1178, 1) <- ~( !X, Y : ( spoon(X) & above_of(X,Y) ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1179, 1) <- ~( !X : ( spoon(X) ) => ( there_is_a_bowl | there_is_a_wineGlass | lunch)).
penalty(1180, 1) <- ~( !X, Y : ( spoon(X) & above_of(X,Y) & waterGlass(Y) ) => ( there_is_a_bowl)).
penalty(1181, 1) <- ~( !X, Y : ( spoon(X) & above_of(X,Y) ) => ( there_is_a_bowl | wineGlass(Y))).
penalty(1182, 1) <- ~( !X, Y : ( spoon(X) & above_of(X,Y) & teaCup(Y) ) => ( there_is_a_bowl)).
penalty(1183, 1) <- ~( !X : ( spoon(X) ) => ( there_is_a_bowl | lunch | dinner)).
penalty(1184, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_soupPlate & above_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(1185, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_soupPlate & above_of(X,Y) ) => ( wineGlass(Y))).
penalty(1186, 1) <- ~( !X, Y : ( spoon(X) & above_of(X,Y) & dinner ) => ( there_is_a_soupPlate)).
penalty(1187, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_saucer & above_of(X,Y) ) => ( there_is_a_waterGlass)).
penalty(1188, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_saucer & above_of(X,Y) & teaCup(Y) ) => ( false)).
penalty(1189, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_waterGlass & there_is_a_wineGlass & left_of(X,Y) ) => ( false)).
penalty(1190, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_waterGlass & left_of(X,Y) ) => ( lunch)).
penalty(1191, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_waterGlass & above_of(X,Y) & teaCup(Y) ) => ( false)).
penalty(1192, 1) <- ~( !X : ( spoon(X) & there_is_a_wineGlass ) => ( there_is_a_waterGlass | dinner)).
penalty(1193, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) ) => ( there_is_a_waterGlass | there_is_a_wineGlass)).
penalty(1194, 1) <- ~( !X : ( spoon(X) ) => ( there_is_a_waterGlass | there_is_a_teaCup | dinner)).
penalty(1195, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) ) => ( there_is_a_waterGlass | dinner)).
penalty(1196, 1) <- ~( !X : ( spoon(X) ) => ( there_is_a_waterGlass | breakfast | dinner)).
penalty(1197, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_wineGlass & left_of(X,Y) & lunch ) => ( false)).
penalty(1198, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_wineGlass & left_of(X,Y) ) => ( dinner)).
penalty(1199, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_wineGlass & above_of(X,Y) & waterGlass(Y) ) => ( false)).
penalty(1200, 1) <- ~( !X, Y : ( spoon(X) & there_is_a_wineGlass & above_of(X,Y) ) => ( wineGlass(Y))).
penalty(1201, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) ) => ( there_is_a_wineGlass | lunch)).
penalty(1202, 1) <- ~( !X, Y : ( spoon(X) & left_of(X,Y) ) => ( lunch | dinner)).
penalty(1203, 1) <- ~( !X : ( smallKnife(X) & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1204, 1) <- ~( !X : ( smallKnife(X) & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1205, 1) <- ~( !X : ( smallKnife(X) & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1206, 1) <- ~( !X : ( smallKnife(X) & there_is_a_spoon ) => ( there_is_a_bowl | lunch)).
penalty(1207, 1) <- ~( !X : ( plate(X) & there_is_a_spoon ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1208, 1) <- ~( !X : ( plate(X) ) => ( there_is_a_fork | there_is_a_waterGlass | there_is_a_teaCup)).
penalty(1209, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) ) => ( there_is_a_fork | knife(Y))).
penalty(1210, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) ) => ( there_is_a_fork | bowl(Y))).
penalty(1211, 1) <- ~( !X : ( plate(X) & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1212, 1) <- ~( !X : ( plate(X) & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1213, 1) <- ~( !X : ( plate(X) & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1214, 1) <- ~( !X : ( plate(X) & there_is_a_spoon ) => ( there_is_a_bowl | lunch)).
penalty(1215, 1) <- ~( !X : ( plate(X) & there_is_a_spoon & there_is_a_wineGlass ) => ( there_is_a_waterGlass)).
penalty(1216, 1) <- ~( !X : ( plate(X) & there_is_a_spoon ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(1217, 1) <- ~( !X : ( plate(X) & there_is_a_spoon ) => ( there_is_a_waterGlass | breakfast)).
penalty(1218, 1) <- ~( !X : ( plate(X) & there_is_a_spoon ) => ( there_is_a_wineGlass | breakfast)).
penalty(1219, 1) <- ~( !X : ( plate(X) & there_is_a_spoon & lunch ) => ( there_is_a_wineGlass)).
penalty(1220, 1) <- ~( !X, Y : ( plate(X) & there_is_a_spoon & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(1221, 1) <- ~( !X, Y : ( plate(X) & there_is_a_spoon & left_of(X,Y) ) => ( knife(Y))).
penalty(1222, 1) <- ~( !X, Y : ( plate(X) & there_is_a_smallKnife & left_of(X,Y) ) => ( knife(Y))).
penalty(1223, 1) <- ~( !X : ( plate(X) & there_is_a_bowl ) => ( there_is_a_wineGlass | breakfast)).
penalty(1224, 1) <- ~( !X : ( plate(X) & there_is_a_bowl & lunch ) => ( there_is_a_wineGlass)).
penalty(1225, 1) <- ~( !X, Y : ( plate(X) & there_is_a_bowl & left_of(X,Y) ) => ( knife(Y))).
penalty(1226, 1) <- ~( !X, Y : ( plate(X) & there_is_a_bowl & on(X,Y) ) => ( bowl(Y))).
penalty(1227, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1228, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1229, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1230, 1) <- ~( !X, Y : ( plate(X) & there_is_a_teaCup & on(X,Y) ) => ( there_is_a_bowl)).
penalty(1231, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) & breakfast ) => ( there_is_a_bowl)).
penalty(1232, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) ) => ( there_is_a_bowl | lunch)).
penalty(1233, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) ) => ( there_is_a_bowl | soupPlate(Y))).
penalty(1234, 1) <- ~( !X, Y : ( plate(X) & there_is_a_soupPlate & left_of(X,Y) ) => ( knife(Y))).
penalty(1235, 1) <- ~( !X, Y : ( plate(X) & there_is_a_soupPlate & on(X,Y) ) => ( soupPlate(Y))).
penalty(1236, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) ) => ( there_is_a_soupPlate | bowl(Y))).
penalty(1237, 1) <- ~( !X, Y : ( plate(X) & there_is_a_saucer & left_of(X,Y) ) => ( knife(Y))).
penalty(1238, 1) <- ~( !X, Y : ( plate(X) & there_is_a_waterGlass & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(1239, 1) <- ~( !X, Y : ( plate(X) & there_is_a_waterGlass & left_of(X,Y) ) => ( knife(Y))).
penalty(1240, 1) <- ~( !X : ( plate(X) ) => ( there_is_a_waterGlass | there_is_a_teaCup | lunch)).
penalty(1241, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) ) => ( there_is_a_waterGlass | bowl(Y))).
penalty(1242, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) & fork(Y) ) => ( there_is_a_wineGlass)).
penalty(1243, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) ) => ( there_is_a_wineGlass | knife(Y))).
penalty(1244, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) ) => ( there_is_a_wineGlass | bowl(Y))).
penalty(1245, 1) <- ~( !X, Y : ( plate(X) & there_is_a_teaCup & left_of(X,Y) ) => ( knife(Y))).
penalty(1246, 1) <- ~( !X, Y : ( plate(X) & there_is_a_teaCup & on(X,Y) ) => ( bowl(Y))).
penalty(1247, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) & breakfast ) => ( knife(Y))).
penalty(1248, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) & fork(Y) ) => ( lunch)).
penalty(1249, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) ) => ( lunch | knife(Y))).
penalty(1250, 1) <- ~( !X, Y : ( plate(X) & left_of(X,Y) ) => ( fork(Y) | knife(Y))).
penalty(1251, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) & breakfast ) => ( bowl(Y))).
penalty(1252, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) ) => ( lunch | bowl(Y))).
penalty(1253, 1) <- ~( !X, Y : ( plate(X) & on(X,Y) ) => ( bowl(Y) | soupPlate(Y))).
penalty(1254, 1) <- ~( !X : ( bowl(X) & there_is_a_fork ) => ( there_is_a_wineGlass | breakfast)).
penalty(1255, 1) <- ~( !X : ( bowl(X) & there_is_a_fork & lunch ) => ( there_is_a_wineGlass)).
penalty(1256, 1) <- ~( !X, Y : ( bowl(X) & there_is_a_fork & left_of(X,Y) ) => ( knife(Y))).
penalty(1257, 1) <- ~( !X, Y : ( bowl(X) & there_is_a_fork & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(1258, 1) <- ~( !X : ( bowl(X) & there_is_a_knife ) => ( there_is_a_wineGlass | breakfast)).
penalty(1259, 1) <- ~( !X : ( bowl(X) & there_is_a_knife & lunch ) => ( there_is_a_wineGlass)).
penalty(1260, 1) <- ~( !X, Y : ( bowl(X) & there_is_a_knife & left_of(X,Y) ) => ( knife(Y))).
penalty(1261, 1) <- ~( !X, Y : ( bowl(X) & there_is_a_knife & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(1262, 1) <- ~( !X, Y : ( bowl(X) & left_of(X,Y) ) => ( there_is_a_knife | spoon(Y))).
penalty(1263, 1) <- ~( !X, Y : ( bowl(X) & there_is_a_smallKnife & left_of(X,Y) ) => ( knife(Y))).
penalty(1264, 1) <- ~( !X, Y : ( bowl(X) & there_is_a_smallKnife & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(1265, 1) <- ~( !X : ( bowl(X) & there_is_a_plate ) => ( there_is_a_wineGlass | breakfast)).
penalty(1266, 1) <- ~( !X : ( bowl(X) & there_is_a_plate & lunch ) => ( there_is_a_wineGlass)).
penalty(1267, 1) <- ~( !X, Y : ( bowl(X) & there_is_a_plate & left_of(X,Y) ) => ( knife(Y))).
penalty(1268, 1) <- ~( !X, Y : ( bowl(X) & there_is_a_plate & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(1269, 1) <- ~( !X, Y : ( bowl(X) & left_of(X,Y) ) => ( there_is_a_plate | spoon(Y))).
penalty(1270, 1) <- ~( !X, Y : ( bowl(X) & there_is_a_saucer & left_of(X,Y) ) => ( knife(Y))).
penalty(1271, 1) <- ~( !X, Y : ( bowl(X) & there_is_a_saucer & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(1272, 1) <- ~( !X, Y : ( bowl(X) & there_is_a_wineGlass & left_of(X,Y) ) => ( knife(Y))).
penalty(1273, 1) <- ~( !X, Y : ( bowl(X) & there_is_a_wineGlass & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(1274, 1) <- ~( !X, Y : ( bowl(X) & left_of(X,Y) ) => ( knife(Y) | spoon(Y))).
penalty(1275, 1) <- ~( !X : ( saucer(X) & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1276, 1) <- ~( !X : ( saucer(X) & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1277, 1) <- ~( !X : ( saucer(X) & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1278, 1) <- ~( !X : ( saucer(X) & there_is_a_spoon ) => ( there_is_a_bowl | lunch)).
penalty(1279, 1) <- ~( !X, Y : ( soupPlate(X) & there_is_a_fork & left_of(X,Y) ) => ( knife(Y))).
penalty(1280, 1) <- ~( !X, Y : ( soupPlate(X) & there_is_a_fork & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(1281, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & knife(Y) ) => ( there_is_a_fork)).
penalty(1282, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) ) => ( there_is_a_fork | spoon(Y))).
penalty(1283, 1) <- ~( !X, Y : ( soupPlate(X) & there_is_a_knife & left_of(X,Y) ) => ( knife(Y))).
penalty(1284, 1) <- ~( !X, Y : ( soupPlate(X) & there_is_a_knife & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(1285, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) ) => ( there_is_a_knife | spoon(Y))).
penalty(1286, 1) <- ~( !X, Y : ( soupPlate(X) & there_is_a_smallKnife & left_of(X,Y) ) => ( knife(Y))).
penalty(1287, 1) <- ~( !X, Y : ( soupPlate(X) & there_is_a_smallKnife & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(1288, 1) <- ~( !X, Y : ( soupPlate(X) & there_is_a_plate & left_of(X,Y) ) => ( knife(Y))).
penalty(1289, 1) <- ~( !X, Y : ( soupPlate(X) & there_is_a_plate & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(1290, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) ) => ( there_is_a_plate | spoon(Y))).
penalty(1291, 1) <- ~( !X, Y : ( soupPlate(X) & there_is_a_saucer & left_of(X,Y) ) => ( knife(Y))).
penalty(1292, 1) <- ~( !X, Y : ( soupPlate(X) & there_is_a_saucer & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(1293, 1) <- ~( !X, Y : ( soupPlate(X) & there_is_a_waterGlass & left_of(X,Y) ) => ( knife(Y))).
penalty(1294, 1) <- ~( !X, Y : ( soupPlate(X) & there_is_a_waterGlass & left_of(X,Y) & spoon(Y) ) => ( false)).
penalty(1295, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & knife(Y) ) => ( there_is_a_waterGlass)).
penalty(1296, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) ) => ( there_is_a_waterGlass | spoon(Y))).
penalty(1297, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & lunch ) => ( knife(Y))).
penalty(1298, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & lunch & spoon(Y) ) => ( false)).
penalty(1299, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & knife(Y) ) => ( lunch)).
penalty(1300, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) ) => ( lunch | spoon(Y))).
penalty(1301, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & dinner ) => ( spoon(Y))).
penalty(1302, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) ) => ( dinner | knife(Y))).
penalty(1303, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) & spoon(Y) ) => ( dinner)).
penalty(1304, 1) <- ~( !X, Y : ( soupPlate(X) & left_of(X,Y) ) => ( knife(Y) | spoon(Y))).
penalty(1305, 1) <- ~( !X : ( waterGlass(X) & there_is_a_fork ) => ( there_is_a_knife | lunch)).
penalty(1306, 1) <- ~( !X : ( waterGlass(X) & there_is_a_fork ) => ( there_is_a_plate | lunch)).
penalty(1307, 1) <- ~( !X : ( waterGlass(X) ) => ( there_is_a_fork | there_is_a_knife | there_is_a_bowl)).
penalty(1308, 1) <- ~( !X : ( waterGlass(X) & there_is_a_spoon ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1309, 1) <- ~( !X : ( waterGlass(X) & there_is_a_teaCup ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(1310, 1) <- ~( !X : ( waterGlass(X) & there_is_a_teaCup ) => ( there_is_a_fork | there_is_a_smallKnife)).
penalty(1311, 1) <- ~( !X : ( waterGlass(X) ) => ( there_is_a_fork | there_is_a_plate | there_is_a_bowl)).
penalty(1312, 1) <- ~( !X : ( waterGlass(X) & there_is_a_teaCup ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1313, 1) <- ~( !X : ( waterGlass(X) ) => ( there_is_a_fork | there_is_a_bowl | breakfast)).
penalty(1314, 1) <- ~( !X : ( waterGlass(X) & there_is_a_teaCup ) => ( there_is_a_fork | there_is_a_saucer)).
penalty(1315, 1) <- ~( !X : ( waterGlass(X) ) => ( there_is_a_knife | there_is_a_bowl | there_is_a_pastaPlate)).
penalty(1316, 1) <- ~( !X : ( waterGlass(X) ) => ( there_is_a_knife | there_is_a_bowl | lunch)).
penalty(1317, 1) <- ~( !X : ( waterGlass(X) & there_is_a_spoon ) => ( there_is_a_bowl | lunch)).
penalty(1318, 1) <- ~( !X, Y : ( waterGlass(X) & there_is_a_smallKnife & there_is_a_bowl & left_of(X,Y) ) => ( false)).
penalty(1319, 1) <- ~( !X, Y : ( waterGlass(X) & there_is_a_smallKnife & left_of(X,Y) ) => ( there_is_a_soupPlate)).
penalty(1320, 1) <- ~( !X : ( waterGlass(X) ) => ( there_is_a_plate | there_is_a_bowl | there_is_a_pastaPlate)).
penalty(1321, 1) <- ~( !X : ( waterGlass(X) ) => ( there_is_a_plate | there_is_a_bowl | lunch)).
penalty(1322, 1) <- ~( !X, Y : ( waterGlass(X) & there_is_a_bowl & there_is_a_saucer & left_of(X,Y) ) => ( false)).
penalty(1323, 1) <- ~( !X : ( waterGlass(X) & there_is_a_wineGlass ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1324, 1) <- ~( !X, Y : ( waterGlass(X) & left_of(X,Y) ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1325, 1) <- ~( !X, Y : ( waterGlass(X) & there_is_a_saucer & left_of(X,Y) ) => ( there_is_a_soupPlate)).
penalty(1326, 1) <- ~( !X : ( wineGlass(X) & there_is_a_fork ) => ( there_is_a_pastaPlate | lunch)).
penalty(1327, 1) <- ~( !X : ( wineGlass(X) & there_is_a_knife & there_is_a_spoon ) => ( there_is_a_waterGlass)).
penalty(1328, 1) <- ~( !X : ( wineGlass(X) ) => ( there_is_a_knife | there_is_a_pastaPlate | there_is_a_soupPlate)).
penalty(1329, 1) <- ~( !X : ( wineGlass(X) & there_is_a_spoon & there_is_a_plate ) => ( there_is_a_waterGlass)).
penalty(1330, 1) <- ~( !X : ( wineGlass(X) & there_is_a_spoon ) => ( there_is_a_waterGlass | dinner)).
penalty(1331, 1) <- ~( !X : ( wineGlass(X) ) => ( there_is_a_plate | there_is_a_pastaPlate | there_is_a_soupPlate)).
penalty(1332, 1) <- ~( !X : ( wineGlass(X) & there_is_a_waterGlass ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1333, 1) <- ~( !X : ( wineGlass(X) ) => ( there_is_a_pastaPlate | there_is_a_soupPlate | lunch)).
penalty(1334, 1) <- ~( !X : ( teaCup(X) & there_is_a_waterGlass ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(1335, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(1336, 1) <- ~( !X : ( teaCup(X) & there_is_a_waterGlass ) => ( there_is_a_fork | there_is_a_smallKnife)).
penalty(1337, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) ) => ( there_is_a_fork | there_is_a_smallKnife)).
penalty(1338, 1) <- ~( !X : ( teaCup(X) & there_is_a_waterGlass ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1339, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1340, 1) <- ~( !X : ( teaCup(X) & there_is_a_waterGlass ) => ( there_is_a_fork | there_is_a_saucer)).
penalty(1341, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) ) => ( there_is_a_fork | there_is_a_saucer)).
penalty(1342, 1) <- ~( !X : ( teaCup(X) & there_is_a_waterGlass ) => ( there_is_a_spoon | there_is_a_smallKnife)).
penalty(1343, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) ) => ( there_is_a_spoon | there_is_a_smallKnife)).
penalty(1344, 1) <- ~( !X : ( teaCup(X) & there_is_a_waterGlass ) => ( there_is_a_spoon | there_is_a_saucer)).
penalty(1345, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) ) => ( there_is_a_spoon | there_is_a_saucer)).
penalty(1346, 1) <- ~( !X : ( teaCup(X) & there_is_a_waterGlass ) => ( there_is_a_smallKnife | there_is_a_bowl)).
penalty(1347, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) ) => ( there_is_a_smallKnife | there_is_a_bowl)).
penalty(1348, 1) <- ~( !X : ( teaCup(X) & there_is_a_waterGlass ) => ( there_is_a_bowl | there_is_a_saucer)).
penalty(1349, 1) <- ~( !X, Y : ( teaCup(X) & left_of(X,Y) ) => ( there_is_a_bowl | there_is_a_saucer)).
penalty(1350, 1) <- ~(( there_is_a_fork & there_is_a_waterGlass ) => ( there_is_a_knife | lunch)).
penalty(1351, 1) <- ~(( there_is_a_fork ) => ( there_is_a_knife | there_is_a_waterGlass | there_is_a_wineGlass)).
penalty(1352, 1) <- ~(( there_is_a_fork ) => ( there_is_a_knife | there_is_a_waterGlass | dinner)).
penalty(1353, 1) <- ~(( there_is_a_fork ) => ( there_is_a_knife | there_is_a_wineGlass | lunch)).
penalty(1354, 1) <- ~( !X, Y : ( there_is_a_fork & left_of(X,Y) & spoon(Y) ) => ( there_is_a_knife)).
penalty(1355, 1) <- ~( !X, Y : ( there_is_a_fork & above_of(X,Y) ) => ( there_is_a_knife | fork(X))).
penalty(1356, 1) <- ~( !X : ( there_is_a_fork & food_container(X) ) => ( there_is_a_knife | pastaPlate(X))).
penalty(1357, 1) <- ~(( there_is_a_fork ) => ( there_is_a_knife | lunch | dinner)).
penalty(1358, 1) <- ~(( there_is_a_fork & there_is_a_waterGlass ) => ( there_is_a_plate | lunch)).
penalty(1359, 1) <- ~(( there_is_a_fork ) => ( there_is_a_plate | there_is_a_waterGlass | there_is_a_wineGlass)).
penalty(1360, 1) <- ~(( there_is_a_fork ) => ( there_is_a_plate | there_is_a_waterGlass | dinner)).
penalty(1361, 1) <- ~(( there_is_a_fork ) => ( there_is_a_plate | there_is_a_wineGlass | lunch)).
penalty(1362, 1) <- ~( !X, Y : ( there_is_a_fork & left_of(X,Y) & spoon(Y) ) => ( there_is_a_plate)).
penalty(1363, 1) <- ~( !X, Y : ( there_is_a_fork & above_of(X,Y) ) => ( there_is_a_plate | fork(X))).
penalty(1364, 1) <- ~( !X : ( there_is_a_fork & food_container(X) ) => ( there_is_a_plate | pastaPlate(X))).
penalty(1365, 1) <- ~(( there_is_a_fork ) => ( there_is_a_plate | lunch | dinner)).
penalty(1366, 1) <- ~(( there_is_a_fork & there_is_a_bowl ) => ( there_is_a_wineGlass | breakfast)).
penalty(1367, 1) <- ~(( there_is_a_fork & there_is_a_bowl & lunch ) => ( there_is_a_wineGlass)).
penalty(1368, 1) <- ~(( there_is_a_fork & there_is_a_wineGlass ) => ( there_is_a_pastaPlate | lunch)).
penalty(1369, 1) <- ~(( there_is_a_fork ) => ( there_is_a_pastaPlate | breakfast | lunch)).
penalty(1370, 1) <- ~( !X, Y : ( there_is_a_fork & left_of(X,Y) & food_container(X) & spoon(Y) ) => ( false)).
penalty(1371, 1) <- ~( !X, Y : ( there_is_a_fork & left_of(X,Y) & dinner & spoon(Y) ) => ( false)).
penalty(1372, 1) <- ~( !X, Y : ( there_is_a_fork & left_of(X,Y) & spoon(Y) ) => ( knife(X))).
penalty(1373, 1) <- ~( !X, Y : ( there_is_a_fork & left_of(X,Y) & spoon(Y) ) => ( food_utensil(X))).
penalty(1374, 1) <- ~( !X, Y : ( there_is_a_fork & above_of(X,Y) & dinner ) => ( fork(X))).
penalty(1375, 1) <- ~( !X, Y : ( there_is_a_fork & above_of(X,Y) & teaCup(Y) ) => ( knife(X))).
penalty(1376, 1) <- ~( !X : ( there_is_a_fork & food_container(X) & dinner ) => ( pastaPlate(X))).
penalty(1377, 1) <- ~(( there_is_a_knife & there_is_a_spoon ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1378, 1) <- ~(( there_is_a_knife ) => ( there_is_a_fork | there_is_a_waterGlass | there_is_a_teaCup)).
penalty(1379, 1) <- ~(( there_is_a_fork | there_is_a_knife | there_is_a_bowl | there_is_a_soupPlate)).
penalty(1380, 1) <- ~(( there_is_a_waterGlass ) => ( there_is_a_fork | there_is_a_knife | there_is_a_bowl)).
penalty(1381, 1) <- ~(( there_is_a_fork | there_is_a_knife | there_is_a_bowl | there_is_a_wineGlass)).
penalty(1382, 1) <- ~(( there_is_a_fork | there_is_a_knife | there_is_a_bowl | dinner)).
penalty(1383, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_fork | there_is_a_knife)).
penalty(1384, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_knife | spoon(Y))).
penalty(1385, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_knife | food_utensil(Y))).
penalty(1386, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_knife | food_container(X))).
penalty(1387, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_fork | there_is_a_knife | spoon(X))).
penalty(1388, 1) <- ~( !X : ( food_utensil(X) ) => ( there_is_a_fork | there_is_a_knife | spoon(X))).
penalty(1389, 1) <- ~(( there_is_a_spoon & there_is_a_plate ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1390, 1) <- ~(( there_is_a_spoon ) => ( there_is_a_fork | there_is_a_bowl | there_is_a_soupPlate)).
penalty(1391, 1) <- ~(( there_is_a_spoon & there_is_a_waterGlass ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1392, 1) <- ~(( there_is_a_spoon ) => ( there_is_a_fork | there_is_a_bowl | there_is_a_wineGlass)).
penalty(1393, 1) <- ~(( there_is_a_spoon ) => ( there_is_a_fork | there_is_a_bowl | dinner)).
penalty(1394, 1) <- ~(( there_is_a_waterGlass & there_is_a_teaCup ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(1395, 1) <- ~(( there_is_a_fork | there_is_a_spoon | there_is_a_waterGlass | there_is_a_teaCup)).
penalty(1396, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(1397, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(1398, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_spoon | knife(Y))).
penalty(1399, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_spoon | plate(X))).
penalty(1400, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(1401, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_spoon | food_utensil(Y))).
penalty(1402, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_spoon | food_container(X))).
penalty(1403, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_fork | there_is_a_spoon)).
penalty(1404, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_fork | there_is_a_spoon | knife(X))).
penalty(1405, 1) <- ~( !X : ( food_utensil(X) ) => ( there_is_a_fork | there_is_a_spoon | knife(X))).
penalty(1406, 1) <- ~( !X : ( food_container(X) ) => ( there_is_a_fork | there_is_a_spoon | plate(X))).
penalty(1407, 1) <- ~(( there_is_a_waterGlass & there_is_a_teaCup ) => ( there_is_a_fork | there_is_a_smallKnife)).
penalty(1408, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_fork | there_is_a_smallKnife)).
penalty(1409, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( there_is_a_fork | there_is_a_smallKnife)).
penalty(1410, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_smallKnife | food_utensil(Y))).
penalty(1411, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_fork | there_is_a_smallKnife)).
penalty(1412, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | there_is_a_smallKnife | bowl(Y))).
penalty(1413, 1) <- ~(( there_is_a_plate ) => ( there_is_a_fork | there_is_a_waterGlass | there_is_a_teaCup)).
penalty(1414, 1) <- ~(( there_is_a_fork | there_is_a_plate | there_is_a_bowl | there_is_a_soupPlate)).
penalty(1415, 1) <- ~(( there_is_a_waterGlass ) => ( there_is_a_fork | there_is_a_plate | there_is_a_bowl)).
penalty(1416, 1) <- ~(( there_is_a_fork | there_is_a_plate | there_is_a_bowl | there_is_a_wineGlass)).
penalty(1417, 1) <- ~(( there_is_a_fork | there_is_a_plate | there_is_a_bowl | dinner)).
penalty(1418, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_fork | there_is_a_plate)).
penalty(1419, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_plate | spoon(Y))).
penalty(1420, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_plate | food_utensil(Y))).
penalty(1421, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_plate | food_container(X))).
penalty(1422, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_fork | there_is_a_plate | spoon(X))).
penalty(1423, 1) <- ~( !X : ( food_utensil(X) ) => ( there_is_a_fork | there_is_a_plate | spoon(X))).
penalty(1424, 1) <- ~(( there_is_a_fork | there_is_a_bowl | there_is_a_soupPlate | breakfast)).
penalty(1425, 1) <- ~(( there_is_a_waterGlass & there_is_a_teaCup ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1426, 1) <- ~(( there_is_a_waterGlass ) => ( there_is_a_fork | there_is_a_bowl | breakfast)).
penalty(1427, 1) <- ~(( there_is_a_fork | there_is_a_bowl | there_is_a_wineGlass | breakfast)).
penalty(1428, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1429, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1430, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1431, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_bowl | food_utensil(Y))).
penalty(1432, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_bowl | food_container(X))).
penalty(1433, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_fork | there_is_a_bowl)).
penalty(1434, 1) <- ~(( there_is_a_fork | there_is_a_bowl | breakfast | dinner)).
penalty(1435, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & food_utensil(X) ) => ( there_is_a_fork)).
penalty(1436, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & drink_container(X) ) => ( there_is_a_fork)).
penalty(1437, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_fork | spoon(Y))).
penalty(1438, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_fork | soupPlate(X))).
penalty(1439, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_fork | food_utensil(Y))).
penalty(1440, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_fork | food_container(X))).
penalty(1441, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & drink_container(Y) ) => ( there_is_a_fork)).
penalty(1442, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) ) => ( there_is_a_fork | spoon(X))).
penalty(1443, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) ) => ( there_is_a_fork | wineGlass(Y))).
penalty(1444, 1) <- ~( !X : ( there_is_a_soupPlate & food_utensil(X) ) => ( there_is_a_fork | spoon(X))).
penalty(1445, 1) <- ~( !X : ( there_is_a_soupPlate & food_container(X) ) => ( there_is_a_fork | soupPlate(X))).
penalty(1446, 1) <- ~( !X : ( there_is_a_soupPlate & drink_container(X) ) => ( there_is_a_fork | wineGlass(X))).
penalty(1447, 1) <- ~(( there_is_a_fork | there_is_a_soupPlate | there_is_a_waterGlass | there_is_a_teaCup)).
penalty(1448, 1) <- ~(( there_is_a_fork | there_is_a_soupPlate | there_is_a_waterGlass | breakfast)).
penalty(1449, 1) <- ~(( there_is_a_fork | there_is_a_soupPlate | breakfast | lunch)).
penalty(1450, 1) <- ~(( there_is_a_waterGlass & there_is_a_teaCup ) => ( there_is_a_fork | there_is_a_saucer)).
penalty(1451, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_fork | there_is_a_saucer)).
penalty(1452, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( there_is_a_fork | there_is_a_saucer)).
penalty(1453, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_saucer | food_utensil(Y))).
penalty(1454, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_fork | there_is_a_saucer)).
penalty(1455, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | there_is_a_saucer | bowl(Y))).
penalty(1456, 1) <- ~(( there_is_a_fork | there_is_a_waterGlass | there_is_a_teaCup | dinner)).
penalty(1457, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_waterGlass | food_utensil(Y))).
penalty(1458, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | there_is_a_waterGlass | plate(X))).
penalty(1459, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | there_is_a_waterGlass | bowl(Y))).
penalty(1460, 1) <- ~( !X, Y : ( on(X,Y) & food_utensil(Y) ) => ( there_is_a_fork | there_is_a_waterGlass)).
penalty(1461, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | there_is_a_waterGlass | food_container(Y))).
penalty(1462, 1) <- ~(( there_is_a_fork | there_is_a_waterGlass | breakfast | dinner)).
penalty(1463, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) & food_utensil(X) ) => ( there_is_a_fork)).
penalty(1464, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) & drink_container(X) ) => ( there_is_a_fork)).
penalty(1465, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) ) => ( there_is_a_fork | spoon(Y))).
penalty(1466, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) ) => ( there_is_a_fork | soupPlate(X))).
penalty(1467, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) ) => ( there_is_a_fork | food_utensil(Y))).
penalty(1468, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) ) => ( there_is_a_fork | food_container(X))).
penalty(1469, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) & drink_container(Y) ) => ( there_is_a_fork)).
penalty(1470, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) ) => ( there_is_a_fork | spoon(X))).
penalty(1471, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) ) => ( there_is_a_fork | wineGlass(Y))).
penalty(1472, 1) <- ~( !X : ( there_is_a_wineGlass & food_utensil(X) ) => ( there_is_a_fork | spoon(X))).
penalty(1473, 1) <- ~( !X : ( there_is_a_wineGlass & food_container(X) ) => ( there_is_a_fork | soupPlate(X))).
penalty(1474, 1) <- ~( !X : ( there_is_a_wineGlass & drink_container(X) ) => ( there_is_a_fork | wineGlass(X))).
penalty(1475, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_fork | there_is_a_teaCup)).
penalty(1476, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | there_is_a_teaCup | food_utensil(Y))).
penalty(1477, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_fork | there_is_a_teaCup)).
penalty(1478, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | there_is_a_teaCup | plate(X))).
penalty(1479, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | there_is_a_teaCup | bowl(Y))).
penalty(1480, 1) <- ~( !X, Y : ( on(X,Y) & food_utensil(Y) ) => ( there_is_a_fork | there_is_a_teaCup)).
penalty(1481, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | there_is_a_teaCup | food_container(Y))).
penalty(1482, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_fork | breakfast)).
penalty(1483, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & lunch ) => ( there_is_a_fork)).
penalty(1484, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & dinner ) => ( there_is_a_fork)).
penalty(1485, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & knife(Y) ) => ( there_is_a_fork)).
penalty(1486, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_fork | knife(X))).
penalty(1487, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_fork | spoon(Y))).
penalty(1488, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_fork | food_utensil(Y))).
penalty(1489, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_fork | breakfast)).
penalty(1490, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & lunch ) => ( there_is_a_fork)).
penalty(1491, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_fork | waterGlass(Y))).
penalty(1492, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_fork | teaCup(X))).
penalty(1493, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | breakfast | spoon(Y))).
penalty(1494, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | breakfast | food_utensil(Y))).
penalty(1495, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | breakfast | food_container(X))).
penalty(1496, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_fork | breakfast)).
penalty(1497, 1) <- ~( !X, Y : ( left_of(X,Y) & lunch ) => ( there_is_a_fork | spoon(Y))).
penalty(1498, 1) <- ~( !X, Y : ( left_of(X,Y) & lunch ) => ( there_is_a_fork | bowl(X))).
penalty(1499, 1) <- ~( !X, Y : ( left_of(X,Y) & lunch ) => ( there_is_a_fork | food_utensil(Y))).
penalty(1500, 1) <- ~( !X, Y : ( left_of(X,Y) & lunch ) => ( there_is_a_fork | food_container(X))).
penalty(1501, 1) <- ~( !X, Y : ( left_of(X,Y) & lunch & drink_container(Y) ) => ( there_is_a_fork)).
penalty(1502, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( there_is_a_fork | spoon(Y))).
penalty(1503, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( there_is_a_fork | soupPlate(X))).
penalty(1504, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( there_is_a_fork | food_utensil(Y))).
penalty(1505, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( there_is_a_fork | food_container(X))).
penalty(1506, 1) <- ~( !X, Y : ( left_of(X,Y) & knife(Y) ) => ( there_is_a_fork | food_container(X))).
penalty(1507, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | waterGlass(Y) | food_utensil(Y))).
penalty(1508, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_fork | waterGlass(Y))).
penalty(1509, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | teaCup(X) | food_utensil(Y))).
penalty(1510, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_fork | teaCup(X))).
penalty(1511, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | food_utensil(Y) | drink_container(X))).
penalty(1512, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_fork | food_utensil(Y) | drink_container(Y))).
penalty(1513, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_fork | breakfast | spoon(X))).
penalty(1514, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch ) => ( there_is_a_fork | spoon(X))).
penalty(1515, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch ) => ( there_is_a_fork | waterGlass(Y))).
penalty(1516, 1) <- ~( !X, Y : ( above_of(X,Y) & dinner ) => ( there_is_a_fork | spoon(X))).
penalty(1517, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_fork | knife(X) | spoon(X))).
penalty(1518, 1) <- ~( !X, Y : ( above_of(X,Y) & wineGlass(Y) ) => ( there_is_a_fork | spoon(X))).
penalty(1519, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | smallKnife(Y) | bowl(Y))).
penalty(1520, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | bowl(Y) | saucer(X))).
penalty(1521, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_fork | bowl(Y) | food_utensil(Y))).
penalty(1522, 1) <- ~( !X, Y : ( on(X,Y) & food_container(Y) ) => ( there_is_a_fork | bowl(Y))).
penalty(1523, 1) <- ~( !X : ( food_utensil(X) ) => ( there_is_a_fork | breakfast | spoon(X))).
penalty(1524, 1) <- ~( !X : ( food_utensil(X) & lunch ) => ( there_is_a_fork | spoon(X))).
penalty(1525, 1) <- ~( !X : ( food_utensil(X) & dinner ) => ( there_is_a_fork | spoon(X))).
penalty(1526, 1) <- ~( !X : ( food_container(X) & lunch ) => ( there_is_a_fork | bowl(X))).
penalty(1527, 1) <- ~( !X : ( food_container(X) & dinner ) => ( there_is_a_fork | soupPlate(X))).
penalty(1528, 1) <- ~( !X : ( drink_container(X) & lunch ) => ( there_is_a_fork | waterGlass(X))).
penalty(1529, 1) <- ~(( there_is_a_knife & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1530, 1) <- ~(( there_is_a_knife & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1531, 1) <- ~(( there_is_a_knife & there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1532, 1) <- ~(( there_is_a_knife & there_is_a_spoon ) => ( there_is_a_bowl | lunch)).
penalty(1533, 1) <- ~(( there_is_a_knife & there_is_a_spoon & there_is_a_wineGlass ) => ( there_is_a_waterGlass)).
penalty(1534, 1) <- ~(( there_is_a_knife & there_is_a_spoon ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(1535, 1) <- ~(( there_is_a_knife & there_is_a_spoon ) => ( there_is_a_waterGlass | breakfast)).
penalty(1536, 1) <- ~(( there_is_a_knife & there_is_a_spoon ) => ( there_is_a_wineGlass | breakfast)).
penalty(1537, 1) <- ~(( there_is_a_knife & there_is_a_spoon & lunch ) => ( there_is_a_wineGlass)).
penalty(1538, 1) <- ~( !X, Y : ( there_is_a_knife & there_is_a_spoon & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(1539, 1) <- ~(( there_is_a_knife & there_is_a_bowl ) => ( there_is_a_wineGlass | breakfast)).
penalty(1540, 1) <- ~(( there_is_a_knife & there_is_a_bowl & lunch ) => ( there_is_a_wineGlass)).
penalty(1541, 1) <- ~( !X, Y : ( there_is_a_knife & there_is_a_waterGlass & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(1542, 1) <- ~(( there_is_a_knife ) => ( there_is_a_waterGlass | there_is_a_teaCup | lunch)).
penalty(1543, 1) <- ~( !X, Y : ( there_is_a_knife & left_of(X,Y) & fork(Y) ) => ( there_is_a_wineGlass)).
penalty(1544, 1) <- ~( !X, Y : ( there_is_a_knife & left_of(X,Y) & food_container(X) & spoon(Y) ) => ( false)).
penalty(1545, 1) <- ~( !X, Y : ( there_is_a_knife & left_of(X,Y) & fork(Y) ) => ( lunch)).
penalty(1546, 1) <- ~( !X, Y : ( there_is_a_knife & left_of(X,Y) & fork(Y) ) => ( plate(X))).
penalty(1547, 1) <- ~( !X, Y : ( there_is_a_knife & left_of(X,Y) & food_container(Y) ) => ( fork(X))).
penalty(1548, 1) <- ~( !X, Y : ( there_is_a_knife & left_of(X,Y) & spoon(Y) ) => ( knife(X))).
penalty(1549, 1) <- ~( !X, Y : ( there_is_a_knife & left_of(X,Y) & spoon(Y) ) => ( food_utensil(X))).
penalty(1550, 1) <- ~( !X, Y : ( there_is_a_knife & above_of(X,Y) & teaCup(Y) ) => ( knife(X))).
penalty(1551, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) & food_utensil(X) ) => ( there_is_a_knife)).
penalty(1552, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) ) => ( there_is_a_knife | spoon(Y))).
penalty(1553, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) ) => ( there_is_a_knife | bowl(X))).
penalty(1554, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) ) => ( there_is_a_knife | food_utensil(Y))).
penalty(1555, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) & food_container(Y) ) => ( there_is_a_knife)).
penalty(1556, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) ) => ( there_is_a_knife | food_container(X))).
penalty(1557, 1) <- ~( !X, Y : ( there_is_a_bowl & above_of(X,Y) ) => ( there_is_a_knife | spoon(X))).
penalty(1558, 1) <- ~( !X : ( there_is_a_bowl & food_utensil(X) ) => ( there_is_a_knife | spoon(X))).
penalty(1559, 1) <- ~( !X : ( there_is_a_bowl & food_container(X) ) => ( there_is_a_knife | bowl(X))).
penalty(1560, 1) <- ~(( there_is_a_knife | there_is_a_bowl | there_is_a_pastaPlate | there_is_a_soupPlate)).
penalty(1561, 1) <- ~(( there_is_a_waterGlass ) => ( there_is_a_knife | there_is_a_bowl | there_is_a_pastaPlate)).
penalty(1562, 1) <- ~(( there_is_a_knife | there_is_a_bowl | there_is_a_pastaPlate | there_is_a_wineGlass)).
penalty(1563, 1) <- ~(( lunch ) => ( there_is_a_knife | there_is_a_bowl | there_is_a_pastaPlate)).
penalty(1564, 1) <- ~(( there_is_a_knife | there_is_a_bowl | there_is_a_pastaPlate | dinner)).
penalty(1565, 1) <- ~(( there_is_a_waterGlass ) => ( there_is_a_knife | there_is_a_bowl | lunch)).
penalty(1566, 1) <- ~(( there_is_a_knife | there_is_a_bowl | there_is_a_waterGlass | there_is_a_wineGlass)).
penalty(1567, 1) <- ~(( there_is_a_knife | there_is_a_bowl | there_is_a_waterGlass | dinner)).
penalty(1568, 1) <- ~(( there_is_a_knife | there_is_a_bowl | there_is_a_wineGlass | lunch)).
penalty(1569, 1) <- ~(( there_is_a_knife | there_is_a_bowl | lunch | dinner)).
penalty(1570, 1) <- ~(( there_is_a_wineGlass ) => ( there_is_a_knife | there_is_a_pastaPlate | there_is_a_soupPlate)).
penalty(1571, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_knife | there_is_a_pastaPlate)).
penalty(1572, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_knife | there_is_a_pastaPlate | spoon(Y))).
penalty(1573, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_knife | there_is_a_pastaPlate | food_utensil(Y))).
penalty(1574, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(Y) ) => ( there_is_a_knife | there_is_a_pastaPlate)).
penalty(1575, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_knife | there_is_a_pastaPlate | food_container(X))).
penalty(1576, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_knife | there_is_a_pastaPlate | spoon(X))).
penalty(1577, 1) <- ~( !X : ( food_utensil(X) ) => ( there_is_a_knife | there_is_a_pastaPlate | spoon(X))).
penalty(1578, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & food_utensil(X) ) => ( there_is_a_knife)).
penalty(1579, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_knife | spoon(Y))).
penalty(1580, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_knife | soupPlate(X))).
penalty(1581, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_knife | food_utensil(Y))).
penalty(1582, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & food_container(Y) ) => ( there_is_a_knife)).
penalty(1583, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_knife | food_container(X))).
penalty(1584, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) ) => ( there_is_a_knife | spoon(X))).
penalty(1585, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) ) => ( there_is_a_knife | wineGlass(Y))).
penalty(1586, 1) <- ~( !X : ( there_is_a_soupPlate & food_utensil(X) ) => ( there_is_a_knife | spoon(X))).
penalty(1587, 1) <- ~( !X : ( there_is_a_soupPlate & food_container(X) ) => ( there_is_a_knife | soupPlate(X))).
penalty(1588, 1) <- ~( !X : ( there_is_a_soupPlate & drink_container(X) ) => ( there_is_a_knife | wineGlass(X))).
penalty(1589, 1) <- ~( !X, Y : ( there_is_a_waterGlass & above_of(X,Y) ) => ( there_is_a_knife | waterGlass(Y))).
penalty(1590, 1) <- ~( !X : ( there_is_a_waterGlass & drink_container(X) ) => ( there_is_a_knife | waterGlass(X))).
penalty(1591, 1) <- ~(( there_is_a_knife | there_is_a_waterGlass | there_is_a_teaCup | dinner)).
penalty(1592, 1) <- ~(( there_is_a_knife | there_is_a_waterGlass | breakfast | dinner)).
penalty(1593, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) ) => ( there_is_a_knife | wineGlass(Y))).
penalty(1594, 1) <- ~( !X : ( there_is_a_wineGlass & drink_container(X) ) => ( there_is_a_knife | wineGlass(X))).
penalty(1595, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) & food_utensil(X) ) => ( there_is_a_knife)).
penalty(1596, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) ) => ( there_is_a_knife | spoon(Y))).
penalty(1597, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) ) => ( there_is_a_knife | bowl(X))).
penalty(1598, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) ) => ( there_is_a_knife | food_utensil(Y))).
penalty(1599, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) & food_container(Y) ) => ( there_is_a_knife)).
penalty(1600, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) ) => ( there_is_a_knife | food_container(X))).
penalty(1601, 1) <- ~( !X, Y : ( there_is_a_teaCup & above_of(X,Y) ) => ( there_is_a_knife | spoon(X))).
penalty(1602, 1) <- ~( !X, Y : ( there_is_a_teaCup & above_of(X,Y) ) => ( there_is_a_knife | teaCup(Y))).
penalty(1603, 1) <- ~( !X : ( there_is_a_teaCup & food_utensil(X) ) => ( there_is_a_knife | spoon(X))).
penalty(1604, 1) <- ~( !X : ( there_is_a_teaCup & food_container(X) ) => ( there_is_a_knife | bowl(X))).
penalty(1605, 1) <- ~( !X : ( there_is_a_teaCup & drink_container(X) ) => ( there_is_a_knife | teaCup(X))).
penalty(1606, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & breakfast ) => ( there_is_a_knife)).
penalty(1607, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & spoon(Y) ) => ( there_is_a_knife)).
penalty(1608, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_knife | spoon(X))).
penalty(1609, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_knife | pastaPlate(Y))).
penalty(1610, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & food_utensil(Y) ) => ( there_is_a_knife)).
penalty(1611, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_knife | food_container(Y))).
penalty(1612, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast ) => ( there_is_a_knife | spoon(Y))).
penalty(1613, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast ) => ( there_is_a_knife | bowl(X))).
penalty(1614, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast ) => ( there_is_a_knife | food_utensil(Y))).
penalty(1615, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast & food_container(Y) ) => ( there_is_a_knife)).
penalty(1616, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast ) => ( there_is_a_knife | food_container(X))).
penalty(1617, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( there_is_a_knife | pastaPlate(X))).
penalty(1618, 1) <- ~( !X, Y : ( left_of(X,Y) & spoon(Y) ) => ( there_is_a_knife | food_container(X))).
penalty(1619, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_knife | spoon(X) | food_utensil(Y))).
penalty(1620, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(Y) ) => ( there_is_a_knife | spoon(X))).
penalty(1621, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_knife | spoon(X) | food_container(X))).
penalty(1622, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_knife | pastaPlate(Y) | food_utensil(Y))).
penalty(1623, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(Y) ) => ( there_is_a_knife | pastaPlate(Y))).
penalty(1624, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_knife | pastaPlate(Y) | food_container(X))).
penalty(1625, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(Y) ) => ( there_is_a_knife | food_container(X))).
penalty(1626, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_knife | food_utensil(X) | food_utensil(Y))).
penalty(1627, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_knife | food_utensil(X) | food_container(X))).
penalty(1628, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_knife | food_utensil(Y) | food_container(Y))).
penalty(1629, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_knife | food_container(X) | food_container(Y))).
penalty(1630, 1) <- ~( !X, Y : ( above_of(X,Y) & breakfast ) => ( there_is_a_knife | spoon(X))).
penalty(1631, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch ) => ( there_is_a_knife | waterGlass(Y))).
penalty(1632, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_knife | fork(X) | spoon(X))).
penalty(1633, 1) <- ~( !X, Y : ( above_of(X,Y) & teaCup(Y) ) => ( there_is_a_knife | spoon(X))).
penalty(1634, 1) <- ~( !X : ( food_utensil(X) & breakfast ) => ( there_is_a_knife | spoon(X))).
penalty(1635, 1) <- ~( !X : ( food_utensil(X) ) => ( there_is_a_knife | fork(X) | spoon(X))).
penalty(1636, 1) <- ~( !X : ( food_container(X) & breakfast ) => ( there_is_a_knife | bowl(X))).
penalty(1637, 1) <- ~( !X : ( drink_container(X) & lunch ) => ( there_is_a_knife | waterGlass(X))).
penalty(1638, 1) <- ~(( there_is_a_spoon & there_is_a_smallKnife ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1639, 1) <- ~(( there_is_a_spoon & there_is_a_smallKnife ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1640, 1) <- ~(( there_is_a_spoon & there_is_a_smallKnife ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1641, 1) <- ~(( there_is_a_spoon & there_is_a_smallKnife ) => ( there_is_a_bowl | lunch)).
penalty(1642, 1) <- ~(( there_is_a_spoon & there_is_a_plate ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1643, 1) <- ~(( there_is_a_spoon & there_is_a_plate ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1644, 1) <- ~(( there_is_a_spoon & there_is_a_plate ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1645, 1) <- ~(( there_is_a_spoon & there_is_a_plate ) => ( there_is_a_bowl | lunch)).
penalty(1646, 1) <- ~(( there_is_a_spoon & there_is_a_plate & there_is_a_wineGlass ) => ( there_is_a_waterGlass)).
penalty(1647, 1) <- ~(( there_is_a_spoon & there_is_a_plate ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(1648, 1) <- ~(( there_is_a_spoon & there_is_a_plate ) => ( there_is_a_waterGlass | breakfast)).
penalty(1649, 1) <- ~(( there_is_a_spoon & there_is_a_plate ) => ( there_is_a_wineGlass | breakfast)).
penalty(1650, 1) <- ~(( there_is_a_spoon & there_is_a_plate & lunch ) => ( there_is_a_wineGlass)).
penalty(1651, 1) <- ~( !X, Y : ( there_is_a_spoon & there_is_a_plate & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(1652, 1) <- ~(( there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_pastaPlate | there_is_a_soupPlate)).
penalty(1653, 1) <- ~(( there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_pastaPlate | there_is_a_wineGlass)).
penalty(1654, 1) <- ~(( there_is_a_spoon & there_is_a_saucer ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1655, 1) <- ~( !X, Y : ( there_is_a_spoon & on(X,Y) ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1656, 1) <- ~(( there_is_a_spoon & there_is_a_saucer ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1657, 1) <- ~(( there_is_a_spoon & there_is_a_saucer ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1658, 1) <- ~(( there_is_a_spoon & there_is_a_saucer ) => ( there_is_a_bowl | lunch)).
penalty(1659, 1) <- ~(( there_is_a_spoon & there_is_a_waterGlass ) => ( there_is_a_bowl | lunch)).
penalty(1660, 1) <- ~(( there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_waterGlass | there_is_a_wineGlass)).
penalty(1661, 1) <- ~( !X, Y : ( there_is_a_spoon & on(X,Y) ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1662, 1) <- ~(( there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_waterGlass | dinner)).
penalty(1663, 1) <- ~( !X, Y : ( there_is_a_spoon & on(X,Y) ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1664, 1) <- ~(( there_is_a_spoon ) => ( there_is_a_bowl | there_is_a_wineGlass | lunch)).
penalty(1665, 1) <- ~( !X, Y : ( there_is_a_spoon & left_of(X,Y) & waterGlass(Y) ) => ( there_is_a_bowl)).
penalty(1666, 1) <- ~( !X, Y : ( there_is_a_spoon & on(X,Y) ) => ( there_is_a_bowl | lunch)).
penalty(1667, 1) <- ~(( there_is_a_spoon ) => ( there_is_a_bowl | lunch | dinner)).
penalty(1668, 1) <- ~( !X, Y : ( there_is_a_spoon & left_of(X,Y) & fork(Y) ) => ( there_is_a_pastaPlate)).
penalty(1669, 1) <- ~(( there_is_a_spoon & there_is_a_wineGlass ) => ( there_is_a_waterGlass | dinner)).
penalty(1670, 1) <- ~(( there_is_a_spoon ) => ( there_is_a_waterGlass | there_is_a_teaCup | dinner)).
penalty(1671, 1) <- ~(( there_is_a_spoon ) => ( there_is_a_waterGlass | breakfast | dinner)).
penalty(1672, 1) <- ~( !X, Y : ( there_is_a_spoon & left_of(X,Y) & food_utensil(X) & knife(Y) ) => ( false)).
penalty(1673, 1) <- ~( !X, Y : ( there_is_a_spoon & left_of(X,Y) & fork(Y) ) => ( pastaPlate(X))).
penalty(1674, 1) <- ~( !X, Y : ( there_is_a_spoon & left_of(X,Y) & knife(Y) ) => ( food_container(X))).
penalty(1675, 1) <- ~(( there_is_a_waterGlass & there_is_a_teaCup ) => ( there_is_a_spoon | there_is_a_smallKnife)).
penalty(1676, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_spoon | there_is_a_smallKnife)).
penalty(1677, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( there_is_a_spoon | there_is_a_smallKnife)).
penalty(1678, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_spoon | there_is_a_smallKnife)).
penalty(1679, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_spoon | there_is_a_smallKnife | knife(X))).
penalty(1680, 1) <- ~( !X : ( food_container(X) ) => ( there_is_a_spoon | there_is_a_smallKnife | plate(X))).
penalty(1681, 1) <- ~(( there_is_a_waterGlass & there_is_a_teaCup ) => ( there_is_a_spoon | there_is_a_saucer)).
penalty(1682, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_spoon | there_is_a_saucer)).
penalty(1683, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( there_is_a_spoon | there_is_a_saucer)).
penalty(1684, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_spoon | there_is_a_saucer)).
penalty(1685, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_spoon | there_is_a_saucer | knife(X))).
penalty(1686, 1) <- ~( !X : ( food_container(X) ) => ( there_is_a_spoon | there_is_a_saucer | plate(X))).
penalty(1687, 1) <- ~( !X, Y : ( there_is_a_waterGlass & left_of(X,Y) & fork(Y) ) => ( there_is_a_spoon)).
penalty(1688, 1) <- ~(( there_is_a_spoon | there_is_a_waterGlass | there_is_a_teaCup | lunch)).
penalty(1689, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) & drink_container(X) ) => ( there_is_a_spoon)).
penalty(1690, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) & drink_container(Y) ) => ( there_is_a_spoon)).
penalty(1691, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) ) => ( there_is_a_spoon | knife(X))).
penalty(1692, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) ) => ( there_is_a_spoon | wineGlass(Y))).
penalty(1693, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_spoon)).
penalty(1694, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) & food_container(Y) ) => ( there_is_a_spoon)).
penalty(1695, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) ) => ( there_is_a_spoon | drink_container(Y))).
penalty(1696, 1) <- ~( !X : ( there_is_a_wineGlass & food_container(X) ) => ( there_is_a_spoon | plate(X))).
penalty(1697, 1) <- ~( !X : ( there_is_a_wineGlass & drink_container(X) ) => ( there_is_a_spoon | wineGlass(X))).
penalty(1698, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( there_is_a_spoon | there_is_a_wineGlass)).
penalty(1699, 1) <- ~( !X, Y : ( there_is_a_teaCup & above_of(X,Y) & waterGlass(Y) ) => ( there_is_a_spoon)).
penalty(1700, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_spoon | there_is_a_teaCup)).
penalty(1701, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_spoon | there_is_a_teaCup)).
penalty(1702, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_spoon | fork(X))).
penalty(1703, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(X) ) => ( there_is_a_spoon | plate(X))).
penalty(1704, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_spoon | breakfast)).
penalty(1705, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & lunch ) => ( there_is_a_spoon)).
penalty(1706, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_spoon | waterGlass(Y))).
penalty(1707, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_spoon | teaCup(X))).
penalty(1708, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_spoon | breakfast)).
penalty(1709, 1) <- ~( !X, Y : ( left_of(X,Y) & lunch & drink_container(Y) ) => ( there_is_a_spoon)).
penalty(1710, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( there_is_a_spoon | lunch)).
penalty(1711, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( there_is_a_spoon | plate(X))).
penalty(1712, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(Y) ) => ( there_is_a_spoon | fork(X))).
penalty(1713, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(Y) ) => ( there_is_a_spoon | plate(Y))).
penalty(1714, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_spoon | waterGlass(Y))).
penalty(1715, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_spoon | teaCup(X))).
penalty(1716, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_spoon | breakfast | knife(X))).
penalty(1717, 1) <- ~( !X, Y : ( above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_spoon | breakfast)).
penalty(1718, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(Y) ) => ( there_is_a_spoon | breakfast)).
penalty(1719, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_spoon | breakfast | drink_container(Y))).
penalty(1720, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch ) => ( there_is_a_spoon | knife(X))).
penalty(1721, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch & food_utensil(Y) ) => ( there_is_a_spoon)).
penalty(1722, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch & food_container(Y) ) => ( there_is_a_spoon)).
penalty(1723, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch ) => ( there_is_a_spoon | drink_container(Y))).
penalty(1724, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_spoon | fork(X) | knife(X))).
penalty(1725, 1) <- ~( !X, Y : ( above_of(X,Y) & waterGlass(Y) ) => ( there_is_a_spoon | knife(X))).
penalty(1726, 1) <- ~( !X, Y : ( above_of(X,Y) & wineGlass(Y) ) => ( there_is_a_spoon | knife(X))).
penalty(1727, 1) <- ~( !X, Y : ( above_of(X,Y) & teaCup(Y) ) => ( there_is_a_spoon | knife(X))).
penalty(1728, 1) <- ~( !X, Y : ( above_of(X,Y) & drink_container(Y) ) => ( there_is_a_spoon | knife(X))).
penalty(1729, 1) <- ~( !X : ( food_container(X) ) => ( there_is_a_spoon | breakfast | plate(X))).
penalty(1730, 1) <- ~( !X : ( food_container(X) & lunch ) => ( there_is_a_spoon | plate(X))).
penalty(1731, 1) <- ~( !X : ( food_container(X) ) => ( there_is_a_spoon | plate(X) | saucer(X))).
penalty(1732, 1) <- ~( !X : ( there_is_a_smallKnife & drink_container(X) ) => ( there_is_a_waterGlass | teaCup(X))).
penalty(1733, 1) <- ~( !X, Y : ( there_is_a_smallKnife & left_of(X,Y) & food_utensil(X) & knife(Y) ) => ( false)).
penalty(1734, 1) <- ~( !X, Y : ( there_is_a_smallKnife & left_of(X,Y) & food_container(X) ) => ( knife(Y))).
penalty(1735, 1) <- ~( !X, Y : ( there_is_a_smallKnife & left_of(X,Y) & food_container(X) & spoon(Y) ) => ( false)).
penalty(1736, 1) <- ~( !X, Y : ( there_is_a_smallKnife & left_of(X,Y) & food_container(Y) ) => ( fork(X))).
penalty(1737, 1) <- ~( !X, Y : ( there_is_a_smallKnife & left_of(X,Y) & knife(Y) ) => ( food_container(X))).
penalty(1738, 1) <- ~( !X, Y : ( there_is_a_smallKnife & left_of(X,Y) & spoon(Y) ) => ( knife(X))).
penalty(1739, 1) <- ~( !X, Y : ( there_is_a_smallKnife & left_of(X,Y) & spoon(Y) ) => ( food_utensil(X))).
penalty(1740, 1) <- ~( !X, Y : ( there_is_a_smallKnife & above_of(X,Y) & teaCup(Y) ) => ( knife(X))).
penalty(1741, 1) <- ~( !X, Y : ( there_is_a_smallKnife & above_of(X,Y) & wineGlass(Y) ) => ( spoon(X))).
penalty(1742, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) ) => ( there_is_a_smallKnife | bowl(Y))).
penalty(1743, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_smallKnife | there_is_a_bowl | there_is_a_soupPlate)).
penalty(1744, 1) <- ~(( there_is_a_waterGlass & there_is_a_teaCup ) => ( there_is_a_smallKnife | there_is_a_bowl)).
penalty(1745, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_smallKnife | there_is_a_bowl | there_is_a_waterGlass)).
penalty(1746, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_smallKnife | there_is_a_bowl | there_is_a_wineGlass)).
penalty(1747, 1) <- ~( !X, Y : ( there_is_a_teaCup & on(X,Y) ) => ( there_is_a_smallKnife | there_is_a_bowl)).
penalty(1748, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( there_is_a_smallKnife | there_is_a_bowl)).
penalty(1749, 1) <- ~( !X, Y : ( on(X,Y) & breakfast ) => ( there_is_a_smallKnife | there_is_a_bowl)).
penalty(1750, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_smallKnife | there_is_a_bowl | lunch)).
penalty(1751, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_smallKnife | there_is_a_bowl | soupPlate(Y))).
penalty(1752, 1) <- ~( !X, Y : ( there_is_a_soupPlate & on(X,Y) ) => ( there_is_a_smallKnife | soupPlate(Y))).
penalty(1753, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_smallKnife | there_is_a_soupPlate | bowl(Y))).
penalty(1754, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_smallKnife | there_is_a_waterGlass | bowl(Y))).
penalty(1755, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_smallKnife | there_is_a_wineGlass | bowl(Y))).
penalty(1756, 1) <- ~( !X, Y : ( there_is_a_teaCup & on(X,Y) ) => ( there_is_a_smallKnife | bowl(Y))).
penalty(1757, 1) <- ~( !X, Y : ( on(X,Y) & breakfast ) => ( there_is_a_smallKnife | bowl(Y))).
penalty(1758, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_smallKnife | lunch | bowl(Y))).
penalty(1759, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_smallKnife | bowl(Y) | soupPlate(Y))).
penalty(1760, 1) <- ~(( there_is_a_plate & there_is_a_bowl ) => ( there_is_a_wineGlass | breakfast)).
penalty(1761, 1) <- ~(( there_is_a_plate & there_is_a_bowl & lunch ) => ( there_is_a_wineGlass)).
penalty(1762, 1) <- ~( !X, Y : ( there_is_a_plate & there_is_a_waterGlass & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(1763, 1) <- ~(( there_is_a_plate ) => ( there_is_a_waterGlass | there_is_a_teaCup | lunch)).
penalty(1764, 1) <- ~( !X, Y : ( there_is_a_plate & left_of(X,Y) & fork(Y) ) => ( there_is_a_wineGlass)).
penalty(1765, 1) <- ~( !X, Y : ( there_is_a_plate & left_of(X,Y) & food_container(X) & spoon(Y) ) => ( false)).
penalty(1766, 1) <- ~( !X, Y : ( there_is_a_plate & left_of(X,Y) & fork(Y) ) => ( lunch)).
penalty(1767, 1) <- ~( !X, Y : ( there_is_a_plate & left_of(X,Y) & fork(Y) ) => ( plate(X))).
penalty(1768, 1) <- ~( !X, Y : ( there_is_a_plate & left_of(X,Y) & food_container(Y) ) => ( fork(X))).
penalty(1769, 1) <- ~( !X, Y : ( there_is_a_plate & left_of(X,Y) & spoon(Y) ) => ( knife(X))).
penalty(1770, 1) <- ~( !X, Y : ( there_is_a_plate & left_of(X,Y) & spoon(Y) ) => ( food_utensil(X))).
penalty(1771, 1) <- ~( !X, Y : ( there_is_a_plate & above_of(X,Y) & teaCup(Y) ) => ( knife(X))).
penalty(1772, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) & food_utensil(X) ) => ( there_is_a_plate)).
penalty(1773, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) ) => ( there_is_a_plate | spoon(Y))).
penalty(1774, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) ) => ( there_is_a_plate | bowl(X))).
penalty(1775, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) ) => ( there_is_a_plate | food_utensil(Y))).
penalty(1776, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) & food_container(Y) ) => ( there_is_a_plate)).
penalty(1777, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) ) => ( there_is_a_plate | food_container(X))).
penalty(1778, 1) <- ~( !X, Y : ( there_is_a_bowl & above_of(X,Y) ) => ( there_is_a_plate | spoon(X))).
penalty(1779, 1) <- ~( !X : ( there_is_a_bowl & food_utensil(X) ) => ( there_is_a_plate | spoon(X))).
penalty(1780, 1) <- ~( !X : ( there_is_a_bowl & food_container(X) ) => ( there_is_a_plate | bowl(X))).
penalty(1781, 1) <- ~(( there_is_a_plate | there_is_a_bowl | there_is_a_pastaPlate | there_is_a_soupPlate)).
penalty(1782, 1) <- ~(( there_is_a_waterGlass ) => ( there_is_a_plate | there_is_a_bowl | there_is_a_pastaPlate)).
penalty(1783, 1) <- ~(( there_is_a_plate | there_is_a_bowl | there_is_a_pastaPlate | there_is_a_wineGlass)).
penalty(1784, 1) <- ~(( lunch ) => ( there_is_a_plate | there_is_a_bowl | there_is_a_pastaPlate)).
penalty(1785, 1) <- ~(( there_is_a_plate | there_is_a_bowl | there_is_a_pastaPlate | dinner)).
penalty(1786, 1) <- ~(( there_is_a_waterGlass ) => ( there_is_a_plate | there_is_a_bowl | lunch)).
penalty(1787, 1) <- ~(( there_is_a_plate | there_is_a_bowl | there_is_a_waterGlass | there_is_a_wineGlass)).
penalty(1788, 1) <- ~(( there_is_a_plate | there_is_a_bowl | there_is_a_waterGlass | dinner)).
penalty(1789, 1) <- ~(( there_is_a_plate | there_is_a_bowl | there_is_a_wineGlass | lunch)).
penalty(1790, 1) <- ~(( there_is_a_plate | there_is_a_bowl | lunch | dinner)).
penalty(1791, 1) <- ~(( there_is_a_wineGlass ) => ( there_is_a_plate | there_is_a_pastaPlate | there_is_a_soupPlate)).
penalty(1792, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_plate | there_is_a_pastaPlate)).
penalty(1793, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_plate | there_is_a_pastaPlate | spoon(Y))).
penalty(1794, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_plate | there_is_a_pastaPlate | food_utensil(Y))).
penalty(1795, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(Y) ) => ( there_is_a_plate | there_is_a_pastaPlate)).
penalty(1796, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_plate | there_is_a_pastaPlate | food_container(X))).
penalty(1797, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_plate | there_is_a_pastaPlate | spoon(X))).
penalty(1798, 1) <- ~( !X : ( food_utensil(X) ) => ( there_is_a_plate | there_is_a_pastaPlate | spoon(X))).
penalty(1799, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & food_utensil(X) ) => ( there_is_a_plate)).
penalty(1800, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_plate | spoon(Y))).
penalty(1801, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_plate | soupPlate(X))).
penalty(1802, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_plate | food_utensil(Y))).
penalty(1803, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & food_container(Y) ) => ( there_is_a_plate)).
penalty(1804, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_plate | food_container(X))).
penalty(1805, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) ) => ( there_is_a_plate | spoon(X))).
penalty(1806, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) ) => ( there_is_a_plate | wineGlass(Y))).
penalty(1807, 1) <- ~( !X : ( there_is_a_soupPlate & food_utensil(X) ) => ( there_is_a_plate | spoon(X))).
penalty(1808, 1) <- ~( !X : ( there_is_a_soupPlate & food_container(X) ) => ( there_is_a_plate | soupPlate(X))).
penalty(1809, 1) <- ~( !X : ( there_is_a_soupPlate & drink_container(X) ) => ( there_is_a_plate | wineGlass(X))).
penalty(1810, 1) <- ~( !X, Y : ( there_is_a_waterGlass & above_of(X,Y) ) => ( there_is_a_plate | waterGlass(Y))).
penalty(1811, 1) <- ~( !X : ( there_is_a_waterGlass & drink_container(X) ) => ( there_is_a_plate | waterGlass(X))).
penalty(1812, 1) <- ~(( there_is_a_plate | there_is_a_waterGlass | there_is_a_teaCup | dinner)).
penalty(1813, 1) <- ~(( there_is_a_plate | there_is_a_waterGlass | breakfast | dinner)).
penalty(1814, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) ) => ( there_is_a_plate | wineGlass(Y))).
penalty(1815, 1) <- ~( !X : ( there_is_a_wineGlass & drink_container(X) ) => ( there_is_a_plate | wineGlass(X))).
penalty(1816, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) & food_utensil(X) ) => ( there_is_a_plate)).
penalty(1817, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) ) => ( there_is_a_plate | spoon(Y))).
penalty(1818, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) ) => ( there_is_a_plate | bowl(X))).
penalty(1819, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) ) => ( there_is_a_plate | food_utensil(Y))).
penalty(1820, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) & food_container(Y) ) => ( there_is_a_plate)).
penalty(1821, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) ) => ( there_is_a_plate | food_container(X))).
penalty(1822, 1) <- ~( !X, Y : ( there_is_a_teaCup & above_of(X,Y) ) => ( there_is_a_plate | spoon(X))).
penalty(1823, 1) <- ~( !X, Y : ( there_is_a_teaCup & above_of(X,Y) ) => ( there_is_a_plate | teaCup(Y))).
penalty(1824, 1) <- ~( !X : ( there_is_a_teaCup & food_utensil(X) ) => ( there_is_a_plate | spoon(X))).
penalty(1825, 1) <- ~( !X : ( there_is_a_teaCup & food_container(X) ) => ( there_is_a_plate | bowl(X))).
penalty(1826, 1) <- ~( !X : ( there_is_a_teaCup & drink_container(X) ) => ( there_is_a_plate | teaCup(X))).
penalty(1827, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & breakfast ) => ( there_is_a_plate)).
penalty(1828, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & spoon(Y) ) => ( there_is_a_plate)).
penalty(1829, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_plate | spoon(X))).
penalty(1830, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_plate | pastaPlate(Y))).
penalty(1831, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & food_utensil(Y) ) => ( there_is_a_plate)).
penalty(1832, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( there_is_a_plate | food_container(Y))).
penalty(1833, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast ) => ( there_is_a_plate | spoon(Y))).
penalty(1834, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast ) => ( there_is_a_plate | bowl(X))).
penalty(1835, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast ) => ( there_is_a_plate | food_utensil(Y))).
penalty(1836, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast & food_container(Y) ) => ( there_is_a_plate)).
penalty(1837, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast ) => ( there_is_a_plate | food_container(X))).
penalty(1838, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( there_is_a_plate | pastaPlate(X))).
penalty(1839, 1) <- ~( !X, Y : ( left_of(X,Y) & spoon(Y) ) => ( there_is_a_plate | food_container(X))).
penalty(1840, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_plate | spoon(X) | food_utensil(Y))).
penalty(1841, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(Y) ) => ( there_is_a_plate | spoon(X))).
penalty(1842, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_plate | spoon(X) | food_container(X))).
penalty(1843, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_plate | pastaPlate(Y) | food_utensil(Y))).
penalty(1844, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(Y) ) => ( there_is_a_plate | pastaPlate(Y))).
penalty(1845, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_plate | pastaPlate(Y) | food_container(X))).
penalty(1846, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(Y) ) => ( there_is_a_plate | food_container(X))).
penalty(1847, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_plate | food_utensil(X) | food_utensil(Y))).
penalty(1848, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_plate | food_utensil(X) | food_container(X))).
penalty(1849, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_plate | food_utensil(Y) | food_container(Y))).
penalty(1850, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_plate | food_container(X) | food_container(Y))).
penalty(1851, 1) <- ~( !X, Y : ( above_of(X,Y) & breakfast ) => ( there_is_a_plate | spoon(X))).
penalty(1852, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch ) => ( there_is_a_plate | waterGlass(Y))).
penalty(1853, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_plate | fork(X) | spoon(X))).
penalty(1854, 1) <- ~( !X, Y : ( above_of(X,Y) & teaCup(Y) ) => ( there_is_a_plate | spoon(X))).
penalty(1855, 1) <- ~( !X : ( food_utensil(X) & breakfast ) => ( there_is_a_plate | spoon(X))).
penalty(1856, 1) <- ~( !X : ( food_utensil(X) ) => ( there_is_a_plate | fork(X) | spoon(X))).
penalty(1857, 1) <- ~( !X : ( food_container(X) & breakfast ) => ( there_is_a_plate | bowl(X))).
penalty(1858, 1) <- ~( !X : ( drink_container(X) & lunch ) => ( there_is_a_plate | waterGlass(X))).
penalty(1859, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) ) => ( there_is_a_saucer | bowl(Y))).
penalty(1860, 1) <- ~( !X : ( there_is_a_bowl & drink_container(X) ) => ( there_is_a_waterGlass | teaCup(X))).
penalty(1861, 1) <- ~( !X, Y : ( there_is_a_bowl & there_is_a_wineGlass & above_of(X,Y) & food_utensil(Y) ) => ( false)).
penalty(1862, 1) <- ~( !X, Y : ( there_is_a_bowl & there_is_a_wineGlass & above_of(X,Y) & food_container(Y) ) => ( false)).
penalty(1863, 1) <- ~( !X, Y : ( there_is_a_bowl & there_is_a_wineGlass & above_of(X,Y) ) => ( drink_container(Y))).
penalty(1864, 1) <- ~( !X, Y : ( there_is_a_bowl & there_is_a_wineGlass & on(X,Y) ) => ( plate(X))).
penalty(1865, 1) <- ~( !X, Y : ( there_is_a_bowl & there_is_a_wineGlass & on(X,Y) ) => ( bowl(Y))).
penalty(1866, 1) <- ~( !X, Y : ( there_is_a_bowl & there_is_a_wineGlass & on(X,Y) & food_utensil(Y) ) => ( false)).
penalty(1867, 1) <- ~( !X, Y : ( there_is_a_bowl & there_is_a_wineGlass & on(X,Y) ) => ( food_container(Y))).
penalty(1868, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) & food_utensil(X) & knife(Y) ) => ( false)).
penalty(1869, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) & food_container(Y) ) => ( fork(X))).
penalty(1870, 1) <- ~( !X, Y : ( there_is_a_bowl & left_of(X,Y) & knife(Y) ) => ( food_container(X))).
penalty(1871, 1) <- ~( !X, Y : ( there_is_a_bowl & above_of(X,Y) & food_utensil(Y) ) => ( breakfast)).
penalty(1872, 1) <- ~( !X, Y : ( there_is_a_bowl & above_of(X,Y) & food_container(Y) ) => ( breakfast)).
penalty(1873, 1) <- ~( !X, Y : ( there_is_a_bowl & above_of(X,Y) ) => ( breakfast | drink_container(Y))).
penalty(1874, 1) <- ~( !X, Y : ( there_is_a_bowl & above_of(X,Y) & lunch & food_utensil(Y) ) => ( false)).
penalty(1875, 1) <- ~( !X, Y : ( there_is_a_bowl & above_of(X,Y) & lunch & food_container(Y) ) => ( false)).
penalty(1876, 1) <- ~( !X, Y : ( there_is_a_bowl & above_of(X,Y) & lunch ) => ( drink_container(Y))).
penalty(1877, 1) <- ~( !X, Y : ( there_is_a_bowl & above_of(X,Y) & wineGlass(Y) ) => ( spoon(X))).
penalty(1878, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) ) => ( breakfast | plate(X))).
penalty(1879, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) ) => ( breakfast | bowl(Y))).
penalty(1880, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) & food_utensil(Y) ) => ( breakfast)).
penalty(1881, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) ) => ( breakfast | food_container(Y))).
penalty(1882, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) & lunch ) => ( plate(X))).
penalty(1883, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) & lunch ) => ( bowl(Y))).
penalty(1884, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) & lunch & food_utensil(Y) ) => ( false)).
penalty(1885, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) & lunch ) => ( food_container(Y))).
penalty(1886, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) ) => ( smallKnife(Y) | bowl(Y))).
penalty(1887, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) ) => ( bowl(Y) | saucer(X))).
penalty(1888, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) ) => ( bowl(Y) | food_utensil(Y))).
penalty(1889, 1) <- ~( !X, Y : ( there_is_a_bowl & on(X,Y) & food_container(Y) ) => ( bowl(Y))).
penalty(1890, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_soupPlate | there_is_a_saucer)).
penalty(1891, 1) <- ~(( there_is_a_waterGlass & there_is_a_wineGlass ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1892, 1) <- ~( !X, Y : ( there_is_a_wineGlass & on(X,Y) ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1893, 1) <- ~( !X, Y : ( left_of(X,Y) & spoon(Y) ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1894, 1) <- ~( !X, Y : ( left_of(X,Y) & wineGlass(Y) ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1895, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_soupPlate | breakfast)).
penalty(1896, 1) <- ~( !X, Y : ( on(X,Y) & lunch ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1897, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_soupPlate | smallKnife(Y))).
penalty(1898, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_soupPlate | saucer(X))).
penalty(1899, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_soupPlate | food_utensil(Y))).
penalty(1900, 1) <- ~( !X, Y : ( on(X,Y) & food_container(Y) ) => ( there_is_a_bowl | there_is_a_soupPlate)).
penalty(1901, 1) <- ~(( there_is_a_waterGlass & there_is_a_teaCup ) => ( there_is_a_bowl | there_is_a_saucer)).
penalty(1902, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_saucer | there_is_a_waterGlass)).
penalty(1903, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_saucer | there_is_a_wineGlass)).
penalty(1904, 1) <- ~( !X, Y : ( there_is_a_teaCup & on(X,Y) ) => ( there_is_a_bowl | there_is_a_saucer)).
penalty(1905, 1) <- ~( !X, Y : ( left_of(X,Y) & waterGlass(Y) ) => ( there_is_a_bowl | there_is_a_saucer)).
penalty(1906, 1) <- ~( !X, Y : ( on(X,Y) & breakfast ) => ( there_is_a_bowl | there_is_a_saucer)).
penalty(1907, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_saucer | lunch)).
penalty(1908, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_saucer | soupPlate(Y))).
penalty(1909, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_waterGlass | smallKnife(Y))).
penalty(1910, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_waterGlass | saucer(X))).
penalty(1911, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_waterGlass | food_utensil(Y))).
penalty(1912, 1) <- ~( !X, Y : ( on(X,Y) & food_container(Y) ) => ( there_is_a_bowl | there_is_a_waterGlass)).
penalty(1913, 1) <- ~( !X, Y : ( left_of(X,Y) & spoon(Y) ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1914, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_wineGlass | smallKnife(Y))).
penalty(1915, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_wineGlass | saucer(X))).
penalty(1916, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | there_is_a_wineGlass | food_utensil(Y))).
penalty(1917, 1) <- ~( !X, Y : ( on(X,Y) & food_container(Y) ) => ( there_is_a_bowl | there_is_a_wineGlass)).
penalty(1918, 1) <- ~( !X, Y : ( there_is_a_teaCup & above_of(X,Y) & waterGlass(Y) ) => ( there_is_a_bowl)).
penalty(1919, 1) <- ~( !X, Y : ( there_is_a_teaCup & on(X,Y) ) => ( there_is_a_bowl | smallKnife(Y))).
penalty(1920, 1) <- ~( !X, Y : ( there_is_a_teaCup & on(X,Y) ) => ( there_is_a_bowl | saucer(X))).
penalty(1921, 1) <- ~( !X, Y : ( there_is_a_teaCup & on(X,Y) ) => ( there_is_a_bowl | food_utensil(Y))).
penalty(1922, 1) <- ~( !X, Y : ( there_is_a_teaCup & on(X,Y) & food_container(Y) ) => ( there_is_a_bowl)).
penalty(1923, 1) <- ~( !X, Y : ( above_of(X,Y) & teaCup(Y) ) => ( there_is_a_bowl | knife(X))).
penalty(1924, 1) <- ~( !X, Y : ( on(X,Y) & breakfast ) => ( there_is_a_bowl | smallKnife(Y))).
penalty(1925, 1) <- ~( !X, Y : ( on(X,Y) & breakfast ) => ( there_is_a_bowl | saucer(X))).
penalty(1926, 1) <- ~( !X, Y : ( on(X,Y) & breakfast ) => ( there_is_a_bowl | food_utensil(Y))).
penalty(1927, 1) <- ~( !X, Y : ( on(X,Y) & breakfast & food_container(Y) ) => ( there_is_a_bowl)).
penalty(1928, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | lunch | smallKnife(Y))).
penalty(1929, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | lunch | saucer(X))).
penalty(1930, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | lunch | food_utensil(Y))).
penalty(1931, 1) <- ~( !X, Y : ( on(X,Y) & food_container(Y) ) => ( there_is_a_bowl | lunch)).
penalty(1932, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | smallKnife(Y) | soupPlate(Y))).
penalty(1933, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | saucer(X) | soupPlate(Y))).
penalty(1934, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_bowl | soupPlate(Y) | food_utensil(Y))).
penalty(1935, 1) <- ~( !X, Y : ( on(X,Y) & food_container(Y) ) => ( there_is_a_bowl | soupPlate(Y))).
penalty(1936, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & there_is_a_waterGlass & above_of(X,Y) ) => ( waterGlass(Y))).
penalty(1937, 1) <- ~( !X : ( there_is_a_pastaPlate & there_is_a_waterGlass & drink_container(X) ) => ( waterGlass(X))).
penalty(1938, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & above_of(X,Y) ) => ( there_is_a_waterGlass | wineGlass(Y))).
penalty(1939, 1) <- ~( !X : ( there_is_a_pastaPlate & drink_container(X) ) => ( there_is_a_waterGlass | wineGlass(X))).
penalty(1940, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & there_is_a_wineGlass & above_of(X,Y) ) => ( wineGlass(Y))).
penalty(1941, 1) <- ~( !X : ( there_is_a_pastaPlate & there_is_a_wineGlass & drink_container(X) ) => ( wineGlass(X))).
penalty(1942, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & above_of(X,Y) ) => ( there_is_a_wineGlass | waterGlass(Y))).
penalty(1943, 1) <- ~( !X : ( there_is_a_pastaPlate & drink_container(X) ) => ( there_is_a_wineGlass | waterGlass(X))).
penalty(1944, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & food_utensil(X) ) => ( spoon(X))).
penalty(1945, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & food_utensil(X) ) => ( pastaPlate(Y))).
penalty(1946, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & food_utensil(X) & food_utensil(Y) ) => ( false)).
penalty(1947, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & food_utensil(X) ) => ( food_container(Y))).
penalty(1948, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & food_container(X) ) => ( fork(Y))).
penalty(1949, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & fork(Y) ) => ( pastaPlate(X))).
penalty(1950, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( fork(Y) | spoon(X))).
penalty(1951, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( fork(Y) | pastaPlate(Y))).
penalty(1952, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & food_utensil(Y) ) => ( fork(Y))).
penalty(1953, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( fork(Y) | food_utensil(X))).
penalty(1954, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( fork(Y) | food_container(Y))).
penalty(1955, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( spoon(X) | pastaPlate(X))).
penalty(1956, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( spoon(X) | food_utensil(Y))).
penalty(1957, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & food_container(Y) ) => ( spoon(X))).
penalty(1958, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( spoon(X) | food_container(X))).
penalty(1959, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( pastaPlate(X) | pastaPlate(Y))).
penalty(1960, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & food_utensil(Y) ) => ( pastaPlate(X))).
penalty(1961, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( pastaPlate(X) | food_utensil(X))).
penalty(1962, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( pastaPlate(X) | food_container(Y))).
penalty(1963, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( pastaPlate(Y) | food_utensil(Y))).
penalty(1964, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( pastaPlate(Y) | food_container(X))).
penalty(1965, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) & food_utensil(Y) ) => ( food_container(X))).
penalty(1966, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( food_utensil(X) | food_utensil(Y))).
penalty(1967, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( food_utensil(X) | food_container(X))).
penalty(1968, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( food_utensil(Y) | food_container(Y))).
penalty(1969, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & left_of(X,Y) ) => ( food_container(X) | food_container(Y))).
penalty(1970, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & above_of(X,Y) & lunch ) => ( waterGlass(Y))).
penalty(1971, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & above_of(X,Y) ) => ( lunch | wineGlass(Y))).
penalty(1972, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & above_of(X,Y) ) => ( dinner | waterGlass(Y))).
penalty(1973, 1) <- ~( !X, Y : ( there_is_a_pastaPlate & above_of(X,Y) ) => ( waterGlass(Y) | wineGlass(Y))).
penalty(1974, 1) <- ~( !X : ( there_is_a_pastaPlate & food_utensil(X) ) => ( fork(X) | spoon(X))).
penalty(1975, 1) <- ~( !X : ( there_is_a_pastaPlate & drink_container(X) & lunch ) => ( waterGlass(X))).
penalty(1976, 1) <- ~( !X : ( there_is_a_pastaPlate & drink_container(X) ) => ( lunch | wineGlass(X))).
penalty(1977, 1) <- ~( !X : ( there_is_a_pastaPlate & drink_container(X) ) => ( dinner | waterGlass(X))).
penalty(1978, 1) <- ~( !X : ( there_is_a_pastaPlate & drink_container(X) ) => ( waterGlass(X) | wineGlass(X))).
penalty(1979, 1) <- ~(( there_is_a_wineGlass ) => ( there_is_a_pastaPlate | there_is_a_soupPlate | lunch)).
penalty(1980, 1) <- ~(( there_is_a_pastaPlate | there_is_a_soupPlate | breakfast | lunch)).
penalty(1981, 1) <- ~( !X, Y : ( there_is_a_waterGlass & left_of(X,Y) & fork(Y) ) => ( there_is_a_pastaPlate)).
penalty(1982, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( there_is_a_pastaPlate | there_is_a_wineGlass)).
penalty(1983, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & dinner ) => ( there_is_a_pastaPlate)).
penalty(1984, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( there_is_a_pastaPlate | lunch)).
penalty(1985, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( there_is_a_pastaPlate | spoon(Y))).
penalty(1986, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( there_is_a_pastaPlate | soupPlate(X))).
penalty(1987, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( there_is_a_pastaPlate | food_utensil(Y))).
penalty(1988, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner & food_container(Y) ) => ( there_is_a_pastaPlate)).
penalty(1989, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( there_is_a_pastaPlate | food_container(X))).
penalty(1990, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( there_is_a_pastaPlate | plate(X))).
penalty(1991, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(Y) ) => ( there_is_a_pastaPlate | fork(X))).
penalty(1992, 1) <- ~( !X, Y : ( above_of(X,Y) & dinner ) => ( there_is_a_pastaPlate | spoon(X))).
penalty(1993, 1) <- ~( !X : ( food_utensil(X) & dinner ) => ( there_is_a_pastaPlate | spoon(X))).
penalty(1994, 1) <- ~( !X : ( food_container(X) & dinner ) => ( there_is_a_pastaPlate | soupPlate(X))).
penalty(1995, 1) <- ~( !X, Y : ( there_is_a_soupPlate & on(X,Y) ) => ( there_is_a_saucer | soupPlate(Y))).
penalty(1996, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & food_utensil(X) ) => ( there_is_a_waterGlass)).
penalty(1997, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_waterGlass | spoon(Y))).
penalty(1998, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_waterGlass | soupPlate(X))).
penalty(1999, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_waterGlass | food_utensil(Y))).
penalty(2000, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & food_container(Y) ) => ( there_is_a_waterGlass)).
penalty(2001, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( there_is_a_waterGlass | food_container(X))).
penalty(2002, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) ) => ( there_is_a_waterGlass | spoon(X))).
penalty(2003, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) ) => ( there_is_a_waterGlass | wineGlass(Y))).
penalty(2004, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_waterGlass)).
penalty(2005, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) & food_container(Y) ) => ( there_is_a_waterGlass)).
penalty(2006, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) ) => ( there_is_a_waterGlass | drink_container(Y))).
penalty(2007, 1) <- ~( !X : ( there_is_a_soupPlate & food_utensil(X) ) => ( there_is_a_waterGlass | spoon(X))).
penalty(2008, 1) <- ~( !X : ( there_is_a_soupPlate & food_container(X) ) => ( there_is_a_waterGlass | soupPlate(X))).
penalty(2009, 1) <- ~( !X : ( there_is_a_soupPlate & drink_container(X) ) => ( there_is_a_waterGlass | wineGlass(X))).
penalty(2010, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & food_utensil(X) ) => ( lunch)).
penalty(2011, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & food_utensil(X) & dinner ) => ( false)).
penalty(2012, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & food_utensil(X) & knife(Y) ) => ( false)).
penalty(2013, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & drink_container(X) ) => ( lunch)).
penalty(2014, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & drink_container(X) ) => ( waterGlass(X))).
penalty(2015, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & drink_container(X) ) => ( wineGlass(Y))).
penalty(2016, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( lunch | spoon(Y))).
penalty(2017, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( lunch | soupPlate(X))).
penalty(2018, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( lunch | food_utensil(Y))).
penalty(2019, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & food_container(Y) ) => ( lunch)).
penalty(2020, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) ) => ( lunch | food_container(X))).
penalty(2021, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & drink_container(Y) ) => ( lunch)).
penalty(2022, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & dinner ) => ( spoon(Y))).
penalty(2023, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & dinner ) => ( soupPlate(X))).
penalty(2024, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & dinner ) => ( food_utensil(Y))).
penalty(2025, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & dinner & food_container(Y) ) => ( false)).
penalty(2026, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & dinner ) => ( food_container(X))).
penalty(2027, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & food_container(Y) ) => ( fork(X))).
penalty(2028, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & knife(Y) ) => ( food_container(X))).
penalty(2029, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & drink_container(Y) ) => ( waterGlass(X))).
penalty(2030, 1) <- ~( !X, Y : ( there_is_a_soupPlate & left_of(X,Y) & drink_container(Y) ) => ( wineGlass(Y))).
penalty(2031, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) ) => ( lunch | spoon(X))).
penalty(2032, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) ) => ( lunch | wineGlass(Y))).
penalty(2033, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) & food_utensil(Y) ) => ( lunch)).
penalty(2034, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) & food_container(Y) ) => ( lunch)).
penalty(2035, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) ) => ( lunch | drink_container(Y))).
penalty(2036, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) & dinner ) => ( spoon(X))).
penalty(2037, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) & waterGlass(Y) ) => ( knife(X))).
penalty(2038, 1) <- ~( !X, Y : ( there_is_a_soupPlate & above_of(X,Y) & wineGlass(Y) ) => ( spoon(X))).
penalty(2039, 1) <- ~( !X, Y : ( there_is_a_soupPlate & on(X,Y) ) => ( smallKnife(Y) | soupPlate(Y))).
penalty(2040, 1) <- ~( !X, Y : ( there_is_a_soupPlate & on(X,Y) ) => ( saucer(X) | soupPlate(Y))).
penalty(2041, 1) <- ~( !X, Y : ( there_is_a_soupPlate & on(X,Y) ) => ( soupPlate(Y) | food_utensil(Y))).
penalty(2042, 1) <- ~( !X, Y : ( there_is_a_soupPlate & on(X,Y) & food_container(Y) ) => ( soupPlate(Y))).
penalty(2043, 1) <- ~( !X : ( there_is_a_soupPlate & food_utensil(X) ) => ( lunch | spoon(X))).
penalty(2044, 1) <- ~( !X : ( there_is_a_soupPlate & food_utensil(X) & dinner ) => ( spoon(X))).
penalty(2045, 1) <- ~( !X : ( there_is_a_soupPlate & food_container(X) ) => ( lunch | soupPlate(X))).
penalty(2046, 1) <- ~( !X : ( there_is_a_soupPlate & food_container(X) & dinner ) => ( soupPlate(X))).
penalty(2047, 1) <- ~( !X : ( there_is_a_soupPlate & drink_container(X) ) => ( lunch | wineGlass(X))).
penalty(2048, 1) <- ~( !X : ( there_is_a_soupPlate & drink_container(X) ) => ( waterGlass(X) | wineGlass(X))).
penalty(2049, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_soupPlate | there_is_a_saucer | bowl(Y))).
penalty(2050, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_soupPlate)).
penalty(2051, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) & food_container(Y) ) => ( there_is_a_soupPlate)).
penalty(2052, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) ) => ( there_is_a_soupPlate | drink_container(Y))).
penalty(2053, 1) <- ~( !X, Y : ( there_is_a_wineGlass & on(X,Y) ) => ( there_is_a_soupPlate | plate(X))).
penalty(2054, 1) <- ~( !X, Y : ( there_is_a_wineGlass & on(X,Y) ) => ( there_is_a_soupPlate | bowl(Y))).
penalty(2055, 1) <- ~( !X, Y : ( there_is_a_wineGlass & on(X,Y) & food_utensil(Y) ) => ( there_is_a_soupPlate)).
penalty(2056, 1) <- ~( !X, Y : ( there_is_a_wineGlass & on(X,Y) ) => ( there_is_a_soupPlate | food_container(Y))).
penalty(2057, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner & spoon(Y) ) => ( there_is_a_soupPlate)).
penalty(2058, 1) <- ~( !X, Y : ( above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_soupPlate | breakfast)).
penalty(2059, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(Y) ) => ( there_is_a_soupPlate | breakfast)).
penalty(2060, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_soupPlate | breakfast | drink_container(Y))).
penalty(2061, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch & food_utensil(Y) ) => ( there_is_a_soupPlate)).
penalty(2062, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch & food_container(Y) ) => ( there_is_a_soupPlate)).
penalty(2063, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch ) => ( there_is_a_soupPlate | drink_container(Y))).
penalty(2064, 1) <- ~( !X, Y : ( above_of(X,Y) & dinner ) => ( there_is_a_soupPlate | fork(X))).
penalty(2065, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_soupPlate | breakfast | plate(X))).
penalty(2066, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_soupPlate | breakfast | bowl(Y))).
penalty(2067, 1) <- ~( !X, Y : ( on(X,Y) & food_utensil(Y) ) => ( there_is_a_soupPlate | breakfast)).
penalty(2068, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_soupPlate | breakfast | food_container(Y))).
penalty(2069, 1) <- ~( !X, Y : ( on(X,Y) & lunch ) => ( there_is_a_soupPlate | plate(X))).
penalty(2070, 1) <- ~( !X, Y : ( on(X,Y) & lunch ) => ( there_is_a_soupPlate | bowl(Y))).
penalty(2071, 1) <- ~( !X, Y : ( on(X,Y) & lunch & food_utensil(Y) ) => ( there_is_a_soupPlate)).
penalty(2072, 1) <- ~( !X, Y : ( on(X,Y) & lunch ) => ( there_is_a_soupPlate | food_container(Y))).
penalty(2073, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_soupPlate | smallKnife(Y) | bowl(Y))).
penalty(2074, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_soupPlate | bowl(Y) | saucer(X))).
penalty(2075, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_soupPlate | bowl(Y) | food_utensil(Y))).
penalty(2076, 1) <- ~( !X, Y : ( on(X,Y) & food_container(Y) ) => ( there_is_a_soupPlate | bowl(Y))).
penalty(2077, 1) <- ~( !X : ( food_container(X) & dinner ) => ( there_is_a_soupPlate | pastaPlate(X))).
penalty(2078, 1) <- ~( !X : ( there_is_a_saucer & drink_container(X) ) => ( there_is_a_waterGlass | teaCup(X))).
penalty(2079, 1) <- ~( !X, Y : ( there_is_a_saucer & left_of(X,Y) & food_utensil(X) & knife(Y) ) => ( false)).
penalty(2080, 1) <- ~( !X, Y : ( there_is_a_saucer & left_of(X,Y) & food_container(X) ) => ( knife(Y))).
penalty(2081, 1) <- ~( !X, Y : ( there_is_a_saucer & left_of(X,Y) & food_container(X) & spoon(Y) ) => ( false)).
penalty(2082, 1) <- ~( !X, Y : ( there_is_a_saucer & left_of(X,Y) & food_container(Y) ) => ( fork(X))).
penalty(2083, 1) <- ~( !X, Y : ( there_is_a_saucer & left_of(X,Y) & knife(Y) ) => ( food_container(X))).
penalty(2084, 1) <- ~( !X, Y : ( there_is_a_saucer & left_of(X,Y) & spoon(Y) ) => ( knife(X))).
penalty(2085, 1) <- ~( !X, Y : ( there_is_a_saucer & left_of(X,Y) & spoon(Y) ) => ( food_utensil(X))).
penalty(2086, 1) <- ~( !X, Y : ( there_is_a_saucer & above_of(X,Y) & teaCup(Y) ) => ( knife(X))).
penalty(2087, 1) <- ~( !X, Y : ( there_is_a_saucer & above_of(X,Y) & wineGlass(Y) ) => ( spoon(X))).
penalty(2088, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_saucer | there_is_a_waterGlass | bowl(Y))).
penalty(2089, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_saucer | there_is_a_wineGlass | bowl(Y))).
penalty(2090, 1) <- ~( !X, Y : ( there_is_a_teaCup & on(X,Y) ) => ( there_is_a_saucer | bowl(Y))).
penalty(2091, 1) <- ~( !X, Y : ( on(X,Y) & breakfast ) => ( there_is_a_saucer | bowl(Y))).
penalty(2092, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_saucer | lunch | bowl(Y))).
penalty(2093, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_saucer | bowl(Y) | soupPlate(Y))).
penalty(2094, 1) <- ~( !X, Y : ( there_is_a_waterGlass & there_is_a_wineGlass & left_of(X,Y) & fork(Y) ) => ( false)).
penalty(2095, 1) <- ~( !X, Y : ( there_is_a_waterGlass & left_of(X,Y) & food_utensil(X) & knife(Y) ) => ( false)).
penalty(2096, 1) <- ~( !X, Y : ( there_is_a_waterGlass & left_of(X,Y) & fork(Y) ) => ( lunch)).
penalty(2097, 1) <- ~( !X, Y : ( there_is_a_waterGlass & left_of(X,Y) & fork(Y) ) => ( pastaPlate(X))).
penalty(2098, 1) <- ~( !X, Y : ( there_is_a_waterGlass & left_of(X,Y) & knife(Y) ) => ( food_container(X))).
penalty(2099, 1) <- ~( !X, Y : ( there_is_a_waterGlass & above_of(X,Y) & teaCup(Y) ) => ( knife(X))).
penalty(2100, 1) <- ~( !X, Y : ( there_is_a_waterGlass & above_of(X,Y) & wineGlass(Y) ) => ( spoon(X))).
penalty(2101, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) ) => ( there_is_a_waterGlass | wineGlass(Y))).
penalty(2102, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_waterGlass)).
penalty(2103, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) & food_container(Y) ) => ( there_is_a_waterGlass)).
penalty(2104, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) ) => ( there_is_a_waterGlass | drink_container(Y))).
penalty(2105, 1) <- ~( !X : ( there_is_a_wineGlass & drink_container(X) ) => ( there_is_a_waterGlass | wineGlass(X))).
penalty(2106, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( there_is_a_waterGlass | there_is_a_wineGlass)).
penalty(2107, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_waterGlass | there_is_a_teaCup | wineGlass(Y))).
penalty(2108, 1) <- ~( !X, Y : ( above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(2109, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(Y) ) => ( there_is_a_waterGlass | there_is_a_teaCup)).
penalty(2110, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_waterGlass | there_is_a_teaCup | drink_container(Y))).
penalty(2111, 1) <- ~( !X : ( drink_container(X) ) => ( there_is_a_waterGlass | there_is_a_teaCup | wineGlass(X))).
penalty(2112, 1) <- ~(( there_is_a_waterGlass | there_is_a_teaCup | lunch | dinner)).
penalty(2113, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_waterGlass | food_utensil(X) | food_utensil(Y))).
penalty(2114, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_waterGlass | food_utensil(X) | food_container(X))).
penalty(2115, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( there_is_a_waterGlass | food_utensil(Y) | food_container(Y))).
penalty(2116, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_waterGlass | breakfast | wineGlass(Y))).
penalty(2117, 1) <- ~( !X, Y : ( above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_waterGlass | breakfast)).
penalty(2118, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(Y) ) => ( there_is_a_waterGlass | breakfast)).
penalty(2119, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_waterGlass | breakfast | drink_container(Y))).
penalty(2120, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch ) => ( there_is_a_waterGlass | knife(X))).
penalty(2121, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch ) => ( there_is_a_waterGlass | wineGlass(Y))).
penalty(2122, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch & food_utensil(Y) ) => ( there_is_a_waterGlass)).
penalty(2123, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch & food_container(Y) ) => ( there_is_a_waterGlass)).
penalty(2124, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch ) => ( there_is_a_waterGlass | drink_container(Y))).
penalty(2125, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_waterGlass | smallKnife(Y) | bowl(Y))).
penalty(2126, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_waterGlass | bowl(Y) | saucer(X))).
penalty(2127, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_waterGlass | bowl(Y) | food_utensil(Y))).
penalty(2128, 1) <- ~( !X, Y : ( on(X,Y) & food_container(Y) ) => ( there_is_a_waterGlass | bowl(Y))).
penalty(2129, 1) <- ~( !X : ( food_container(X) & lunch ) => ( there_is_a_waterGlass | plate(X))).
penalty(2130, 1) <- ~( !X : ( drink_container(X) ) => ( there_is_a_waterGlass | breakfast | wineGlass(X))).
penalty(2131, 1) <- ~( !X : ( drink_container(X) & lunch ) => ( there_is_a_waterGlass | wineGlass(X))).
penalty(2132, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) & drink_container(X) ) => ( lunch)).
penalty(2133, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) & drink_container(X) ) => ( waterGlass(X))).
penalty(2134, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) & drink_container(X) ) => ( wineGlass(Y))).
penalty(2135, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) & drink_container(Y) ) => ( lunch)).
penalty(2136, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) & drink_container(Y) ) => ( waterGlass(X))).
penalty(2137, 1) <- ~( !X, Y : ( there_is_a_wineGlass & left_of(X,Y) & drink_container(Y) ) => ( wineGlass(Y))).
penalty(2138, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) ) => ( lunch | wineGlass(Y))).
penalty(2139, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) & food_utensil(Y) ) => ( lunch)).
penalty(2140, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) & food_container(Y) ) => ( lunch)).
penalty(2141, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) ) => ( lunch | drink_container(Y))).
penalty(2142, 1) <- ~( !X, Y : ( there_is_a_wineGlass & above_of(X,Y) & waterGlass(Y) ) => ( knife(X))).
penalty(2143, 1) <- ~( !X : ( there_is_a_wineGlass & drink_container(X) ) => ( lunch | wineGlass(X))).
penalty(2144, 1) <- ~( !X : ( there_is_a_wineGlass & drink_container(X) ) => ( waterGlass(X) | wineGlass(X))).
penalty(2145, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_wineGlass | there_is_a_teaCup)).
penalty(2146, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_wineGlass | there_is_a_teaCup)).
penalty(2147, 1) <- ~( !X : ( drink_container(X) ) => ( there_is_a_wineGlass | there_is_a_teaCup | waterGlass(X))).
penalty(2148, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & knife(Y) ) => ( there_is_a_wineGlass)).
penalty(2149, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_wineGlass | breakfast)).
penalty(2150, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & lunch ) => ( there_is_a_wineGlass)).
penalty(2151, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_wineGlass | waterGlass(Y))).
penalty(2152, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_wineGlass | teaCup(X))).
penalty(2153, 1) <- ~( !X, Y : ( left_of(X,Y) & bowl(Y) ) => ( there_is_a_wineGlass | breakfast)).
penalty(2154, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_wineGlass | breakfast)).
penalty(2155, 1) <- ~( !X, Y : ( left_of(X,Y) & lunch & bowl(Y) ) => ( there_is_a_wineGlass)).
penalty(2156, 1) <- ~( !X, Y : ( left_of(X,Y) & lunch & drink_container(Y) ) => ( there_is_a_wineGlass)).
penalty(2157, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( there_is_a_wineGlass | lunch)).
penalty(2158, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( there_is_a_wineGlass | pastaPlate(X))).
penalty(2159, 1) <- ~( !X, Y : ( left_of(X,Y) & knife(Y) ) => ( there_is_a_wineGlass | food_container(X))).
penalty(2160, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_wineGlass | waterGlass(Y))).
penalty(2161, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_wineGlass | teaCup(X))).
penalty(2162, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_wineGlass | breakfast | waterGlass(Y))).
penalty(2163, 1) <- ~( !X, Y : ( above_of(X,Y) & food_utensil(Y) ) => ( there_is_a_wineGlass | breakfast)).
penalty(2164, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(Y) ) => ( there_is_a_wineGlass | breakfast)).
penalty(2165, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( there_is_a_wineGlass | breakfast | drink_container(Y))).
penalty(2166, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch ) => ( there_is_a_wineGlass | waterGlass(Y))).
penalty(2167, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch & food_utensil(Y) ) => ( there_is_a_wineGlass)).
penalty(2168, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch & food_container(Y) ) => ( there_is_a_wineGlass)).
penalty(2169, 1) <- ~( !X, Y : ( above_of(X,Y) & lunch ) => ( there_is_a_wineGlass | drink_container(Y))).
penalty(2170, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_wineGlass | smallKnife(Y) | bowl(Y))).
penalty(2171, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_wineGlass | bowl(Y) | saucer(X))).
penalty(2172, 1) <- ~( !X, Y : ( on(X,Y) ) => ( there_is_a_wineGlass | bowl(Y) | food_utensil(Y))).
penalty(2173, 1) <- ~( !X, Y : ( on(X,Y) & food_container(Y) ) => ( there_is_a_wineGlass | bowl(Y))).
penalty(2174, 1) <- ~( !X : ( drink_container(X) ) => ( there_is_a_wineGlass | breakfast | waterGlass(X))).
penalty(2175, 1) <- ~( !X : ( drink_container(X) & lunch ) => ( there_is_a_wineGlass | waterGlass(X))).
penalty(2176, 1) <- ~( !X : ( drink_container(X) ) => ( there_is_a_wineGlass | waterGlass(X) | teaCup(X))).
penalty(2177, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) & food_utensil(X) & knife(Y) ) => ( false)).
penalty(2178, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) & drink_container(X) ) => ( waterGlass(Y))).
penalty(2179, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) & drink_container(X) ) => ( teaCup(X))).
penalty(2180, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) & food_container(Y) ) => ( fork(X))).
penalty(2181, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) & knife(Y) ) => ( food_container(X))).
penalty(2182, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) & drink_container(Y) ) => ( waterGlass(Y))).
penalty(2183, 1) <- ~( !X, Y : ( there_is_a_teaCup & left_of(X,Y) & drink_container(Y) ) => ( teaCup(X))).
penalty(2184, 1) <- ~( !X, Y : ( there_is_a_teaCup & above_of(X,Y) & waterGlass(Y) ) => ( spoon(X))).
penalty(2185, 1) <- ~( !X, Y : ( there_is_a_teaCup & on(X,Y) ) => ( smallKnife(Y) | bowl(Y))).
penalty(2186, 1) <- ~( !X, Y : ( there_is_a_teaCup & on(X,Y) ) => ( bowl(Y) | saucer(X))).
penalty(2187, 1) <- ~( !X, Y : ( there_is_a_teaCup & on(X,Y) ) => ( bowl(Y) | food_utensil(Y))).
penalty(2188, 1) <- ~( !X, Y : ( there_is_a_teaCup & on(X,Y) & food_container(Y) ) => ( bowl(Y))).
penalty(2189, 1) <- ~( !X : ( there_is_a_teaCup & drink_container(X) ) => ( waterGlass(X) | teaCup(X))).
penalty(2190, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & breakfast ) => ( there_is_a_teaCup)).
penalty(2191, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_teaCup | lunch)).
penalty(2192, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_teaCup | waterGlass(X))).
penalty(2193, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( there_is_a_teaCup | wineGlass(Y))).
penalty(2194, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast & drink_container(Y) ) => ( there_is_a_teaCup)).
penalty(2195, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_teaCup | lunch)).
penalty(2196, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_teaCup | waterGlass(X))).
penalty(2197, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( there_is_a_teaCup | wineGlass(Y))).
penalty(2198, 1) <- ~( !X : ( drink_container(X) & breakfast ) => ( there_is_a_teaCup | waterGlass(X))).
penalty(2199, 1) <- ~( !X : ( drink_container(X) ) => ( there_is_a_teaCup | waterGlass(X) | wineGlass(X))).
penalty(2200, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & breakfast & knife(Y) ) => ( false)).
penalty(2201, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & knife(Y) ) => ( lunch)).
penalty(2202, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & dinner & spoon(Y) ) => ( false)).
penalty(2203, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & dinner ) => ( spoon(X))).
penalty(2204, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & dinner ) => ( pastaPlate(Y))).
penalty(2205, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & dinner & food_utensil(Y) ) => ( false)).
penalty(2206, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & dinner ) => ( food_container(Y))).
penalty(2207, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & knife(Y) ) => ( fork(X))).
penalty(2208, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) & spoon(Y) ) => ( knife(X))).
penalty(2209, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(X) ) => ( food_utensil(Y) | food_container(Y))).
penalty(2210, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & breakfast ) => ( waterGlass(Y))).
penalty(2211, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & breakfast ) => ( teaCup(X))).
penalty(2212, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( breakfast | lunch)).
penalty(2213, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( breakfast | waterGlass(X))).
penalty(2214, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( breakfast | wineGlass(Y))).
penalty(2215, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & lunch ) => ( waterGlass(X))).
penalty(2216, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) & lunch ) => ( wineGlass(Y))).
penalty(2217, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( lunch | waterGlass(Y))).
penalty(2218, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( lunch | teaCup(X))).
penalty(2219, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( waterGlass(X) | waterGlass(Y))).
penalty(2220, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( waterGlass(X) | teaCup(X))).
penalty(2221, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( waterGlass(Y) | wineGlass(Y))).
penalty(2222, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(X) ) => ( wineGlass(Y) | teaCup(X))).
penalty(2223, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast & food_container(Y) ) => ( fork(X))).
penalty(2224, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast & knife(Y) ) => ( food_container(X))).
penalty(2225, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast & drink_container(Y) ) => ( waterGlass(Y))).
penalty(2226, 1) <- ~( !X, Y : ( left_of(X,Y) & breakfast & drink_container(Y) ) => ( teaCup(X))).
penalty(2227, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( breakfast | lunch)).
penalty(2228, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( breakfast | waterGlass(X))).
penalty(2229, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( breakfast | wineGlass(Y))).
penalty(2230, 1) <- ~( !X, Y : ( left_of(X,Y) & lunch & drink_container(Y) ) => ( waterGlass(X))).
penalty(2231, 1) <- ~( !X, Y : ( left_of(X,Y) & lunch & drink_container(Y) ) => ( wineGlass(Y))).
penalty(2232, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( lunch | dinner)).
penalty(2233, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( lunch | pastaPlate(X))).
penalty(2234, 1) <- ~( !X, Y : ( left_of(X,Y) & knife(Y) ) => ( lunch | food_container(X))).
penalty(2235, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( lunch | waterGlass(Y))).
penalty(2236, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( lunch | teaCup(X))).
penalty(2237, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner & fork(Y) ) => ( pastaPlate(X))).
penalty(2238, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner & spoon(Y) ) => ( soupPlate(X))).
penalty(2239, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner & spoon(Y) ) => ( food_container(X))).
penalty(2240, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( spoon(X) | food_utensil(Y))).
penalty(2241, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner & food_container(Y) ) => ( spoon(X))).
penalty(2242, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( spoon(X) | food_container(X))).
penalty(2243, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( pastaPlate(Y) | food_utensil(Y))).
penalty(2244, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner & food_container(Y) ) => ( pastaPlate(Y))).
penalty(2245, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( pastaPlate(Y) | food_container(X))).
penalty(2246, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner & food_utensil(Y) ) => ( food_container(X))).
penalty(2247, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( food_utensil(X) | food_utensil(Y))).
penalty(2248, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( food_utensil(X) | food_container(X))).
penalty(2249, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( food_utensil(Y) | food_container(Y))).
penalty(2250, 1) <- ~( !X, Y : ( left_of(X,Y) & dinner ) => ( food_container(X) | food_container(Y))).
penalty(2251, 1) <- ~( !X, Y : ( left_of(X,Y) & fork(Y) ) => ( plate(X) | pastaPlate(X))).
penalty(2252, 1) <- ~( !X, Y : ( left_of(X,Y) & knife(Y) ) => ( fork(X) | food_container(X))).
penalty(2253, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(Y) ) => ( fork(X) | spoon(X))).
penalty(2254, 1) <- ~( !X, Y : ( left_of(X,Y) & food_container(Y) ) => ( fork(X) | pastaPlate(Y))).
penalty(2255, 1) <- ~( !X, Y : ( left_of(X,Y) & knife(Y) ) => ( food_utensil(X) | food_container(X))).
penalty(2256, 1) <- ~( !X, Y : ( left_of(X,Y) & spoon(Y) ) => ( knife(X) | food_container(X))).
penalty(2257, 1) <- ~( !X, Y : ( left_of(X,Y) & spoon(Y) ) => ( food_utensil(X) | food_container(X))).
penalty(2258, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( waterGlass(X) | waterGlass(Y))).
penalty(2259, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( waterGlass(X) | teaCup(X))).
penalty(2260, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( waterGlass(Y) | wineGlass(Y))).
penalty(2261, 1) <- ~( !X, Y : ( left_of(X,Y) & drink_container(Y) ) => ( wineGlass(Y) | teaCup(X))).
penalty(2262, 1) <- ~( !X, Y : ( left_of(X,Y) & food_utensil(Y) ) => ( food_utensil(X) | food_container(X))).
penalty(2263, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( food_utensil(X) | food_utensil(Y) | drink_container(X))).
penalty(2264, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( food_utensil(X) | food_utensil(Y) | drink_container(Y))).
penalty(2265, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( food_utensil(X) | food_container(X) | drink_container(X))).
penalty(2266, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( food_utensil(X) | food_container(X) | drink_container(Y))).
penalty(2267, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( food_utensil(Y) | food_container(Y) | drink_container(X))).
penalty(2268, 1) <- ~( !X, Y : ( left_of(X,Y) ) => ( food_utensil(Y) | food_container(Y) | drink_container(Y))).
penalty(2269, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( breakfast | lunch | wineGlass(Y))).
penalty(2270, 1) <- ~( !X, Y : ( above_of(X,Y) & food_utensil(Y) ) => ( breakfast | lunch)).
penalty(2271, 1) <- ~( !X, Y : ( above_of(X,Y) & food_container(Y) ) => ( breakfast | lunch)).
penalty(2272, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( breakfast | lunch | drink_container(Y))).
penalty(2273, 1) <- ~( !X, Y : ( above_of(X,Y) & dinner ) => ( fork(X) | spoon(X))).
penalty(2274, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( fork(X) | knife(X) | spoon(X))).
penalty(2275, 1) <- ~( !X, Y : ( above_of(X,Y) & teaCup(Y) ) => ( knife(X) | spoon(X))).
penalty(2276, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( smallKnife(Y) | saucer(Y) | drink_container(Y))).
penalty(2277, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( smallKnife(Y) | food_container(Y) | drink_container(Y))).
penalty(2278, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( saucer(Y) | food_utensil(Y) | drink_container(Y))).
penalty(2279, 1) <- ~( !X, Y : ( above_of(X,Y) ) => ( food_utensil(Y) | food_container(Y) | drink_container(Y))).
penalty(2280, 1) <- ~( !X, Y : ( on(X,Y) & breakfast ) => ( smallKnife(Y) | bowl(Y))).
penalty(2281, 1) <- ~( !X, Y : ( on(X,Y) & breakfast ) => ( bowl(Y) | saucer(X))).
penalty(2282, 1) <- ~( !X, Y : ( on(X,Y) & breakfast ) => ( bowl(Y) | food_utensil(Y))).
penalty(2283, 1) <- ~( !X, Y : ( on(X,Y) & breakfast & food_container(Y) ) => ( bowl(Y))).
penalty(2284, 1) <- ~( !X, Y : ( on(X,Y) ) => ( lunch | smallKnife(Y) | bowl(Y))).
penalty(2285, 1) <- ~( !X, Y : ( on(X,Y) ) => ( lunch | bowl(Y) | saucer(X))).
penalty(2286, 1) <- ~( !X, Y : ( on(X,Y) ) => ( lunch | bowl(Y) | food_utensil(Y))).
penalty(2287, 1) <- ~( !X, Y : ( on(X,Y) & food_container(Y) ) => ( lunch | bowl(Y))).
penalty(2288, 1) <- ~( !X, Y : ( on(X,Y) ) => ( smallKnife(Y) | bowl(Y) | soupPlate(Y))).
penalty(2289, 1) <- ~( !X, Y : ( on(X,Y) ) => ( bowl(Y) | saucer(X) | soupPlate(Y))).
penalty(2290, 1) <- ~( !X, Y : ( on(X,Y) ) => ( bowl(Y) | soupPlate(Y) | food_utensil(Y))).
penalty(2291, 1) <- ~( !X, Y : ( on(X,Y) & food_container(Y) ) => ( bowl(Y) | soupPlate(Y))).
penalty(2292, 1) <- ~( !X : ( food_utensil(X) & dinner ) => ( fork(X) | spoon(X))).
penalty(2293, 1) <- ~( !X : ( food_container(X) & dinner ) => ( pastaPlate(X) | soupPlate(X))).
penalty(2294, 1) <- ~( !X : ( drink_container(X) & breakfast ) => ( waterGlass(X) | teaCup(X))).
penalty(2295, 1) <- ~( !X : ( drink_container(X) ) => ( breakfast | lunch | wineGlass(X))).
penalty(2296, 1) <- ~( !X : ( drink_container(X) ) => ( breakfast | waterGlass(X) | wineGlass(X))).
penalty(2297, 1) <- ~( !X : ( drink_container(X) & lunch ) => ( waterGlass(X) | wineGlass(X))).
penalty(2298, 1) <- ~( !X : ( drink_container(X) ) => ( waterGlass(X) | wineGlass(X) | teaCup(X))).
}
}



structure S : V {
    time = {breakfast}
    breakfastTime = {breakfast}
    lunchTime = {}
    dinnerTime = {}


    fork = {fork}
    knife = {}
    spoon = {spoon}
    plate = {plate}
    bowl = {}
    soupPlate = {}
    teaCup = {}
    waterGlass = {water}
    wineGlass = {}

    RuleNr = {0..2298}
    Penalty = {0..1}

    // above_of = {(f, water)}

    PossCo = {0..3}
    // Example 1 in claudien:
    // position = {(0, 0, 0, fork); (1, 0, 0, plate); (2, 0, 0, knife); (2, 1, 0, water)}
    
    // Example 2 in claudien:
    // position = {(0, 0, 0, fork); (1, 0, 0, plate); (2, 0, 0, knife); (2, 1, 0, wine)}

    // Example 3 in claudien:
    // position = {(0, 0, 0, plate); (1, 0, 0, fork); (2, 0, 0, knife); (2, 1, 0, wine)}

    // Not an example but should work with penalty/penalty
    position = {(0, 0, 0, plate); (1, 0, 0, fork); (2, 0, 0, water); (2, 1, 0, spoon)}

    // Breakfast 1: should generate model
    // position = {(0, 0, 0, plate); (1, 0, 0, knife); (1, 1, 0, cup)}

    // Breakfast 3
    // position = {(0, 0, 0, fork); (1, 0, 0, plate); (2, 0, 0, knife); (2, 1, 0, cup); (3, 1, 0, water)}

    // Breakfast: should give unsatisfiable. Because there is a waterGlass and teaCup... (is important rule, without penalty)
    // position = {(0, 0, 0, plate); (0, 0, 1, bowl); (1, 0, 0, knife); (2, 1, 0, cup); (3, 0, 0, spoon); (3, 1, 0, water)}

}

procedure main() {
    stdoptions.nrpropsteps=1
    // stdoptions.longestbranch=1
    stdoptions.liftedunitpropagation=false
    stdoptions.cpsupport=true
    // stdoptions.verbosity.grounding=1
    stdoptions.verbosity.solving=1
    stdoptions.groundwithbounds=false
    stdoptions.splitdefs=false

    sol = minimize(T,S,penaltyTerm)[1]
    print(sol[V::penalty].ct)
}






